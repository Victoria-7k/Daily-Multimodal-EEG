-- Source contract for the portable HTML report.
-- The HTML artifact datasets are parsed from the reviewed Markdown report and checked against synced JSON summaries.
SELECT *
FROM eegpt_b0_a1_a2_report_snapshot;
