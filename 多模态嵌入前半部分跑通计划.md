# Daily Multimodal 多模态嵌入前半部分跑通计划

> **目标：** 跑通皮肤电、PPG、加速度、面部录像、录音、脑电的多模态信号编码流程，完成图中任务的前半部分：从原始多模态数据到可学习个体/事件级嵌入，不进入后半部分脑电教师模型微调和知识蒸馏。

**总体思路：** 以每条情绪评分事件为中心样本，使用 `absolute_onset_time` 做跨模态绝对时间对齐；EEG 使用 `RecordingStartTime + onset` 校验相对时间；PPG/GSR/ACC 使用 CSV 内绝对时间列切窗；视频与录音使用同日期 MP4 及其音频流切片。第一版先完成 manifest、单样本切窗、单样本 embedding、10 条样本 smoke test，再扩大到完整可用样本集。

**技术栈建议：** Python 3.10+、PyTorch、MNE、pandas、numpy、scipy、opencv/ffmpeg/ffprobe、torchaudio、transformers、OpenFace 或等价面部特征提取工具、pytest、YAML 配置。

---

## 1. 已确认数据位置与数据事实

### 1.1 服务器与访问别名

- SSH 别名：`ncc_serve_4090`
- 远端主机：`10.20.37.212:1022`
- 远端用户：`lzs`
- 已验证连接后远端默认目录：`/home/lzs`

### 1.2 原始数据根目录

- EEG 与评分：
  - `/mnt/dataset4/sitian/DailyEEG_dataset`
  - EEG：`sub-xx/ses-xx/eeg/*.bdf`
  - EEG 侧车 JSON：`sub-xx/ses-xx/eeg/*.json`
  - 情绪评分：`sub-xx/ses-xx/beh/*.tsv`
- 面部录像与录音：
  - `/mnt/dataset1/sitian/video`
  - 结构示例：`/mnt/dataset1/sitian/video/sub2/0228/*.MP4`
  - MP4 抽样确认包含音频流：AAC，48 kHz，双声道
  - 少数日期还有独立 `.AAC` 文件
- PPG、皮肤电、加速度：
  - `/mnt/dataset0/sitian/DailyEmotion/Wear_data/out`
  - PPG：`*_PPG.csv`，列为 `PPG,csv_time_PPG`
  - 皮肤电：`*_GSR.csv`，列为 `GSR,csv_time_GSR`
  - 加速度：`*_ACC.csv`，列为 `Motion_dataX,Motion_dataY,Motion_dataZ,csv_time_motion`
  - 另有汇总：`*.csv`、`*_min.csv`、`*.mat`

### 1.3 已巡检到的数据规模

- EEG 数据集：
  - 被试数：15，`sub-01` 到 `sub-15`
  - session 目录数：154
  - 有效 EEG BDF：152
  - 有效 EEG JSON：152
  - 有效评分 TSV：152
  - 总大小约：177.6 GB
  - EEG 采样率分布：`500 Hz` 145 段，`1000 Hz` 6 段，`250 Hz` 1 段
- 视频/录音目录：
  - 总大小约：5.2 TB
  - MP4：1428 个
  - AAC：13 个
  - 已看到 `sub2` 到 `sub15`，未看到 `sub1`
- 穿戴数据：
  - 总大小约：13.9 GB
  - CSV：952 个
  - MAT：191 个
  - 解析出 191 个记录时间段
  - 其中 190 个时间段同时包含 ACC、GSR、PPG
  - 缺 PPG 的一组：`20250614141219 -> 20250614175737`

### 1.4 事件级覆盖情况

以情绪评分行为单位统计：

- 总评分行数：1272
- 有同被试同日期视频目录的评分行：1103
- 有穿戴时间段覆盖的评分行：1137
- 有 ACC/GSR/PPG 三模态覆盖的评分行：1127
- 同时有视频日期和 ACC/GSR/PPG 的评分行：995

第一版完整多模态嵌入建议优先使用这 995 条评分事件作为可启动样本集。后续再处理缺视频、缺穿戴或仅部分模态可用的样本。

### 1.5 已知缺口与特殊情况

- `sub-01` 没有在视频目录中看到对应 `sub1`，因此 sub-01 暂时不能用于完整视频模态训练。
- `sub-01/ses-03` 被拆成 3 段 BDF/JSON/TSV，需要按段处理，不能简单当成单文件 session。
- 以下 session 目录存在但当前未看到 BDF/JSON/TSV：
  - `sub-05/ses-07`
  - `sub-13/ses-12`
  - `sub-13/ses-13`
  - `sub-15/ses-11`
- 穿戴文件名没有显式被试编号，当前应通过绝对时间窗口对齐。
- 视频目录日期是 `MMDD`，EEG/评分/穿戴是绝对年月日时间，需要用 EEG JSON 或评分 TSV 的年份补齐。
- MP4 的 `creation_time` 抽样显示是 UTC 时间，和北京时间对齐时需要明确时区换算策略。

---

## 2. 服务器工作目录与协作规范

### 2.1 必须创建自己的服务器工作目录

备注：需要在服务器合适位置创建一个属于自己的文件夹，名字为 `wzw`。当前路径固定为：

```bash
/mnt/dataset4/sitian/wzw
```

项目目录建议为：

```bash
/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding
```

原因：`/mnt/dataset4` 空间较大，且 EEG 数据位于 `/mnt/dataset4/sitian/DailyEEG_dataset`，将个人工作目录固定在 `/mnt/dataset4/sitian/wzw` 下更便于和原始数据相邻管理。所有代码、配置、manifest、日志、临时输出、embedding 输出都应写入该 `wzw` 目录下，不改动任何原始数据目录。

### 2.2 本地优先，服务器同步

后续工作规范 1：在本地创建和修改文件，再同步至服务器端的 `wzw` 文件夹下。

本地工作目录：

```text
G:\Daily Multimodal
```

固定同步方向：

```powershell
scp -r .\configs .\src .\scripts .\tests .\pyproject.toml ncc_serve_4090:/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding/
```

### 2.3 大规模运行前先做快速小规模可行性测试

后续工作规范 2：尽量在大规模、长时间程序运行前都先做快速的小规模可行性测试。

强制执行顺序：

1. 先跑 1 条评分事件，确认所有模态可定位、可切窗、可输出 embedding。
2. 再跑 10 条评分事件，确认批处理、日志、异常跳过、输出格式稳定。
3. 再跑 1 个被试的所有可用事件，确认跨日期和跨 session 情况。
4. 最后才跑全部完整多模态事件。

每次扩大规模前都必须检查：

- manifest 样本数是否符合预期。
- 每个模态的命中率是否符合预期。
- 输出 embedding shape 是否一致。
- 日志中是否有静默失败。
- 输出目录是否写在 `wzw` 下。

---

## 3. 建议项目架构

本地与服务器保持同一套项目结构：

```text
DailyMultimodalEmbedding/
  README.md
  pyproject.toml
  configs/
    paths.local.yaml
    paths.server.yaml
    windowing.yaml
    encoders.yaml
    smoke.yaml
  src/
    daily_multimodal/
      __init__.py
      config.py
      logging_utils.py
      schema.py
      manifest/
        __init__.py
        build_manifest.py
        validate_manifest.py
      io/
        __init__.py
        labels.py
        eeg.py
        wear.py
        video.py
        audio.py
      alignment/
        __init__.py
        time_utils.py
        event_windows.py
        coverage.py
      preprocessing/
        __init__.py
        eeg_preprocess.py
        physio_preprocess.py
        video_preprocess.py
        audio_preprocess.py
      encoders/
        __init__.py
        eeg_encoder.py
        ppg_gsr_encoder.py
        acc_encoder.py
        video_encoder.py
        audio_encoder.py
      fusion/
        __init__.py
        pack_embeddings.py
        masks.py
      export/
        __init__.py
        save_npz.py
        save_parquet.py
  scripts/
    00_check_environment.py
    01_build_manifest.py
    02_validate_alignment.py
    03_probe_one_event.py
    04_extract_one_event_embeddings.py
    05_extract_smoke_embeddings.py
    06_extract_subject_embeddings.py
    07_extract_all_embeddings.py
  tests/
    test_time_utils.py
    test_manifest_schema.py
    test_wear_filename_parse.py
    test_event_window.py
  outputs/
    manifests/
    embeddings/
    logs/
    reports/
```

### 3.1 配置文件职责

- `configs/paths.server.yaml`
  - 记录服务器原始数据根目录和 `wzw` 输出目录。
- `configs/windowing.yaml`
  - 记录每条评分事件的切窗范围，例如评分前 60 秒到评分后 0 秒，或评分前 30 秒到评分后 30 秒。
- `configs/encoders.yaml`
  - 记录每个模态的编码器、采样率、输出维度和 checkpoint 路径。
- `configs/smoke.yaml`
  - 记录小规模测试样本数，例如 `max_events: 10`。

### 3.2 核心数据结构

manifest 中每条记录至少包含：

```text
event_id
subject_id
session_id
segment_id
rating_row_index
absolute_onset_time
eeg_recording_start_time
eeg_onset_seconds
eeg_bdf_path
eeg_json_path
beh_tsv_path
wear_ppg_path
wear_gsr_path
wear_acc_path
video_day_dir
candidate_mp4_paths
has_eeg
has_ppg
has_gsr
has_acc
has_video
has_audio
label_columns
activity_category
social_presence
```

embedding 输出中每条记录至少包含：

```text
event_id
subject_id
session_id
absolute_onset_time
z_eeg
z_ppg
z_gsr
z_acc
z_video
z_audio
modality_mask
labels
source_paths
```

---

## 4. 分阶段执行计划

### 阶段 0：创建服务器工作目录

**目标：** 在服务器上建立个人工作空间，后续所有输出都写入该目录。

建议命令：

```bash
ssh ncc_serve_4090 "mkdir -p /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding/{configs,src,scripts,tests,outputs/manifests,outputs/embeddings,outputs/logs,outputs/reports}"
```

验收标准：

- `ssh ncc_serve_4090 "test -d /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding && echo OK"` 输出 `OK`。

### 阶段 1：本地项目脚手架

**目标：** 在本地建立项目结构，再同步到服务器。

本地创建内容：

- `configs/paths.server.yaml`
- `configs/windowing.yaml`
- `configs/encoders.yaml`
- `configs/smoke.yaml`
- `src/daily_multimodal/` 模块目录
- `scripts/` 运行入口
- `tests/` 单元测试

`paths.server.yaml` 建议内容：

```yaml
data_roots:
  eeg_dataset: /mnt/dataset4/sitian/DailyEEG_dataset
  video: /mnt/dataset1/sitian/video
  wear: /mnt/dataset0/sitian/DailyEmotion/Wear_data/out

work_root: /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding

outputs:
  manifests: /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding/outputs/manifests
  embeddings: /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding/outputs/embeddings
  logs: /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding/outputs/logs
  reports: /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding/outputs/reports
```

`windowing.yaml` 第一版建议内容：

```yaml
event_window:
  start_seconds: -60
  end_seconds: 0

video_audio:
  timezone: Asia/Shanghai
  mp4_creation_time_timezone: UTC

resample:
  eeg_hz: 250
  ppg_hz: 64
  gsr_hz: 32
  acc_hz: 32
  audio_hz: 16000
  video_fps: 1
```

验收标准：

- 本地文件存在。
- 同步到服务器后，服务器 `wzw` 项目目录结构一致。

### 阶段 2：构建只读 manifest

**目标：** 不读取大文件内容，只通过路径、JSON、TSV、文件名和时间戳生成事件级 manifest。

输入：

- EEG JSON：读取 `RecordingStartTime`、`RecordingDuration`、`SamplingFrequency`
- 评分 TSV：读取 `onset`、`duration`、`absolute_onset_time`、情绪标签列
- 穿戴文件名：解析 `start/end/modality`
- 视频目录：解析 `subN/MMDD`，先做日期级候选匹配

输出：

```text
outputs/manifests/events_manifest.parquet
outputs/reports/manifest_coverage.json
```

验收标准：

- manifest 行数应为 1272。
- 完整 ACC/GSR/PPG 覆盖事件数应接近已巡检值 1127。
- 视频日期覆盖事件数应接近已巡检值 1103。
- 完整多模态候选事件数应接近已巡检值 995。

### 阶段 3：视频与音频精确时间对齐

**目标：** 从日期级视频候选进一步定位到覆盖评分窗口的 MP4 文件。

步骤：

1. 对每个候选 MP4 调用 `ffprobe`。
2. 读取 `creation_time` 和 `duration`。
3. 将 `creation_time` 从 UTC 转为 `Asia/Shanghai`。
4. 判断 MP4 时间段是否覆盖评分事件窗口。
5. 对跨多个 MP4 的窗口，记录多个候选片段和相对切片时间。

验收标准：

- 每条有视频日期的事件都能得到 `video_candidates` 字段。
- 每个候选 MP4 记录：
  - `mp4_path`
  - `mp4_start_time`
  - `mp4_end_time`
  - `clip_start_seconds`
  - `clip_end_seconds`
  - `has_audio_stream`
- 抽样 10 条事件，人工检查时间换算合理。

### 阶段 4：单事件切窗 smoke test

**目标：** 选一条完整多模态事件，完成所有模态的切窗，不训练模型。

建议优先选择：

- 非 `sub-01`
- 同时有 EEG、PPG、GSR、ACC、视频、音频
- EEG 采样率为 500 Hz
- 视频目录中 MP4 覆盖评分时间

输出：

```text
outputs/reports/probe_one_event.json
outputs/reports/probe_one_event_shapes.txt
```

验收标准：

- EEG 切窗 shape 稳定。
- PPG/GSR/ACC 切窗非空。
- 视频帧能正常抽取。
- 音频 waveform 能正常抽取并重采样到 16 kHz。
- 所有切窗都来自原始数据，只写入 `wzw` 下的报告或临时输出。

### 阶段 5：单事件 embedding smoke test

**目标：** 对阶段 4 的同一条事件输出各模态 embedding。

建议第一版编码器：

- EEG：MNE 基础预处理后接轻量 1D CNN / PatchTST baseline，先随机初始化或使用已有 EEG checkpoint。
- PPG/GSR：先用统计特征 + 小型 MLP 跑通，再替换为 CLSP 类预训练模型。
- ACC：先用统计特征 + 小型 MLP 跑通，再替换为 SelfPAB。
- 视频：先用 OpenFace 导出面部特征，再做时间池化。
- 音频：使用 wav2vec2.0 提取 hidden states，再做时间池化。

输出：

```text
outputs/embeddings/one_event_embeddings.npz
```

验收标准：

- 每个模态输出一个固定维度向量。
- `modality_mask` 正确标记存在模态。
- 输出包含 `event_id` 和所有源路径。

### 阶段 6：10 条事件小规模可行性测试

**目标：** 验证批处理、异常处理、日志和输出格式。

运行范围：

- 从完整多模态候选事件中取 10 条。
- 尽量覆盖不同被试、不同日期、不同视频文件数量。

输出：

```text
outputs/embeddings/smoke_10_events.npz
outputs/logs/smoke_10_events.log
outputs/reports/smoke_10_events_report.json
```

验收标准：

- 10 条事件全部完成或明确记录失败原因。
- 每个成功事件的 embedding shape 一致。
- 日志中每个失败事件都有 `event_id`、模态、路径和异常类型。
- 程序运行时间可接受，再进入下一阶段。

### 阶段 7：单被试完整测试

**目标：** 选择一个覆盖较好的被试，跑完该被试所有可用评分事件。

建议优先被试：

- `sub-10`：评分行 103，视频日期覆盖 103，穿戴三模态覆盖 103。
- `sub-14`：评分行 103，视频日期覆盖 103，穿戴三模态覆盖 103。

输出：

```text
outputs/embeddings/sub-10_embeddings.npz
outputs/reports/sub-10_report.json
```

验收标准：

- 单被试事件级 embedding 全部可加载。
- 每个模态缺失率为 0 或有明确原因。
- 运行时间和显存占用被记录。

### 阶段 8：完整多模态候选集批处理

**目标：** 对 995 条完整候选事件批量提取 embedding。

输出：

```text
outputs/embeddings/all_complete_multimodal_embeddings.npz
outputs/manifests/all_complete_multimodal_manifest.parquet
outputs/reports/all_complete_multimodal_report.json
```

验收标准：

- 完成事件数接近 995。
- 每条事件都有 `event_id`、标签、各模态 embedding、mask、源路径。
- 失败事件单独输出失败清单。
- 输出可被后续多模态融合模型直接读取。

---

## 5. 模态处理细节

### 5.1 EEG

对齐依据：

- `RecordingStartTime`
- `onset`
- `absolute_onset_time`

处理原则：

- 以 `absolute_onset_time` 为主，使用 `RecordingStartTime + onset` 做一致性校验。
- 不同采样率统一重采样到 `250 Hz`。
- 第一版只做必要滤波、坏段检查和固定窗口裁剪，不做复杂清洗。

输出建议：

```text
z_eeg: float32[num_events, eeg_dim]
```

### 5.2 PPG 与 GSR

对齐依据：

- PPG：`csv_time_PPG`
- GSR：`csv_time_GSR`

处理原则：

- 用绝对时间切评分前窗口。
- 第一版保留原始序列，同时计算均值、标准差、斜率、分位数等统计特征。
- 后续替换或叠加 CLSP 预训练表征。

输出建议：

```text
z_ppg: float32[num_events, physio_dim]
z_gsr: float32[num_events, physio_dim]
```

### 5.3 加速度

对齐依据：

- `csv_time_motion`

处理原则：

- 使用 X/Y/Z 三轴。
- 增加合加速度模长 `sqrt(x^2 + y^2 + z^2)`。
- 第一版用统计特征 + 小型序列模型跑通。
- 后续替换或叠加 SelfPAB。

输出建议：

```text
z_acc: float32[num_events, acc_dim]
```

### 5.4 面部录像

对齐依据：

- 视频目录：`subN/MMDD`
- MP4 元信息：`creation_time + duration`
- 评分事件：`absolute_onset_time`

处理原则：

- 先用 `ffprobe` 建立 MP4 时间索引。
- 对覆盖窗口的 MP4 进行裁剪或直接按时间抽帧。
- 第一版建议每秒 1 帧或每 2 秒 1 帧，降低计算成本。
- OpenFace 输出面部 landmark、AU、head pose、gaze 等特征后池化。

输出建议：

```text
z_video: float32[num_events, video_dim]
```

### 5.5 录音

对齐依据：

- 优先使用 MP4 内音频流。
- 对有独立 `.AAC` 的日期，作为辅助候选，但第一版先以 MP4 内音轨为主。

处理原则：

- 从覆盖事件窗口的 MP4 中抽取音频。
- 重采样到 16 kHz。
- 使用 wav2vec2.0 提取 hidden states。
- 对时间维做 mean pooling 或 attention pooling。

输出建议：

```text
z_audio: float32[num_events, audio_dim]
```

---

## 6. 最小可行命令流

以下命令是后续项目实现后建议的运行顺序。

### 6.1 本地测试

```powershell
pytest .\tests -q
```

预期：

```text
所有单元测试通过
```

### 6.2 同步到服务器

```powershell
scp -r .\configs .\src .\scripts .\tests .\pyproject.toml ncc_serve_4090:/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding/
```

预期：

```text
文件同步完成，无 Permission denied
```

### 6.3 服务器环境检查

```bash
ssh ncc_serve_4090 "cd /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding && python scripts/00_check_environment.py --config configs/paths.server.yaml"
```

预期：

```text
EEG root exists
Video root exists
Wear root exists
ffprobe available
python packages available
```

### 6.4 构建 manifest

```bash
ssh ncc_serve_4090 "cd /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding && python scripts/01_build_manifest.py --config configs/paths.server.yaml --out outputs/manifests/events_manifest.parquet"
```

预期：

```text
events_total ~= 1272
complete_wear_events ~= 1127
video_day_events ~= 1103
complete_multimodal_candidates ~= 995
```

### 6.5 单事件探针

```bash
ssh ncc_serve_4090 "cd /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding && python scripts/03_probe_one_event.py --manifest outputs/manifests/events_manifest.parquet --require-all-modalities"
```

预期：

```text
EEG window OK
PPG window OK
GSR window OK
ACC window OK
Video clip OK
Audio clip OK
```

### 6.6 10 条事件 smoke test

```bash
ssh ncc_serve_4090 "cd /mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding && python scripts/05_extract_smoke_embeddings.py --manifest outputs/manifests/events_manifest.parquet --max-events 10"
```

预期：

```text
10 events processed or explicit failures logged
embedding file saved under outputs/embeddings
```

---

## 7. 风险与处理策略

### 7.1 视频时区风险

风险：MP4 `creation_time` 是 UTC，评分时间是本地时间。

处理：

- 统一将 MP4 `creation_time` 转为 `Asia/Shanghai`。
- 抽样检查若干事件的 EEG 时间、评分时间、视频时间是否落在同一日同一时段。

### 7.2 穿戴数据跨天风险

风险：穿戴文件名中的开始和结束时间可能跨天，不能只按日期匹配。

处理：

- 必须按绝对时间 interval 匹配。
- 同一天多段穿戴记录时选择覆盖评分窗口的那一段。

### 7.3 sub-01 缺视频

风险：sub-01 无法用于完整视频多模态。

处理：

- 完整多模态第一版排除 sub-01。
- 保留 sub-01 的 EEG + PPG/GSR/ACC + 音频/视频缺失 mask，用于后续缺模态实验。

### 7.4 EEG 多采样率

风险：不同采样率会造成模型输入长度不一致。

处理：

- 第一版统一重采样到 250 Hz。
- manifest 记录原始采样率。

### 7.5 大文件运行时间风险

风险：视频和 BDF 文件很大，直接全量跑会耗时且难定位错误。

处理：

- 严格执行单事件、10 事件、单被试、全量四级测试。
- 每一级都生成报告后再进入下一级。

---

## 8. 最终阶段产物

第一阶段完成后，至少应得到：

```text
outputs/manifests/events_manifest.parquet
outputs/manifests/all_complete_multimodal_manifest.parquet
outputs/embeddings/all_complete_multimodal_embeddings.npz
outputs/reports/manifest_coverage.json
outputs/reports/all_complete_multimodal_report.json
outputs/logs/*.log
```

其中 `all_complete_multimodal_embeddings.npz` 应能支持后续图中第二阶段模型：

- 生理信号嵌入：PPG、GSR
- 加速度嵌入：ACC
- 音视频信号嵌入：video、audio
- 脑电嵌入：EEG
- 个体/事件级可学习嵌入
- 模态缺失 mask
- 情绪标签与活动上下文标签

---

## 9. 第一轮建议执行顺序

- [ ] 在服务器创建 `wzw` 工作目录。
- [ ] 在本地建立项目脚手架。
- [ ] 写 `paths.server.yaml`、`windowing.yaml`、`smoke.yaml`。
- [ ] 同步本地项目到服务器 `wzw` 目录。
- [ ] 写并运行环境检查脚本。
- [ ] 构建只读 manifest。
- [ ] 校验 manifest 覆盖率是否接近当前巡检结果。
- [ ] 做单事件所有模态切窗。
- [ ] 做单事件 embedding。
- [ ] 做 10 条事件 smoke test。
- [ ] 做 `sub-10` 或 `sub-14` 单被试完整测试。
- [ ] 做 995 条完整多模态候选事件批处理。
