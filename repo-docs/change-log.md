# Repo-docs 变更记录

| Timestamp | Request | Actions | Verification | Result |
| --- | --- | --- | --- | --- |
| 2026-07-08 Asia/Shanghai | Execute post-structure-search video guidance: summarize current conclusion and run the next diagnostics | Added B1 window representation export (`scripts/41_build_video_window_repr_embeddings.py`, `src/daily_multimodal/training/video_window_repr.py`), affine few-shot personalization support in `scripts/39_run_video_personalization.py`, and centered cross-subject transfer matrix analysis (`scripts/42_analyze_video_transfer_matrix.py`, `src/daily_multimodal/training/video_transfer_matrix.py`). Generated local reports under `outputs/reports/video_roi_b1/`, `outputs/reports/video_window_b1/`, `outputs/reports/video_personalization/`, and `outputs/reports/video_transfer_matrix/`. Updated `docs/video_modality_stage_conclusions.md` and this command reference. Synced through 52a0676 plus current working tree. | TDD red observed for missing window representation and transfer modules and missing affine personalization option, then `python -m pytest tests/test_video_window_repr.py tests/test_video_personalization.py tests/test_video_transfer_matrix.py tests/test_video_event_aggregation.py tests/test_video_loso_diagnostics.py -q` passed 8 tests. `python -m pytest tests/test_video_grl_adapter.py tests/test_video_variant_ablation.py tests/test_video_embedding_probes.py -q` passed 25 tests and skipped 2. `python -m compileall -q src scripts tests` passed. `python C:\Users\28303\.codex\skills\repo-docs\scripts\validate_repo_docs.py repo-docs --repo-root .` reported OK with 0 errors and 23 warnings. | Current conclusion: video-only structure search is closed for now. Local R1/R2 B1 finalization is blocked by missing original ROI embeddings and has a server command report. Window-level subject-centered B1 lowers LOSO RMSE from `0.9933` to `0.8242` but r remains near zero (`0.0157`). Affine personalization improves original B1 RMSE at K=10 (`0.8813`) but not r, and does not beat centered C1 0-shot RMSE (`0.7983`). Transfer matrix shows strong within-subject centered mapping (diagonal r mean `0.6981`) but almost no cross-subject transfer (off-diagonal r mean `0.0089`). |
| 2026-07-07 Asia/Shanghai | Implement B1-frozen event-level modeling, LOSO diagnostics, and few-shot personalization | Added event-level B1 adapter aggregation (`scripts/37_build_video_event_embeddings.py`, `src/daily_multimodal/training/video_event_aggregation.py`), LOSO failure diagnostics and subject-centered diagnostic labels (`scripts/38_analyze_video_loso_failure.py`, `scripts/38_prepare_centered_video_targets.py`, `src/daily_multimodal/training/video_loso_diagnostics.py`), and residual-bias few-shot personalization (`scripts/39_run_video_personalization.py`, `src/daily_multimodal/training/video_personalization.py`). Generated local B1 event E1/E2/E3 bundles and reports under `outputs/embeddings/video_event_b1/`, `outputs/reports/video_event_b1/`, `outputs/reports/video_loso_diagnostics/`, and `outputs/reports/video_personalization/`. Updated `docs/video_modality_stage_conclusions.md` and `repo-docs/references/commands-and-artifacts.md`. Synced through 52a0676 plus current working tree. | TDD red observed for missing modules, then `python -m pytest tests/test_video_event_aggregation.py tests/test_video_loso_diagnostics.py tests/test_video_personalization.py -q` passed 5 tests. `python -m pytest tests/test_video_grl_adapter.py tests/test_video_variant_ablation.py tests/test_video_embedding_probes.py -q` passed 25 tests and skipped 2. `python -m compileall -q src scripts tests` passed. `python C:\Users\28303\.codex\skills\repo-docs\scripts\validate_repo_docs.py repo-docs --repo-root .` reported OK with 0 errors and 21 warnings. | B1 representation bundle was confirmed as `8328` windows, `694` events, `repr__B1.shape=(8328,64)`, no NaNs. Event pooling retained all `694` events but did not beat B1 window baseline on S4/S2, so 12-window TCN/Transformer remains gated off. LOSO diagnostics found mixed signs (`9` positive-r and `5` negative-r subjects). Event-level subject-centered E3 reached LOSO r `0.1385` / RMSE `0.8894` as diagnostic-only evidence for baseline calibration. Simple residual few-shot did not improve B1 0-shot: best r was 1-session `0.0170` with worse RMSE `0.9996`. |
| 2026-07-07 Asia/Shanghai | Run B1/B2 repeat stability and adapter-output representation audit | Added OOF representation export to `scripts/35_run_video_grl_adapter_ablation.py` / `src/daily_multimodal/training/video_grl_adapter.py`; added `scripts/36_analyze_video_grl_adapter.py` and `src/daily_multimodal/training/video_grl_adapter_analysis.py`; ran five-seed B1 vs B2_lam0.005 vs B2_lam0.01 LOSO/S4/S2 stability and LOSO B0/B1/B2 adapter-z audit on `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding`; synced result tables back under `outputs/server_sync/video_v4d_results_2026-07-07/server_tree/outputs/reports/video_grl_adapter/`. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_video_grl_adapter.py tests/test_video_domain_robust.py -q` passed 5 tests and skipped 4; local compileall and CLI help passed. Server `PYTHONPATH=src python -m pytest tests/test_video_domain_robust.py tests/test_video_grl_adapter.py -q` passed 9 tests. Server runner `outputs/reports/video_grl_adapter/run_b1_b2_repeat_and_repr_audit.sh` completed and wrote `b1_b2_repeat_stability/repeat_stability_summary.{json,md}` plus `representation_audit/b0_b1_b2_representation_audit.{json,md}`. | Five-seed mean r: LOSO B1 `-0.0010`, B2_lam0.005 `0.0025`, B2_lam0.01 `0.0026`; S4 B1 `0.1815`, B2_lam0.005 `0.1872`, B2_lam0.01 `0.1800`; S2 B1 `0.1868`, B2_lam0.005 `0.1784`, B2_lam0.01 `0.1638`. Adapter-z lowers Subject/Session Probe but Fatigue Ridge does not improve overall versus B0, so B1 gains look more like supervised adapter + nonlinear head fitting than a clearly better linear fatigue geometry. |
| 2026-07-07 Asia/Shanghai | Add and run B0-B5 adapter/GRL video ablation | Added `src/daily_multimodal/training/video_grl_adapter.py`, `scripts/35_run_video_grl_adapter_ablation.py`, and `tests/test_video_grl_adapter.py`; ran B0/B1 plus conservative B2/B3/B4 GRL lambda sweeps and B5_A1 on fixed-salt A0/A1. Updated `docs/video_modality_stage_conclusions.md` and this command reference. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_video_grl_adapter.py -q` passed 2 tests and skipped 1 torch smoke; local `python -m pytest tests/test_video_domain_robust.py tests/test_video_grl_adapter.py -q` passed 3 and skipped 3; local compileall and CLI help passed. Server `PYTHONPATH=src python -m pytest tests/test_video_domain_robust.py tests/test_video_grl_adapter.py -q` passed 6 tests. Server screening runner wrote `outputs/reports/video_grl_adapter/b0_b5_fixed_salt/b0_b5_grl_summary.{json,md}`. | B1 adapter lowered Subject Probe from B0 `0.9739` to `0.8707` and Session Probe from `0.9525` to `0.8869`, with r changing from B0 LOSO/S1/S4/S2 `-0.0392/0.3316/0.1546/0.1824` to B1 `-0.0005/0.3897/0.1821/0.2026`. GRL gains were mixed; B2_lam0.005, B3_lam0.05, and B4_lam0.005 are second-pass candidates, while B5_A1 did not beat B1. |
| 2026-07-07 Asia/Shanghai | Rerun fixed-salt A0/A1/A2 probes and LOSO/S1/S4/S2 | Ran the fixed projection-salt A0/A1/A2 train-only evaluation on `ncc_serve_4090`, keeping validation/test rows deterministic A0 upper-body while A1/A2 were used only as train-fold overrides. Updated `docs/video_modality_stage_conclusions.md` and `repo-docs/references/commands-and-artifacts.md` with the new summary. Synced through 2b95141 plus current working tree. | Server runner `outputs/reports/video_v4d_fixed_salt/run_a0_a2_fixed_salt_probes_eval.sh` completed and wrote `outputs/reports/video_v4d_fixed_salt/a0_a2_probe_eval_summary.{json,md}`, detailed downstream reports under `outputs/reports/video_variants/v4d_a0_a2_fixed_salt_train_only/`, and probe reports under `outputs/reports/video_probes/v4d_a0_a2_fixed_salt_train_only/`. | A0 subject `0.9583`, session `0.9525`, LOSO r `-0.0150`, S1 r `0.3357`, S4 r `0.1509`, S2 r `0.1241`; A1 subject `0.9583`, session `0.9523`, LOSO r `-0.0198`, S1 r `0.3254`, S4 r `0.1823`, S2 r `0.1387`; A2 subject `0.9568`, session `0.9520`, LOSO r `-0.0356`, S1 r `0.3250`, S4 r `0.1634`, S2 r `0.1556`. A1/A2 do not materially reduce subject/session shortcut and do not improve LOSO/S1, though A1 improves S4 and A2 improves S2. |
| 2026-07-07 Asia/Shanghai | Update saved V4d conclusions after fixed-salt audit | Updated `docs/video_modality_stage_conclusions.md`, the bilingual video roadmap, and command/artifact references so the old A1/A2 near-orthogonal paired-audit interpretation is marked invalid and superseded by the fixed projection-salt audit. Synced through 2b95141 plus current working tree. | Fixed-salt server audit reported A0-vs-A1 cosine mean `0.9990`, L2 mean `0.0408`, and A0-vs-A2 cosine mean `0.9938`, L2 mean `0.1076`; old pre-fix artifacts/reports remain archived under `outputs/archive/invalid_projection_salt_20260707_0125/`. | Current conclusion: A1/A2 are light-to-moderate perturbations under the shared projection. Old A0-A2 downstream/probe conclusions are invalid and should not be cited; rerun downstream/probes or mixed/interpolation experiments from the fixed-salt artifacts. |
| 2026-07-06 Asia/Shanghai | 固定 wear 预处理方式，全量重处理 wear 数据并重跑 Wear-only 实验 | 在服务器固定使用 `wear_signal_preprocessing_v1`，全量生成 `wear_physio_features_preprocessed_v1_full_mainface_embeddings.npz` 与 `wear_deep_sequence_preprocessed_v1_full_mainface_embeddings.npz`；随后用同一套 `scripts/22_run_wear_quality_ablation.py` 重跑 W1-W7 + W5a-W5d，并补充 raw/preprocessed mean baseline 与质量指标对比。Synced through current working tree。 | 本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding tests.test_wear_quality_ablation` 通过 16 tests；服务器同组 pytest 通过 16 tests；preprocessed physio/deep 全量均 `embedded_count=7584`、`success_count=7584`、`failure_count=744`、NaN `0`；preprocessed W1-W7/W5a-W5d 表已写入 `outputs/reports/wear_quality_ablation_preprocessed_w1_w7_w5abcd_fatigue_table.md`。 | 完成。全量质量对比：A/B/C 从 raw `3307/2901/1376` 变为 preprocessed `4466/2596/522`，motion artifact risk `3189 -> 984`，ACC mean motion_intensity `0.2487 -> 0.0838`，ACC median stationary_ratio `0.2539 -> 0.7618`；代价是 PPG HR plausible `5516 -> 4986`。下游：preprocessed W1 physio full RMSE `0.8824`、r `0.0045`；W2 deep full RMSE `0.8921`、r `-0.0142`；W6 physio A+B RMSE `0.8804`、r `-0.0059`；W7 deep A+B RMSE `0.8885`、r `-0.0101`。Raw-vs-preprocessed 对比见 `outputs/reports/wear_quality_ablation_raw_vs_preprocessed_fatigue_compare.md`，mean baseline 见 `outputs/reports/wear_mean_baseline_raw_vs_preprocessed_fatigue_table.md`。 |
| 2026-07-06 Asia/Shanghai | 抽样检查 wear 是否有真实预处理痕迹；没有则增加预处理并展示质量指标和波形图 | 抽样读取服务器 `wear_physio_features_v2_full_mainface_quality_audit_embeddings.npz` 的 12 个 `quality_flags`，只发现 `motion_artifact_risk` 这类质量标记，没有滤波/平滑/伪迹校正字段。新增 `wear_physio_features_preprocessed_v1` / `wear_deep_sequence_preprocessed_v1`，实现 `wear_signal_preprocessing_v1`：PPG robust winsorize + 近似 0.5-5Hz bandpass，GSR robust winsorize + 约 1Hz lowpass，ACC robust winsorize + median gravity removal + 约 5Hz smoothing；新增质量字段和测试。Synced through current working tree。 | 本地先跑新增单测得到 `KeyError: wear_preprocessing_applied`，再实现后该测试通过；本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding tests.test_wear_quality_ablation` 通过 16 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_wear_real_embedding.py -q` 通过 14 tests；服务器抽取 50 个 `has_wear` 窗口 smoke，raw 与 preprocessed 均 `success_count=50`、NaN `0`。 | 完成抽样验证，未做全量重跑。对比报告在 `outputs/reports/wear_preprocessing_smoke50_compare.json/.md`，波形图在 `outputs/reports/wear_preprocessing_smoke50_waveforms/`。50 窗口中：PPG peak insufficient `1 -> 0`，GSR SCR abnormal `1 -> 0`，ACC mean motion_intensity `0.1756 -> 0.0667`，ACC median stationary_ratio `0.3997 -> 0.8527`，A/B/C 从 `32/16/2` 变为 `35/15/0`，motion artifact risk `14 -> 5`；PPG HR plausible `37 -> 36` 略降，需要后续全量前再调参。 |
| 2026-07-05 Asia/Shanghai | Add V4d A0-A3 train-only appearance ablations | Added A1/A2/A3 augmentation profiles to `dinov2_roi.py` and `scripts/27_extract_dinov2_roi_embeddings.py`: A1 color+brightness, A2 A1+random grayscale, and A3 A2+crop/scale jitter. Added fold-aware train-only embedding overrides to `scripts/26_run_video_variant_ablation.py` via `eval.npz::train.npz` variant specs and to `scripts/28_run_video_embedding_probes.py` via `--train-embeddings`, so validation/test always use deterministic A0 upper-body embeddings. Synced through 2b95141 plus current working tree. | TDD red observed for missing A1/A2/A3 profiles, missing train override support in downstream evaluation, and missing `train_embeddings` probe support. Local `python -m pytest tests/test_dinov2_roi_embeddings.py tests/test_video_variant_ablation.py tests/test_video_embedding_probes.py -q` passed 37 tests; local compileall passed. Server focused tests passed 37 tests and server compileall passed. Server A1/A2/A3 one-window smokes verified expected encoder versions, augmentation ops, no NaNs, masks `[[0,0,1,0]]`, and failures `[]`. | A0-A3 ablation is now executable without test-time augmentation. Full train-only A1/A2/A3 embedding generation is running sequentially on `ncc_serve_4090` as PID `2211748`; A1 was active with failures `[]` at the last check. |
| 2026-07-05 Asia/Shanghai | Queue strict A0-A3 train-only evaluation runner | Added the server-side run record for `outputs/reports/video_v4d_ablation/run_a0_a3_train_only_eval.sh`. The runner waits for A1/A2/A3 train-only artifacts, validates them against deterministic A0 sample IDs/subjects/events/labels, then runs downstream LOSO/S1/S4/S2 and probes with deterministic upper-body embeddings on every validation/test row. Synced through 2b95141 plus current working tree. | Local focused split-hygiene tests `python -m pytest tests/test_video_variant_ablation.py tests/test_video_embedding_probes.py -q` passed 21 tests. Remote process check showed A1 train-only extraction running as PID `2211765` under generator PID `2211748`, no matching global augmented eval extractor, and eval runner PID `1779027` waiting for `outputs/embeddings/video_v4d_A1_upper_body_train_embeddings.npz`. | Wait for the generator to finish A1/A2/A3, then inspect `outputs/reports/video_variants/v4d_a0_a3_train_only/` and `outputs/reports/video_probes/v4d_a0_a3_train_only/`. |
| 2026-07-06 Asia/Shanghai | Stop A3 after paired embedding audit | Completed A0-A2 train-only downstream/probe analysis and added paired embedding audit outputs under `outputs/reports/video_v4d_ablation/paired_embedding_audit/`. Stopped A3 because A1/A2 already showed that the augmented embedding distribution was too far from A0 for same-window pairs. Synced through 2b95141 plus current working tree. | Remote paired audit showed A0-vs-A1 cosine mean `-0.0189`, L2 mean `1.4268`, and A0-vs-A2 cosine mean `0.0196`, L2 mean `1.3995`. Remote process check at 2026-07-06 23:00 Asia/Shanghai confirmed PIDs `3403028`, `2211748`, and `1779027` were no longer running; A1/A2 artifacts remained present and A3 final `.npz` was not written. | Use A0-A2 reports for interpretation. Do not wait for or cite A3; future V4d work should use milder or mixed original/augmented embeddings instead of full train replacement. |
| 2026-07-06 Asia/Shanghai | Add Weak-Aug V4d branch | Added `v4d_weak_color_brightness_contrast` to the DINOv2 ROI augmentation profiles and CLI choices. The profile uses only weak brightness/contrast/color jitter and avoids grayscale, blur, and crop/scale jitter. Synced through 2b95141 plus current working tree. | TDD red observed for unsupported Weak-Aug profile, then local `python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 17 tests. Server focused tests passed 17 tests, and server one-window smoke verified encoder version `video_v4d_weak_aug_dinov2_upper_body_full_frame_fallback_mean_std_max`, mask `[[0,0,1,0]]`, ops `['weak_brightness', 'weak_contrast', 'weak_color_jitter']`, and failures `[]`. | Full Weak-Aug train-only extraction/evaluation is running on `ncc_serve_4090` as runner PID `380378` and extractor PID `380420`. Results will appear under `outputs/reports/video_v4d_weak/`, `outputs/reports/video_variants/v4d_weak_train_only/`, and `outputs/reports/video_probes/v4d_weak_train_only/`. |
| 2026-07-07 Asia/Shanghai | Regenerate A1/A2 after projection-salt correction | Confirmed that previous A1/A2 paired audit was confounded by different projection matrices. The code now uses `_projection_salt_for_region(...)` so augmented variants share the base V4a region projection salt while retaining variant-specific `encoder_version`. Old A1/A2 artifacts and old A0-A2 reports were archived under `outputs/archive/invalid_projection_salt_20260707_0125/`. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 18 tests and local compileall passed. Server focused tests passed 18 tests. Remote function check returned `projection_salt_upper=video_v4a_dinov2_upper_body_mean_std_max` while A1 encoder version remained `video_v4d_a1_dinov2_upper_body_full_frame_fallback_mean_std_max`. | Fixed-salt A1/A2 regeneration is running on `ncc_serve_4090` as runner PID `471015`, A1 extractor PID `471891`. Wait for `outputs/reports/video_v4d_fixed_salt/paired_embedding_audit/` before reinterpreting augmentation strength or running mixed/interpolation training experiments. |
| 2026-07-05 Asia/Shanghai | Correct V4d augmentation evaluation protocol | Stopped the full `video_v4d_appearance_aug_dinov2_upper_body_embeddings.npz` extraction because pre-augmenting every row in a global `.npz` would also augment validation/test rows if used directly for probes or split evaluation. Updated the bilingual roadmap and command references to require train-fold-only random augmentation and deterministic original upper-body inputs for validation/test. Synced through 2b95141 plus current working tree. | Checked the server run: it had reached about 5.9% with failures `[]`; killed the remaining writer process and verified no matching extraction process remained. Updated `docs/superpowers/plans/2026-07-04-video-modality-roadmap-{en,zh}.md` and `repo-docs/references/commands-and-artifacts.md` so the invalid global full augmented `.npz` route is not presented as an evaluation artifact. | V4d appearance augmentation remains the priority, but the next implementation must be fold-aware: training may use random augmented views, while validation/test probes and LOSO/S1/S4/S2 evaluation must use original deterministic upper-body embeddings. |
| 2026-07-05 Asia/Shanghai | Continue V4d appearance augmentation and start full upper-body run | Added the `v4d_appearance_mild` DINOv2 augmentation profile with random-grayscale and light-blur ops, kept the older `v4d_mild_color_crop_scale` profile compatible, exposed the new profile in `scripts/27_extract_dinov2_roi_embeddings.py`, batched original plus augmented views into one frame-encoder pass when possible, and added OpenCV fast paths for blur/resize with NumPy fallbacks. Synced through 2b95141 plus current working tree. | TDD red observed for the missing `v4d_appearance_mild` profile and for the previous two-call augmentation batching path. Local `python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 15 tests and local `python -m compileall -q src scripts tests` passed. Server `PYTHONPATH=src python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 15 tests and server compileall passed. Server upper-body appearance smokes verified `(1,256)` and `(10,256)` outputs, expected encoder version/quality flags, no NaNs, and failures `[]`; OpenCV fast paths improved the 10-window timing smoke from about 251 seconds to about 41 seconds with `--batch-size 32`. | Superseded by the protocol correction above: the full global appearance-augmented extraction was stopped and must not be used as a split-evaluation artifact. The augmentation implementation remains useful for a future fold-aware train-only path. |
| 2026-07-05 Asia/Shanghai | Re-prioritize V4d after the ROI geometry audit | Updated the bilingual video roadmap and command references so V4d now prioritizes appearance augmentation on upper-body ROI before lightweight ROI stabilization, re-probe/evaluation, and only then GRL. The rationale records DINOv2 Session Probe `0.969` versus Geometry-only Session Probe `0.364`, pointing to appearance cues rather than simple ROI geometry as the dominant remaining session shortcut. Synced through 2b95141 plus current working tree. | Verified the new priority language appears in both `docs/superpowers/plans/2026-07-04-video-modality-roadmap-en.md` and `docs/superpowers/plans/2026-07-04-video-modality-roadmap-zh.md`, and in `repo-docs/references/commands-and-artifacts.md`. Ran `python C:\Users\28303\.codex\skills\repo-docs\scripts\validate_repo_docs.py repo-docs --repo-root .`: `OK: 0 errors, 17 warning(s)`. | V4d execution order is now appearance augmentation first, lightweight ROI stabilization second, probes/evaluation third, and GRL only if subject/session shortcuts remain high. |
| 2026-07-05 15:05 Asia/Shanghai | Make V4d DINOv2 extraction chunk-resumable | Added `start_index` support to `build_dinov2_roi_embeddings` and `--start-index` to `scripts/27_extract_dinov2_roi_embeddings.py`, preserving existing `--max-windows` semantics while allowing full V4d extraction to be split into recoverable chunks. Summary output now records `start_index` and `source_row_count`. Synced through 2b95141 plus current working tree. | TDD red observed locally with `TypeError: build_dinov2_roi_embeddings() got an unexpected keyword argument 'start_index'`; after implementation, local `python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 13 tests and CLI help exposed `--start-index`. Server `PYTHONPATH=src python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 13 tests; server compileall passed. A real server V4d augmentation chunk smoke with `--start-index 10 --max-windows 1` wrote `outputs/embeddings/video_v4d_aug_dinov2_2xroi_chunk_smoke_start10_embeddings.npz` and the frame-sequence bundle; verification matched sample id `sub-02_ses-01_00_row-0001_win-0010`, shape `(1,256)` / `(1,16,768)`, mask `[[0,0,1,0]]`, no NaNs, failures `[]`, and augmentation flags. | Full V4d Step 5 remains pending, but the extraction stage can now be resumed/chunked instead of losing all progress after a long interrupted run. |
| 2026-07-05 14:35 Asia/Shanghai | Formalize the V4d Step 5 success gate | Added `src/daily_multimodal/training/video_v4d_success.py`, `scripts/32_check_video_v4d_success.py`, and `tests/test_video_v4d_success.py`. The gate compares baseline and candidate probe/downstream reports and passes only when subject/session probe accuracy strictly drops while LOSO/S4/S2 fatigue RMSE does not increase and Pearson r does not decrease. Synced through 2b95141 plus current working tree. | TDD red observed locally with `ModuleNotFoundError` before implementation; a subsequent negative-control test caught the first bug where equal probe accuracy was accepted. Local `python -m pytest tests/test_video_v4d_success.py -q` passed 4 tests; `python scripts/32_check_video_v4d_success.py --help` passed; server `PYTHONPATH=src python -m pytest tests/test_video_v4d_success.py -q` passed 4 tests; server compileall passed. Server V4a self-check wrote `outputs/reports/video_v4d_success/v4a_self_check.{json,md}` and exited with the expected gate failure (`passed=False`). | Task 6 Step 5 is not complete because no full V4d artifact/evaluation exists yet, but its pass/fail criterion is now executable and rejects unchanged subject/session probes. |
| 2026-07-05 13:55 Asia/Shanghai | Add V4d subject/session adversarial interface | Added `src/daily_multimodal/embeddings/video_domain_robust.py` and `tests/test_video_domain_robust.py`. The module provides stable subject/session target encoding, PyTorch gradient reversal, subject/session adversarial heads, and combined adversarial loss metrics without changing the current `.npz` embedding contract. Synced through 2b95141 plus current working tree. | TDD red observed locally with `ModuleNotFoundError` before implementation. Local `python -m pytest tests/test_video_domain_robust.py -q` passed 1 test and skipped 2 PyTorch tests because local PyTorch is absent. Server `lzs` environment `PYTHONPATH=src python -m pytest tests/test_video_domain_robust.py -q` passed 3 tests, including gradient sign reversal and head/loss coverage. Server `PYTHONPATH=src python -m compileall -q src/daily_multimodal/embeddings/video_domain_robust.py tests/test_video_domain_robust.py` passed. | Task 6 Step 4 is complete at the reusable interface level. Task 6 Step 5 remains pending because it requires a full V4d artifact/training run and post-run subject/session/fatigue probes. |
| 2026-07-05 13:20 Asia/Shanghai | Freeze explicit V4d stable ROI policy | Added `--fallback-video-region full_frame` to `scripts/27_extract_dinov2_roi_embeddings.py` / `dinov2_roi.py` for region-cache extraction. The policy lets V4d runs request `upper_body` first and fall back to `full_frame` only when the requested cache clip is missing, while leaving the default R1/2x face ROI baseline unchanged. Quality flags now record requested/effective/fallback regions and whether fallback was used. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 12 tests; local `python -m compileall -q src scripts tests` passed; remote `PYTHONPATH=src python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 12 tests; remote `PYTHONPATH=src python -m compileall -q src scripts tests` passed; CLI help exposes `--fallback-video-region`; server smoke over one real upper-body cached window wrote `(1,256)` embedding with mask `[[0,0,1,0]]`, no NaNs, failures `[]`, encoder version `video_v4a_dinov2_upper_body_full_frame_fallback_mean_std_max`, and fallback quality flags. | Task 6 Step 3 is complete at interface, test, and smoke level. True missing-upper-body fallback behavior is covered by local and remote unit tests; full V4d augmented extraction/evaluation remains pending because the earlier full run was stopped under shared GPU saturation. |
| 2026-07-05 12:40 Asia/Shanghai | Start Task 6 V4d robustness with raw-frame augmentation | Added `--augmentation-profile v4d_mild_color_crop_scale` and `--augmentation-views` to `scripts/27_extract_dinov2_roi_embeddings.py` / `dinov2_roi.py`. The profile applies deterministic brightness, contrast, color jitter, crop jitter, and scale jitter before frozen DINOv2 encoding, averages original plus augmented frame embeddings, writes V4d encoder versions, and records augmentation quality flags. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 11 tests; local `python -m compileall -q src scripts tests` passed; remote `PYTHONPATH=src python -m pytest tests/test_dinov2_roi_embeddings.py -q` passed 11 tests; remote `PYTHONPATH=src python -m compileall -q src scripts tests` passed; server smoke over one real 2xROI window wrote `(1,256)` embedding and `(1,16,768)` frame sequence with mask `[[0,0,1,0]]`, no NaNs, failures `[]`, and five augmentation ops in quality flags. | Task 6 Step 1/2 are complete at the interface and smoke level. A full V4d 2xROI extraction was started but stopped after 6 hours because shared GPUs were saturated and progress was only about 35.6%; no full V4d downstream result is claimed yet. |
| 2026-07-05 06:20 Asia/Shanghai | Complete Task 5 R1/R2/R3 video region comparison | Finished the grouped upper-body/full-frame region cache, extracted V4a DINOv2 embeddings for R2 upper-body and R3 full-frame, then evaluated R1/R2/R3 under LOSO, S1, S4, and S2 with `scripts/26_run_video_variant_ablation.py`. Updated the bilingual roadmap execution notes. Synced through 2b95141 plus current working tree. | Region cache validation: upper-body videos/sidecars `8328/8328`, full-frame videos/sidecars `8328/8328`, manifest rows `16656`, failure count `0`. R2 and R3 embedding validation: each `face_emb.shape=(8328,256)`, frame sequences `(8328,16,768)`, face mask sum `8328`, no NaN values, failures `[]`. Region comparison summary written to `outputs/reports/video_variants/regions/region_comparison_summary.md`. | Completed Task 5 for the video roadmap. Decision: keep R1 / 2x face ROI as the default baseline for now because R2 upper-body improves LOSO/S1/S4 RMSE but is worse on S2; R3 full-frame is not default because it hurts LOSO/S2 RMSE despite stronger S1/S4 Pearson r. |
| 2026-07-04 23:45 Asia/Shanghai | Speed up Task 5 upper/full region cache generation | Added event-level grouped writing to `src/daily_multimodal/embeddings/video_regions.py` for default OpenCV `upper_body`/`full_frame` runs: rows are grouped by source video plus event, the source is decoded once per group, and sidecars record `region_source=source_video_event_group`. Updated `tests/test_video_regions.py`, command references, and the embedding contract. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_video_regions.py -q` passed 9 tests; local `python -m compileall -q src scripts tests` passed; remote `PYTHONPATH=src python -m pytest tests/test_video_regions.py -q` passed 9 tests; remote `PYTHONPATH=src python -m compileall -q src scripts tests` passed. | In progress for Task 5: the old 16 chunk jobs were stopped and restarted as grouped chunks under `outputs/reports/video_regions_chunks16_grouped/` with logs `outputs/logs/video_regions_chunks/chunk16_grouped_*.log`. After about 10 minutes, all 16 grouped PIDs were still active, failures had not appeared in logs, and upper/full cache clips had increased to 4736. |
| 2026-07-04 23:10 Asia/Shanghai | Keep Task 5 moving while upper/full region cache is still decode-bound | Added `--direct-video-region-from-window` to `scripts/27_extract_dinov2_roi_embeddings.py` for `upper_body` and `full_frame`, allowing V4a DINOv2 smoke/debug extraction directly from source-video frames with the same region semantics; increased the background region-cache batch to 16 resume-safe chunks because the server has CPU headroom. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_dinov2_roi_embeddings.py tests/test_video_regions.py -q` passed 18 tests; local `python -m compileall -q src scripts tests` passed; remote `PYTHONPATH=src python -m pytest tests/test_dinov2_roi_embeddings.py tests/test_video_regions.py -q` passed 18 tests. Server direct DINOv2 smoke over two windows wrote `(2,256)` upper-body and full-frame embeddings with mask sum `2`, failures `[]`, no NaN values, and encoder versions `video_v4a_dinov2_upper_body_mean_std_max` / `video_v4a_dinov2_full_frame_mean_std_max`. Full direct extraction was stopped after observing slow one-window source-video decoding under current server load. | In progress for Task 5: direct R2/R3 extraction is available for verified smoke/debug, but the planned full ROI comparison still waits on full R2/R3 artifacts; region cache generation continues in 16 chunks. |
| 2026-07-04 22:55 Asia/Shanghai | Reduce memory pressure in full video region cache chunks | Scoped the OpenCV frame cache to one window and changed region frame sampling so each source window is decoded sequentially while retaining only the 16 target frames, instead of storing all decoded frames from the 10-second source window. Restarted the server chunk jobs with resume-safe skip-existing behavior. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_video_regions.py -q` passed 8 tests and `python -m compileall -q src scripts tests` passed; remote `PYTHONPATH=src python -m pytest tests/test_video_regions.py -q` passed 8 tests. After restart, per-process RSS dropped from roughly 1GB+ peaks to about 300-650MB while the cache continued growing. | In progress for Task 5: upper-body/full-frame cache generation is still decode-bound, but it is now safer for the full 8328-window run and can resume without rewriting existing clips. |
| 2026-07-04 22:35 Asia/Shanghai | Make video region cache reruns resume-safe while full chunks are running | Added default skip-existing behavior to `build_video_region_cache` and `scripts/29_prepare_video_regions.py`, plus `--no-skip-existing` and `--progress-every`; synced the updated files to `ncc_serve_4090` for future retry chunks without interrupting the already-running jobs. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_video_regions.py -q` passed 7 tests; remote `PYTHONPATH=src python -m pytest tests/test_video_regions.py -q` passed 7 tests; full upper/full region cache jobs were still running and had written 1946 clips after about 31 minutes. | In progress for Task 5: region cache generation remains the active long-running step, but future restarts can reuse existing `window.mp4`/`region.json` pairs instead of rewriting them. |
| 2026-07-04 19:25 Asia/Shanghai | Close the V4a-to-V4b frame-sequence artifact gap | Added optional `--frame-sequences-out` support to `scripts/27_extract_dinov2_roi_embeddings.py` / `dinov2_roi.py`, writing aligned `frame_embeddings`, `frame_sequence_mask`, metadata, `modality_mask`, and encoder version for V4b; updated `video_temporal.py` so V4b respects source `modality_mask[:,2]` and masks source-unusable rows as `source_video_mask_zero`. Synced through 2b95141 plus current working tree. | Watched `python -m pytest tests/test_dinov2_roi_embeddings.py::test_build_dinov2_roi_embeddings_can_write_frame_sequence_bundle_for_v4b -q` fail first on missing `frame_sequences_out`, then pass; watched `python -m pytest tests/test_video_temporal_embeddings.py::test_video_temporal_embeddings_respect_source_video_mask -q` fail first on `mask_sum=2`, then pass after source-mask handling; on server `ncc_serve_4090`, a real one-window V4a smoke over the 0.5 FPS ROI cache wrote `face_emb.shape=(1,256)`, `frame_embeddings.shape=(1,16,768)`, `sampled_frame_count=16`, `usable_frame_count=16`, and failures `[]`; the full offline server run wrote `face_emb.shape=(8328,256)`, `frame_embeddings.shape=(8328,16,768)`, `face_mask_sum=8328`, `subject_count=14`, no NaN values, and failures `[]`. | Completed Task 1 for the video roadmap: V4a full embedding and frame-sequence artifacts are available for probes, V4b, and downstream split evaluation. |
| 2026-07-04 20:50 Asia/Shanghai | Run V4a probe diagnostics on the full DINOv2 artifact | Ran `scripts/28_run_video_embedding_probes.py` on `outputs/embeddings/video_v4a_dinov2_2xroi_embeddings.npz` for LOSO, S1, S4, and S2 fatigue Ridge splits, while also recording subject and within-subject session Logistic probes. Synced through 2b95141 plus current working tree. | Server reports written to `outputs/reports/video_probes/v4a_loso_probes.{json,md}`, `v4a_s1_probes.{json,md}`, `v4a_s4_probes.{json,md}`, and `v4a_s2_probes.{json,md}`. Summary: subject accuracy `0.9777`, session accuracy `0.9691`; fatigue LOSO RMSE `1.0333`, r `-0.0460`; S1 RMSE `0.9188`, r `0.2746`; S4 RMSE `0.9620`, r `0.1702`; S2 RMSE `1.0172`, r `0.2254`. | Completed Task 2 for the video roadmap: V4a carries strong subject/session shortcut signals, and same-subject event split is meaningfully better than cross-subject LOSO. |
| 2026-07-04 21:05 Asia/Shanghai | Freeze the V4a downstream split table | Ran `scripts/26_run_video_variant_ablation.py` with `--sample-mode behavior_retained` on `outputs/embeddings/video_v4a_dinov2_2xroi_embeddings.npz` under LOSO, S1, S4, and S2, then wrote `outputs/reports/video_variants/v4a/v4a_split_summary.md` on the server. Synced through 2b95141 plus current working tree. | Server metrics: LOSO RMSE `1.0083`, r `-0.0517`; S1 RMSE `0.8801`, r `0.3258`; S4 RMSE `0.9477`, r `0.1788`; S2 RMSE `0.9703`, r `0.2272`. | Completed Task 3 for the video roadmap: the V4a baseline table is frozen, and the diagnostic rule points to V4b temporal modeling plus ROI comparison before V4d robustness. |
| 2026-07-04 21:20 Asia/Shanghai | Generate and evaluate V4b temporal embeddings | Built `outputs/embeddings/video_v4b_tcn_dinov2_2xroi_embeddings.npz` and `outputs/embeddings/video_v4b_temporal_transformer_dinov2_2xroi_embeddings.npz` from the V4a frame-sequence artifact, verified both `(8328,256)` outputs, then compared V4a/V4b-TCN/V4b-TT under LOSO, S1, S4, and S2 with `scripts/26_run_video_variant_ablation.py`. Synced through 2b95141 plus current working tree. | Summary written to `outputs/reports/video_variants/v4b/v4a_v4b_temporal_summary.md`. Key metrics: LOSO V4a/V4b-TCN/V4b-TT RMSE `1.0083/0.9822/1.0063`, r `-0.0517/-0.0217/-0.0427`; S1 RMSE `0.8801/0.8703/0.8678`, r `0.3258/0.3192/0.3275`; S4 RMSE `0.9477/0.9536/0.9370`, r `0.1788/0.1812/0.1962`; S2 RMSE `0.9703/1.0197/0.9882`, r `0.2272/0.0634/0.1706`. | Completed Task 4 for the video roadmap: temporal modeling provides only small split-specific gains and does not resolve cross-subject or chronological generalization. |
| 2026-07-04 21:45 Asia/Shanghai | Wire video region cache into DINOv2 extraction | Added `--region-cache-root` and `--video-region` to `scripts/27_extract_dinov2_roi_embeddings.py` / `dinov2_roi.py`, with region-specific encoder versions for upper-body and full-frame inputs; changed the default `video_regions.py` writer to emit compact OpenCV-sampled 16-frame, 640px-wide clips instead of copying/transcoding full source MP4s. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_dinov2_roi_embeddings.py tests/test_video_regions.py tests/test_video_temporal_embeddings.py tests/test_video_embedding_probes.py tests/test_video_variant_ablation.py tests/test_video_variant_embeddings.py -q` passed 44 tests; remote `tests/test_video_regions.py` and `tests/test_dinov2_roi_embeddings.py` passed; server one-window region smoke wrote all three regions with failures `[]`; DINOv2 smoke from `upper_body` and `full_frame` region caches wrote `(1,256)` embeddings with encoder versions `video_v4a_dinov2_upper_body_mean_std_max` and `video_v4a_dinov2_full_frame_mean_std_max`. | In progress for Task 5: R1/R2/R3 interfaces are connected and smoke-tested; full region extraction/comparison remains a chunked server batch job because raw-video region generation is I/O-bound. |
| 2026-07-04 22:10 Asia/Shanghai | Start chunked full upper-body/full-frame region cache generation | Added `--start-index`, `--manifest-out`, and `--failures-out` to `scripts/29_prepare_video_regions.py`; fixed upper-body expansion to treat `face_presence.main_face_bbox` as OpenCV `[x,y,w,h]`; changed the default writer to read each source window sequentially and reuse sampled frames between `upper_body` and `full_frame`. Launched eight background chunks on `ncc_serve_4090` for `upper_body` and `full_frame` into `outputs/cache/video_regions`. Synced through 2b95141 plus current working tree. | Local `python -m pytest tests/test_dinov2_roi_embeddings.py tests/test_video_regions.py tests/test_video_temporal_embeddings.py tests/test_video_embedding_probes.py tests/test_video_variant_ablation.py tests/test_video_variant_embeddings.py -q` passed 47 tests; remote `tests/test_video_regions.py` passed 6 tests; chunk smoke with `--start-index 5 --max-windows 2` wrote 4 clips with failures `0`; after starting full chunks, all eight PIDs were running and the cache contained 380 upper/full clips after about 5 minutes. | In progress for Task 5: full R2/R3 region cache generation is underway; DINOv2 extraction and split comparison will run after chunk completion. |
| 2026-07-04 19:05 Asia/Shanghai | Continue executing the video roadmap with video region cache preparation | Added `src/daily_multimodal/embeddings/video_regions.py`, `scripts/29_prepare_video_regions.py`, and `tests/test_video_regions.py`; implemented the planned `2x_face_roi`, `upper_body`, and `full_frame` cache paths, per-region sidecars, manifest/failure outputs, and explicit `upper_body_fallback_full_frame` metadata when localization is unavailable. Synced through 2b95141 plus current working tree. | Watched `python -m pytest tests/test_video_regions.py -q` fail first with missing `daily_multimodal.embeddings.video_regions`, then pass after implementation; `python -m pytest tests/test_video_regions.py tests/test_video_temporal_embeddings.py tests/test_dinov2_roi_embeddings.py tests/test_video_embedding_probes.py tests/test_video_variant_ablation.py tests/test_video_variant_embeddings.py -q` passed 38 tests; `python -m compileall -q src scripts tests` passed; `python scripts\29_prepare_video_regions.py --help` passed; repo-docs validator returned `OK: 0 errors, 16 warning(s)`. | In progress for the full roadmap: local region cache interfaces are ready; real R1/R2/R3 extraction and comparison remain pending on server/cache artifacts. |
| 2026-07-04 18:45 Asia/Shanghai | Continue executing the video roadmap with V4b temporal model scaffolding | Added `src/daily_multimodal/embeddings/video_temporal.py`, `scripts/31_train_video_temporal_encoder.py`, and `tests/test_video_temporal_embeddings.py`; implemented deterministic V4b-TCN and V4b-TemporalTransformer builders over DINOv2 frame sequences while preserving the `face_emb (N,256)` compatibility contract, mask semantics, `quality_flags`, and encoder-version names. Synced through 2b95141 plus current working tree. | Watched `python -m pytest tests/test_video_temporal_embeddings.py -q` fail first with missing `daily_multimodal.embeddings.video_temporal`, then pass after implementation; `python -m pytest tests/test_video_temporal_embeddings.py tests/test_dinov2_roi_embeddings.py tests/test_video_embedding_probes.py tests/test_video_variant_ablation.py tests/test_video_variant_embeddings.py -q` passed 36 tests; `python -m compileall -q src scripts tests` passed; `python scripts\31_train_video_temporal_encoder.py --help` passed; repo-docs validator returned `OK: 0 errors, 15 warning(s)`. | In progress for the full roadmap: local V4b temporal interfaces are ready; real V4b evaluation remains pending on real DINOv2 frame-sequence artifacts. |
| 2026-07-04 18:20 Asia/Shanghai | Execute the video-modality roadmap through the V4a baseline/probe preparation steps | Froze the DINOv2 V4a embedding contract as 16 sampled frames plus `mean_std_max` temporal pooling into a 256D `face_emb` compatibility slot; added explicit `--num-frames` / `--temporal-pooling` CLI flags; updated the video probe API and CLI so `--fold-strategy` covers LOSO, S1, S4, and S2 while preserving the old `--p3-fold-strategy` alias; synced the contract and command references. Synced through 2b95141 plus current working tree. | `python -m pytest tests/test_dinov2_roi_embeddings.py -q`; `python -m pytest tests/test_video_embedding_probes.py tests/test_video_variant_ablation.py -q`; `python scripts\27_extract_dinov2_roi_embeddings.py --help`; `python scripts\28_run_video_embedding_probes.py --help`; local full-artifact run was not attempted because `outputs/window_index/real_cache_face_detected_full_v2_mainface.jsonl`, `outputs/cache/real_stage12_face_filter_full_v2_mainface`, and local `torch`/`transformers`/`cv2` runtime dependencies are absent. | In progress for the full roadmap: V4a code contract and probe interfaces are ready; real V4a artifact generation still requires the server/cache environment. |
| 2026-07-04 17:31 Asia/Shanghai | 梳理项目架构并复查历史对话中的效率问题 | 读取当前 `repo-docs/`、源码入口、脚本清单和 12 个相关历史 Codex 线程摘要；确认根 README 已落后于当前阶段 11-28 代码和视频分支实验；更新根 `README.md` 为当前架构入口，并在 `AGENTS.md` 固化 PowerShell 调远程 SSH 时的 here-string/单引号规则，避免 `$`、heredoc、重定向和多行 Python 被本地 PowerShell 预解析。Synced through 2b95141 plus current working tree。 | 读取 `pyproject.toml`、`README.md`、`repo-docs/README.md`、`repo-docs/references/commands-and-artifacts.md`、源码/脚本入口清单和相关线程摘要；随后运行 `python -m compileall -q src scripts tests` 与 repo-docs validator。 | 完成。根 README 不再误写全量入口为占位；服务器命令书写规范进入项目 agent 规则。 |
| 2026-07-03 Asia/Shanghai | 按新版 2 倍主脸 ROI 预处理结果重跑 Face embedding | 在服务器 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding` 用 `outputs/window_index/real_cache_face_detected_full_v2_mainface.jsonl` 和 `outputs/cache/real_stage12_face_filter_full_v2_mainface` 中已生成的 ROI `window.mp4` 重跑 `scripts/13_extract_face_embeddings.py`；cache profile 使用 `openface_temporal_v1`，OpenFace wrapper 为 `/mnt/dataset4/sitian/wzw/tools/openface/feature_extraction.sh`。为避免单进程过慢，将 8328 行窗口索引拆成 8 个 chunk 并行写 CSV/单块 `.npz`，再合并成 full mainface ROI face `.npz`、失败清单和质量 summary。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 服务器 `PYTHONPATH=src python -m pytest tests/test_face_real_embedding.py -q` 通过 14 tests；20 窗口 smoke 成功产出 `embedded_count=20`、`success_count=1`、`nan_count=0`；8 chunk 合并后用 conda `lzs` 环境读取 `.npz` 确认 `face_emb=(7603,256)`、`modality_mask=(7603,4)`、face mask sum `1554`、NaN `0`。 | 完成。新产物为 `outputs/embeddings/face_openface_real_full_mainface_roi_embeddings.npz`、`outputs/reports/face_openface_real_full_mainface_roi_quality_summary.json` 和 `outputs/reports/face_openface_real_full_mainface_roi_failures.json`；summary 为 `embedded_count=7603`、`success_count=1554`、`masked_count=6049`、`extraction_failed=725`、`quality_threshold_failed=6049`，相对旧 full OpenFace face mask `207` 明显提升。 |
| 2026-07-03 Asia/Shanghai | 将 Wear-only 消融扩展为 W1-W7，并拆分 W5a-W5d 比较 quality flags 与 sample weight | `wear_quality_ablation.py` 新增 A+B 子集实验 W6 physio / W7 deep；将原 W5 拆为 W5a deep full、W5b deep+quality flags、W5c deep+sample weights、W5d deep+flags+weights；W5a-W5d 使用同一 seed offset 做更公平对照；CLI 描述和测试同步更新。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding tests.test_wear_quality_ablation` 通过 15 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_wear_quality_ablation.py -q` 通过 2 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器全量 `scripts/22_run_wear_quality_ablation.py` 完成 `experiment_count=10`。 | 完成。Wear-only fatigue 输出在 `outputs/reports/wear_quality_ablation_w1_w7_w5abcd_fatigue_metrics.json` 和 `.md`；W6/W7 A+B 子集为 `6208` 窗口。关键结果：W1 RMSE `0.8855±0.1906`、r `-0.0387±0.1446`；W5a `0.9231±0.1780`、r `-0.0394±0.1200`；W5b `0.9190±0.1754`、r `0.0026±0.0908`；W5c `0.9271±0.1783`、r `-0.0311±0.1173`；W5d `0.9221±0.1769`、r `0.0093±0.0832`；W6 `0.8806±0.1990`、r `-0.0307±0.1183`；W7 `0.9242±0.1846`、r `-0.0160±0.1013`。 |
| 2026-07-02 Asia/Shanghai | 将 wear 消融改为 Wear-only W1-W5，并输出预测分布统计 | `wear_real.py` 新增 A/B/C wear 质量分级、`wear_deep_sequence_v1` 固定随机 1D conv/TCN-like sequence baseline、`--mask-low-quality-wear` 可选低质量 mask；新增 `training/wear_quality_ablation.py` 和 `scripts/22_run_wear_quality_ablation.py`，直接从 wear 单模态 `.npz` 与 window index 标签运行 W1 physio full、W2 deep full、W3 physio high-quality、W4 deep high-quality、W5 deep+quality flags full。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding tests.test_wear_quality_ablation` 通过；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_wear_real_embedding.py tests/test_wear_quality_ablation.py -q` 通过 15 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器重跑 physio/deep full 均 `success_count=7584`、`source_missing=744`、NaN `0`，质量分布 A/B/C=`3307/2901/1376`。 | 完成。Wear-only fatigue W1-W5 输出在 `outputs/reports/wear_quality_ablation_w1_w5_fatigue_metrics.json` 和 `.md`；结果：W1 RMSE `0.8855±0.1906`、r `-0.0387±0.1446`；W2 RMSE `0.9320±0.1800`、r `-0.0492±0.1094`；W3 RMSE `0.9102±0.2194`、r `-0.0524±0.1315`; W4 RMSE `0.9475±0.1883`、r `0.0256±0.1393`; W5 RMSE `0.9212±0.1738`、r `-0.0046±0.1039`。 |
| 2026-07-02 Asia/Shanghai | 升级 8328 个 face-filtered 窗口的 wear 信号质量检查 | `wear_real.py` 新增 `quality_audit` 汇总，覆盖 rows/rate、invalid/source、duplicate/nonmonotonic timestamps、flatline、PPG peak/heart-rate plausibility、GSR slope/SCR IQR 异常、ACC motion/stationary；`wear_physio_features_v2` 在缺 profile metadata 时可复用 `wear_sequence_v1` metadata 或直接读取 window index 的 wear 源路径；重复秒级时间戳按行顺序摊开用于插值，不再丢弃原始样本。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding` 通过 11 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_wear_real_embedding.py -q` 通过 11 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 full mainface quality audit 输出 `quality_audit.window_count=8328`、`success_count=7584`、`source_missing=744`、`wear_emb=(7584,256)`、NaN `0`。 | 完成。最终 summary 为 `outputs/reports/wear_physio_features_v2_full_mainface_quality_audit_summary.json`；PPG/ACC 原始有效采样率为 `20 Hz`，GSR 为 `40 Hz`，三路 invalid/source ratio 均为 `0`，所有成功窗口有秒级重复时间戳但非单调数为 `0`，flatline 窗口为 `0`。 |
| 2026-07-02 Asia/Shanghai | 对所有 face-detected 窗口执行 2 倍主脸 ROI 预处理并统计 | 在服务器 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding` 对 `outputs/window_index/real_cache_face_detected_full_v2_mainface.jsonl` 的 `8328` 个窗口批量生成 ROI 版 `window.mp4` 与 `window_face_roi.json`，复用 `outputs/cache/real_stage12_face_filter_full_v2_mainface/openface/*/openface_temporal_v1/` cache 结构；补写汇总 `outputs/reports/face_roi_preprocess_full_v2_summary.json`。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 服务器文件计数确认 `openface_target.json=8328`、`window.mp4=8328`、`window_face_roi.json=8328`；汇总脚本确认 `clip_missing_count=0`、总帧数 `41640`、直接 ROI 帧 `19663`、补 ROI 帧 `21977`、至少一帧可靠 ROI 的窗口 `5602`、整窗全画面兜底 `2726`。 | 完成。全量 ROI 预处理已落盘，后续 OpenFace 可直接读取 ROI 版 `window.mp4`。 |
| 2026-07-02 Asia/Shanghai | Face 视频预处理改为 2 倍主脸 ROI，并在无脸帧沿用上一 ROI | `scripts/13_extract_face_embeddings.py` 新增默认 `--face-roi-crop-scale 2.0` 和 `--skip-face-roi-crop`；`face_real.py` 在 OpenFace 前生成 2 倍主脸 ROI 的 `640x640` `window.mp4`，逐帧检测最大主脸，个别无脸帧沿用上一有效 ROI，开头无脸使用后续第一个 ROI，整窗没有可靠 ROI 时退回全画面；新增 ROI sidecar 并把 `face_roi_*` 字段写入 `quality_flags`。为避免 Haar 小框假阳性裁坏画面，加入最小面积比例门槛。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地新增 ROI TDD 测试先因缺 `_expanded_face_roi` / `_face_roi_sequence` / `face_roi_crop_scale` 失败，再实现并通过；本地 `PYTHONPATH=src python -m unittest tests.test_face_real_embedding -v` 通过 14 tests，`python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_face_real_embedding.py -q` 通过 14 tests，`python -m compileall -q src scripts tests` 通过；服务器真实窗口 ROI smoke 生成 `640x640`、`0.5 fps`、5 帧 clip，大脸样例 `face_roi_detected_frame_count=4`、`face_roi_filled_missing_frame_count=1`。 | 完成。ROI 裁剪默认 2 倍；小脸/低置信窗口退回全画面，避免不现实的假阳性裁剪。 |
| 2026-07-02 Asia/Shanghai | 确认 no-face 是否误删手部遮挡/倒转画面，并统计多脸后仅保留主脸 | 对 240 个 no-face 和 240 个 kept 窗口做服务器抽样审计并生成 contact sheet；发现 no-face 抽样中 `131/240` 可被 alt/180 度/侧脸检测重新命中，且肉眼包含倒转、手/手机/电脑遮挡和侧脸。`cache.py` 的 face-presence detector 扩展为正向正脸、alt 正脸、侧脸、180 度倒转正脸；`face_presence` 新增 `max_face_count`、`main_face_bbox`、`main_face_area_ratio`、`detected_orientations`、`retained_without_detected_face` 和 `retention_reason`；同一评分事件其它窗口有脸时，无脸中点窗口保留并标记上下文保留。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 新增 TDD 测试先因缺 `face_presence_summary_from_detections` 失败，再实现并通过；本地 `PYTHONPATH=src python -m unittest tests.test_real_pipeline tests.test_face_real_embedding tests.test_audio_real_embedding tests.test_event_window tests.test_video_audio_alignment` 通过 39 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_real_pipeline.py -q` 通过 11 tests，`python -m compileall -q src scripts tests` 通过；服务器全量 `scripts/11_prepare_real_embedding_cache.py` v2 输出 `selected_window_count=8328`、`face_filter_dropped_count=312`。 | 完成。过滤后索引 `outputs/window_index/real_cache_face_detected_full_v2_mainface.jsonl` 为 `8328` 行；`retained_without_detected_face=1939`，直接检到脸 `6389`，`max_face_count>=2` 为 `2308` 个窗口；最终 no-face 剔除样例主要为空椅、桌面或无主脸画面。 |
| 2026-07-01 Asia/Shanghai | 在服务器上全量验证两分钟前向切窗和人脸检测剔除窗口 | 将当前工作区代码同步到服务器 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding`；重建 `outputs/window_index/window_index.jsonl` 和 `outputs/reports/window_index_summary.json`；用 OpenCV Haar + ffmpeg midpoint 并行 face-presence detector 运行全量 `scripts/11_prepare_real_embedding_cache.py`，输出 `outputs/window_index/real_cache_face_detected_full_midpoint.jsonl`、`outputs/reports/real_embedding_readiness_face_filter_full_midpoint.md` 和 `outputs/reports/real_embedding_failures_face_filter_full_midpoint.json`。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 服务器 `PYTHONPATH=src python -m pytest tests/test_event_window.py tests/test_video_audio_alignment.py tests/test_real_pipeline.py tests/test_face_real_embedding.py tests/test_audio_real_embedding.py -q` 通过 33 tests，后续 face filter 性能改动后 `PYTHONPATH=src python -m pytest tests/test_real_pipeline.py -q` 通过 9 tests；服务器 `python -m compileall -q src scripts tests` 通过；窗口索引 summary 为 `events_total=1272`、`events_selected=720`、`events_skipped=552`、`windows_total=8640`、`skip_reasons={insufficient_video_coverage:502, insufficient_pre_event_history:50}`；全量 face filter readiness 为 `selected_window_count=4097`、`face_filter_dropped_count=4543`、`face_filter_dropped_no_face_count=4543`。 | 完成。过滤后索引行数为 `4097`；失败清单共 `4907` 条，其中 `no_face_detected=4543`、`source_missing=324`、`extraction_failed=40`。 |
| 2026-07-01 Asia/Shanghai | 两分钟前向切窗并在人脸预检后重建真实缓存入口 | `event_windows.py` 默认改为情绪评分前 `-120` 到 `0` 秒、10 秒窗口、10 秒步长；新增窗口 summary，统计不足两分钟历史和精确视频覆盖不足的事件；每个 10 秒样本重算 `video_candidates` clip 秒数。`scripts/11_prepare_real_embedding_cache.py` 默认先用 OpenCV Haar face-presence detector 过滤无脸窗口，写出 face-detected window index，并把 `no_face_detected` / `face_detection_failed` 纳入失败清单契约。同步更新事件窗口、walkthrough、字段契约、embedding 契约和命令参考。Synced through f51af7381779166c4d892da6c3a9657081a06970 plus current working tree。 | 先用 `PYTHONPATH=src python -m unittest tests.test_event_window tests.test_real_pipeline.RealPipelineCacheTests` 观察到缺新 API，再实现并通过；相关回归 `PYTHONPATH=src python -m unittest tests.test_event_window tests.test_video_audio_alignment tests.test_real_pipeline tests.test_face_real_embedding tests.test_audio_real_embedding` 通过 34 tests；`python -m compileall -q src scripts tests` 通过；重建 `outputs/window_index/window_index.jsonl` 和 `outputs/reports/window_index_summary.json` 得到 `events_selected=720`、`events_skipped=552`、`windows_total=8640`。 | 代码和窗口索引完成；本地 Windows 副本不能访问 `/mnt/dataset...` 真视频路径，face-detected cache 需在服务器/挂载可用环境重跑。 |
| 2026-07-01 Asia/Shanghai | 抽查 OpenFace `Starting tracking` 后无 CSV 并增加显式 fallback | 从服务器 `face_openface_real_full_failures.json` 中确认 `extraction_failed=73`，其中 `72` 条日志停在 `Starting tracking`；按 subject 抽样 8 个窗口生成 contact sheet 到 `outputs/reports/face_abort_audit/`，画面主要是边缘脸、远景、低头、手机/桌面遮挡。`face_real.py` 在 OpenFace 对窗口 clip 抛错且显式 fallback 可用时，对同一个 `window.mp4` 生成 OpenCV-compatible CSV，并在 `quality_flags` 写 `openface_fallback_used`、`openface_fallback_stage`、`openface_fallback_reason`。Synced through 1a02a84 plus current working tree。 | 先用 `PYTHONPATH=src python -m unittest tests.test_face_real_embedding.FaceRealEmbeddingTests.test_openface_starting_tracking_no_csv_can_fallback_to_generator` 观察到失败，再实现并通过；`PYTHONPATH=src python -m unittest tests.test_face_real_embedding -v` 通过 10 tests。 | 完成 fallback 接入；它只减少无 CSV 失败，窗口是否可用仍由 `face_detection_success_rate` mask 决定。 |
| 2026-06-29 00:00 Asia/Shanghai | 用 `repo-docs-zh` 为仓库生成中文 repo-docs | 新增中文 `repo-docs/` 包，覆盖 README、主 walkthrough、事件窗口模块、embedding 契约模块、命令产物参考、字段契约、术语表；新增根目录 `AGENTS.md` 维护规则。Synced through 7c75bd3；同时检查了当前未提交工作区状态。 | `python -m pytest tests -q` 通过；repo-docs validator 为 `OK: 0 errors, 6 warning(s)`，剩余 warning 是开头代码名密度和示例数字、示例 `sample_id` 是否要进术语表的阅读提示。 | 完成。 |
| 2026-06-29 Asia/Shanghai | 完成多模态嵌入计划阶段 8 到阶段 9 | 新增完整候选集 basic embedding 提取入口和 numpy baseline MLP 训练入口；更新 README、主 walkthrough、命令产物参考，说明 `scripts/07_extract_all_embeddings.py` 已不再是占位入口，并记录阶段 9 subject split baseline。Synced through 4ecb350。 | `python -m pytest tests -q` 通过；`python -m compileall -q src scripts tests` 通过；临时合成数据 CLI smoke 跑通阶段 8 和阶段 9，阶段 8 `selected_windows=12`、`failure_count=0`，阶段 9 `overfit_passed=True`。 | 完成。 |
| 2026-06-29 Asia/Shanghai | 上服务器验证阶段 8 到阶段 9 | 将本地 `configs/`、`src/`、`scripts/`、`tests/`、`repo-docs/` 和 `pyproject.toml` 同步到 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding`；先跑小批量 10 窗口 smoke，再跑完整候选集和 full baseline。Synced through 4ecb350。 | 服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 19 tests；`python -m compileall -q src scripts tests` 通过；阶段 8 小批量 `selected_windows=995`、`success_count=10`、`failure_count=0`；阶段 8 全量 `success_count=995`、`failure_count=0`；阶段 9 全量 `overfit_passed=True`，subject split 为 train `sub-02` 到 `sub-10`、val `sub-11` 到 `sub-12`、test `sub-13` 到 `sub-15`。 | 完成。 |
| 2026-06-29 Asia/Shanghai | 按阶段完成阶段 10，并维护阶段 10 执行报告 | 新增 `scripts/10_run_upgrade_ablation.py`、`daily_multimodal.training.upgrade_ablation`、`tests/test_upgrade_ablation.py` 和 `阶段10执行报告.md`；实现 baseline reference 快照、registry smoke 框架、subject split 失败清单、`modality_token_attention` 融合升级和 accepted/rollback 对照表。Synced through b4fcc32。 | 本地 `python -m pytest tests -q` 通过 24 tests；`python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 24 tests；阶段 10 小批量 registry smoke 正确记录 `subject_split_incomplete`；阶段 10 全量 `modality_token_attention` 对照 `baseline_metric=0.8756`、`upgrade_metric=0.6968`、`delta=-0.1788`、`decision=accepted`，失败清单为 `[]`。 | 完成。 |
| 2026-06-29 14:10 Asia/Shanghai | 完成真实多模态完整 embedding 计划阶段 11 到阶段 12 | 新增真实 embedding 契约、失败清单 schema、四模态缓存准备模块和 `scripts/11_prepare_real_embedding_cache.py`；让窗口索引读取兼容 UTF-8 BOM；更新执行报告、实施计划勾选、README、主 walkthrough、embedding 契约模块和命令产物参考。Synced through b4fcc32。 | `python -m pytest tests/test_real_embedding_contracts.py tests/test_embedding_failures.py tests/test_real_pipeline.py -q` 通过 9 tests；本地 `python -m pytest tests -q` 通过 34 tests；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 34 tests；本地和服务器 `python -m compileall -q src scripts tests` 通过；repo-docs validator 为 `OK: 0 errors, 6 warning(s)`；服务器阶段 12 用 10 条完整且带 `video_candidates` 的窗口验证，EEG、Wear、Face、Audio 均为 `ready=10, missing=0, failures=0`，失败清单为 `[]`。 | 完成。 |
| 2026-06-29 19:47 Asia/Shanghai | 开始阶段 13 Audio 真实 embedding | 新增 `daily_multimodal.embeddings.audio_real`、`scripts/12_extract_audio_embeddings.py`、`tests/test_audio_real_embedding.py`，并更新 audio real profile、执行报告、主 walkthrough、embedding 契约和命令参考。Synced through pending phase-13 commit。 | `python -m pytest tests -q` 通过 38 tests；`python -m compileall -q src scripts tests` 通过；服务器 `lzs` 环境具备 `torch`、`torchaudio`、`transformers`，`facebook/wav2vec2-base-960h` safetensors fallback 已跑通 10 窗口 `(10, 256)` 和 `sub-12` 单被试 `(25, 256)`，失败清单均为 `[]`、NaN 为 0。 | 单被试完成；全量和 ablation 后续统一执行。 |
| 2026-06-30 Asia/Shanghai | 完成阶段 17 all-real 单被试打包入口 | 新增 `daily_multimodal.embeddings.real_pipeline` 和 `scripts/16_extract_all_real_embeddings.py`，以 window index 为主表合并 EEG/Wear/Face/Audio 单模态真实 `.npz`；更新真实 embedding 契约、字段契约、命令产物参考、实施计划和执行报告。Synced through 20c7c54。 | 本地 `python -m pytest tests/test_real_pipeline.py -q` 通过 5 tests；本地 `python -m pytest tests -q` 通过 60 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 60 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 `sub-12` all-real 打包输出四个 `(25, 256)` embedding、mask sum `[25, 25, 25, 25]`、失败清单 `[]`。 | 单被试完成；全量和阶段 18 ablation 后续统一执行。 |
| 2026-06-30 Asia/Shanghai | 完成阶段 18 real ablation 入口和单被试 smoke | 新增 `daily_multimodal.training.real_embedding_ablation`、`scripts/17_run_real_embedding_ablation.py` 和 `tests/test_real_embedding_ablation.py`；更新实施计划、执行报告和命令产物参考。Synced through 6fcb1a5。 | 本地 `python -m pytest tests/test_real_embedding_ablation.py -q` 通过 3 tests；本地 `python -m pytest tests -q` 通过 63 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 63 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 `sub-12` smoke ablation 产出 `subject_split_incomplete` failures，说明单被试不能训练但入口和失败语义可用。 | 入口完成；全量 all-real ablation 和最终结论后续执行。 |
| 2026-06-30 20:23 Asia/Shanghai | 用 `repo-docs-zh` 更新项目 repo-docs | 同步 repo-docs 到当前 `e64b29f` 和当前工作区 EEG coverage 审计改动：README 改为 basic 到 real 的完整工程线；主 walkthrough 补阶段 14-18、fair leakage controls 和 EEG coverage audit；命令参考补 `scripts/18_run_fair_embedding_ablation.py`、`scripts/19_audit_eeg_coverage.py`；字段契约和术语表补 fair/all-real/real ablation/EEG coverage。Synced through e64b29f plus working tree EEG coverage files。 | `python -m pytest tests -q` 通过 76 tests；`python -m compileall -q src scripts tests` 通过；repo-docs validator 为 `OK: 0 errors, 8 warning(s)`，剩余 warning 为阅读提示、外部计划文档缺 agent 规则提示和工作区文件晚于同步锚点提示。 | 完成。 |
| 2026-06-30 Asia/Shanghai | 同步 real embedding v2 代码、服务器 smoke 和阻断状态 | 新增 fair leakage audit、true OpenFace window clip、EEG coverage audit、audio emotion profiles、wear physio v2、subject-level CV；repo-docs 补 v2 profile、Subject CV 命令和 Task 6 阻断说明。Synced through 32259c1。 | 本地 `python -m pytest tests -q` 通过；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 86 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 fair audit `row_count=781`、`sample_id_aligned=True`、`failure_count=0`；EEG coverage audit `in_range=738`、`negative_offset=29`、`after_recording_end=4`、`whole_day_shift_candidate=10`；Wear v2 10-window `success_count=10`、`nan_count=0`；Subject CV on current all-real `fold_count=14`、`subject_leakage=False`。 | 代码与 smoke 完成；完整 all-real v2 未验收，因 OpenFace Apptainer pull 访问 Docker Hub 超时，服务器缺 `opensmile`、`modelscope` 和默认 Python 下的 `torch`。 |
| 2026-07-01 Asia/Shanghai | 完成 real embedding v2 full、fatigue 下游验证和 repo-docs 同步 | OpenFace Apptainer 通过 Huawei mirror wrapper 接入；EEG full v2、Wear physio v2 full、openSMILE full、true OpenFace full 和 all-real v2 打包完成；fair ablation 与 subject CV 支持 `--modalities` 并输出 Pearson r；下游目标从 `alert` 切为 `fatigue`。Synced through 4b53a36 plus current working tree。 | 本地 `python -m pytest tests -q` 通过 87 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests -q` 通过 87 tests；服务器 `python -m compileall -q src scripts tests` 通过；repo-docs validator 为 `OK: 0 errors, 7 warning(s)`；服务器 v2 pack `selected_windows=781`、shape 均 `(781, 256)`、mask sum `[738, 781, 69, 781]`、NaN 为 0；fatigue EEG/Wear/Audio fair real RMSE `1.0160`、r `0.1205`；fatigue EEG/Wear/Audio subject-CV `fold_count=14`、`subject_leakage=False`、RMSE mean `0.9697`、r mean `0.0636`。 | 完成；四模态 subject-CV 因 Face true OpenFace 覆盖稀疏出现空 fold，稳健验证使用 EEG/Wear/Audio 子集。 |
| 2026-07-01 Asia/Shanghai | 生成本地数据概况图 | 新增 `scripts/21_draw_data_overview.py`，从本地 manifest、window index 和 real embedding 质量摘要生成被试级模态完整性 SVG、完整被试时间轴 SVG、完整率 CSV 和摘要 JSON；命令产物参考补该入口。Synced through 1a02a84 plus current working tree。 | `python scripts/21_draw_data_overview.py` 生成 `outputs/figures/subject_modality_completeness.svg` 和 `outputs/figures/sub-10_modality_timeline.svg`；`python -m py_compile scripts/21_draw_data_overview.py` 通过；SVG 可被 XML parser 解析；repo-docs validator 复查。 | 完成；应用内浏览器安全策略阻止直接打开本地 `file://` SVG，改用结构解析和摘要数字核对。 |
| 2026-07-01 Asia/Shanghai | 生成 `sub-10` 单日精确视频音频时间轴 | `scripts/21_draw_data_overview.py` 新增 `--date` 单日过滤，优先用 `video_candidates` 的 `mp4_start_time`/`mp4_end_time` 画精确 MP4 和音频流横条，并在评分事件标注 fatigue 与对应 MP4；从服务器同步精确 manifest/window index 到 `outputs/server_sync/`；命令产物参考补单日精确用法。Synced through 1a02a84 plus current working tree。 | `python -m unittest tests.test_data_overview_figures -v` 通过；`python scripts/21_draw_data_overview.py --manifest outputs/server_sync/events_manifest_with_video_audio.jsonl --window-index outputs/server_sync/window_index_with_video_audio.jsonl --subject-id sub-10 --date 2025-07-08 --out-dir outputs/figures` 生成 `outputs/figures/sub-10_2025-07-08_precise_timeline.svg`；summary 核对 `events_total=11`、`precise_video_full_coverage=11`、`precise_audio_full_coverage=11`；SVG XML parse 通过；repo-docs validator 复查。 | 完成。 |
| 2026-07-01 Asia/Shanghai | 修复 true OpenFace HAAR/MTCNN 与 codec 失败路径 | `face_real.py` 对 Apptainer wrapper 自动传入容器内 HAAR cascade `-fdloc`，避免 FeatureExtraction 找不到 HAAR 后走 MTCNN/ONet 并 segfault；OpenFace 返回非零但已生成非空 CSV 时继续解析 CSV，避免 `mpeg4` VideoWriter 警告误判为失败。Synced through 1a02a84 plus current working tree。 | 新增 Face 单测覆盖 `-fdloc` 和非零返回码 CSV 复用；服务器 `PYTHONPATH=src python -m pytest tests/test_face_real_embedding.py -q` 通过 9 tests；Face full 重跑后 `success_count=207`、`masked_count=501`、`extraction_failed=73`，all-real v2 mask sum `[738, 781, 207, 781]`；四模态 fatigue fair real RMSE `1.3647`、r `-0.1437`，四模态 LOSO 仍因 `sub-13` 无四模态完整样本空 fold。 | 完成目标两类修复；剩余 Face 失败主要是低检测成功率和少量 `Starting tracking` 后无 CSV。 |
