# Repo-docs 变更记录

| Timestamp | Request | Actions | Verification | Result |
| --- | --- | --- | --- | --- |
| 2026-09-21 16:20:28 +08:00 Asia/Shanghai | 整理根目录 Markdown，并将 0814 window 与 0906 EMA-bag 明确为并列当前路线 | 根目录保留 `README.md` 与 `AGENTS.md`；27份研究文档迁入 `docs/research/current/`、`supporting/` 和 `archive/`；新增研究、Wear、embedding 档案和 superseded 路线索引；根 README 与 repo-docs 阅读路径改为两条并列路线；多情绪矩阵归入 joint evaluation；同步证据生成脚本的默认输出路径并修复迁移及既有报告链接。用户已删除的申请材料保持删除，不纳入新目录。Synced through `387dfd7` plus current working tree. | 全库本地 Markdown 链接检查 `broken_local_links=0`；`py_compile` 通过；repo-docs validator `0 errors / 20 warnings`；`git diff --check` 通过，仅报告 Windows 换行提示。 | 根目录 Markdown 从29份收敛为2份；0814与0906均标记为 Current parallel route，支撑实验和历史工程记录有独立入口，生成脚本不会在根目录重新产生 fusion 证据文档。 |
| 2026-09-20 +08:00 Asia/Shanghai | 将共享多头矩阵改为 11 标签共同监督 EEGPT，并原位覆盖 fatigue-supervised 结果 | 新增 MT11 EEG branch 与监督边界；强化 token profile、11标签顺序、split 和监督来源审计；`94` 生成两协议上游 token；`98` 原位重跑114个结构 run；`99` 生成六张表与 README；`109` 负责通过 preflight 后清除旧 runs/summary 并自动接续。同步双路线计划、脚本索引和命令产物参考。Synced through `a521520` plus current working tree. | 本地与远端4项 branch/结构测试通过；两份 MT11 token 均为 `(28819,256)` 且审计通过；正式矩阵 `114/114`、`missing=0`；远端结果根无旧 fatigue route 字符串，本地 summary 文件集合与远端一致；repo-docs validator 复查。 | 当前共享多头主表固定 `A1_Wphysio_no_audio__eeg_eegpt_partial_ft_multitask_11label_v1`。validation macro sRMSE 在两个协议均选中0814窗口结构；test macro raw r 为 cross-day `0.3733`、within-subject-day `0.2429`。 |
| 2026-09-18 15:05 +08:00 Asia/Shanghai | 生成与共享多头结构总结对应的旧版配置单任务 Markdown 总结 | 从远端同步六张结构 × 11情绪表、逐单元格 CSV、summary 与22份 preflight 到独立 `outputs/server_sync/multiemotion_legacy_replay_20260917/single_task_structure_matrix_summary/`；新增 README，按验证集宏 sRMSE 比较0814窗口路线与最优0906结构，并单独解释 fatigue `0.4240490020` 的精确复现和选型边界。同步项目指南与脚本索引。Synced through `97a87f9` plus current working tree. | 远端 `102` 严格汇总1254/1254、missing=0；本地长表1254行、每格3 seed、无空值，preflight 22份；主表数字逐项对照正式 metrics 与 train-label 标准差，README 中全部相对链接可解析。 | 旧配置结果独立可读，不覆盖20260913共享多头或先前单任务表。 |
| 2026-09-18 12:50 +08:00 Asia/Shanghai | 继续旧版 EEGPT 配置的单任务全表实验 | 核对旧版上游 token 22/22；`104` 完成22/22 bag preflight；新增 `107` 按协议/标签隔离进程的断点续跑队列，启动19结构 × 11标签 × 2协议 × 3下游 seed；`102` 增加下游 seed 与 pipeline 参数；另挂 `108` 在队列成功结束后严格汇总。Synced through `97a87f9` plus current working tree. | 远端 fatigue `last_30s` 三个 raw r 精确保持 `0.4234954566/0.4115336118803172/0.4371179376`，均值 `0.4240490020`；22/22预检通过；本地 Python 编译与 Bash 语法检查通过；远端队列 PID `3613004`，部分新 run 已生成；`102 --allow-partial` 已成功生成预览。 | 全表运行中，结果保存在独立 `multiemotion_legacy_replay_20260917/single_task_matrix_A1`，不覆盖既有结果。 |
| 2026-09-17 19:30 +08:00 Asia/Shanghai | 以旧版配置重做单任务 EEGPT embedding，当前只挂上游任务并估算完成时间 | 新增 `103` 旧窗口级 EEGPT 微调及标签专属 token 写出、`106` 逐标签独立进程的断点续跑队列；`104` 在独立 root 验证旧配置 fatigue 标量门槛。首版长进程在启动 alert 时段错误，已改为每标签独立进程且最多重试3次；仅启动 EEG token 队列，下游全表未启动。Synced through `97a87f9` plus current working tree. | cross-day fatigue 历史 token 与 Phase-1 重生 token 逐元素相同；新旧 bag 数组一致；三个下游 seed 的 raw r 与旧产物逐值相同，均值 `0.4240490020383374`。`cross_day/inspired` 完成，耗时32.35分钟且解冻66/104组；`cross_day/alert` 独立进程在 H20 运行，后台队列等待接续。`103` preflight、Python 编译与 Bash 语法检查通过。 | 新 token bank 单独保存于 `eeg_encoder_256d_tokens_legacy_20260917/`；原 20260913 严格 last-two-block token bank 与全部既有结果不覆盖。 |
| 2026-09-17 +08:00 Asia/Shanghai | 完成两条多情绪路线并解释旧 0.4318/0.4288 | 第一条 `101` 独立单标签标量矩阵完成1254/1254；全量重跑22份 bag preflight，`102` 严格生成六张11标签表与长表，同步 `single_task_structure_matrix_summary/`；把两路线的固定0814结构宏指标、验证集选型和旧划分差异写入独立 README，更新双路线计划、读者指南与命令参考。Synced through `97a87f9` plus current working tree. | `102` 报告 missing=0；两组长表各1254个指标单元格，每格3个 seed；跨路线66组窗口结果的 val/test event 和 targets 匹配，split、Wear/Video token、无音频 mask 精确一致。旧 within-day train-test 有150个重叠 subject-day，修复后为0，两轮只有54个共同 test event；repo-docs validator 0 errors、24个既有 warning，`git diff --check`通过。 | 两组完整结果分开呈现：独立单标签/共享多头在 cross-day 窗口结构的 macro raw r 为0.3688/0.3486，在 repaired within-day 为0.2339/0.2450；三seed差值仅作筛查。 |
| 2026-09-16 +08:00 Asia/Shanghai | 补全用户原定的两条多情绪路线：11套独立 EEGPT partial-FT＋标量回归，以及共享两层 MLP＋11个两层回归头 | 为 `ema_bags.py` 增加11个标签专属 EEG branch；新增 `101` 独立标量结构矩阵与 `102` 独立表格汇总；更新双路线计划、脚本索引及命令参考。第二条 `98`/`99` 的114-run结果保持独立。Synced through `97a87f9` plus current working tree. | 22/22标签×协议 bag preflight 与严格 split/token provenance 审计通过；窗口/动态结构 GPU smoke 各1 run通过；原标量与结构测试5/5及新增 branch 测试1/1通过；正式矩阵已启动，`102 --allow-partial` 成功审计预览；repo-docs validator 为0 errors、24个既有 warning，`git diff --check`通过。 | 第一条1254-run下游路线已接入并运行，结果将写入 `single_task_structure_matrix_A1/`，不会覆盖第二条 H1 表。 |
| 2026-09-16 +08:00 Asia/Shanghai | 将结构 × 11情绪正式输入修正为 `A1_Wphysio_no_audio + eeg_eegpt_partial_ft_v1`，并覆盖既有本地结果入口 | 修改 `structure_emotion.py`、`98`、`99` 的唯一固定 route 与 A1 视频来源；在远端独立 `structure_matrix_A1/` 根重跑19结构×2协议×3下游 seeds；以新汇总覆盖本地 `outputs/server_sync/multiemotion_20260913/structure_matrix_summary/` 的 README、六张表、两份 CSV、summary 与 preflight；同步计划、脚本索引和命令参考。原 B0 运行保留远端追溯。Synced through `97a87f9` plus current working tree. | 两协议各1253-event A1 bag、EEG sample/split provenance preflight 通过；窗口/动态结构 GPU smoke 与本地5项单测通过；正式114/114 runs 完成且无错误；`99` 完整核对 val/test event、targets、标签和 A1 route；六张表1254单元格均为3 seed；本地结果目录无 B0 route 字符串，repo-docs validator 0 errors。 | A1 正式主表已替换旧结果，validation macro sRMSE 在两个协议均选中0814窗口结构；完整表见 `structure_matrix_summary/README.md`。 |
| 2026-09-16 +08:00 Asia/Shanghai | 继续执行固定 B0/Wphysio/EEGPT partial-FT/no-audio 的0814/0906结构 × 11情绪矩阵 | 新增 `src/daily_multimodal/training/structure_emotion.py`、`scripts/multilabel/98_run_structure_emotion_matrix.py`、`99_summarize_structure_emotion_matrix.py` 和 `100_audit_eegpt_single_label_bank.py`；在现有0906 regression body 后统一接两层共享 MLP + 11个两层 head；固定 EEG 上游 seed `240800`、下游 seeds `240800,240801,240802`，完成正式114-run矩阵并同步六张表、长表、宏指标及 bank audit 到 `outputs/server_sync/multiemotion_20260913/`。同步主计划、脚本索引和命令参考。Synced through `97a87f9` plus current working tree. | 22/22 标签专属 EEGPT token 通过 shape/finite/sample/split/supervision 审计；两个协议各1253-event B0包 preflight 通过；19结构 GPU smoke 通过；新增+既有 scalar 单测共5项通过；正式114/114 runs 完成；`99` 完整核对全部 val/test event、targets 与输入来源；六张表共1254个指标单元格全部为3 seed；本地 `py_compile`、repo-docs validator 与 `git diff --check` 通过。 | 两协议的 validation macro sRMSE 均选中0814窗口结构；0906 `within_subject_day` 中等核结构呈现 centered-r 方向的探索性增益，后续按预定配对门槛检验。正式行×列结果见本地 `structure_matrix_summary/README.md`。 |
| 2026-09-14 +08:00 Asia/Shanghai | EEG embedding 只保留一个 seed，下游结构回归使用3 seeds | 停止远端正在执行的66-run上游队列，保留已有完整 token；按 `--seeds 240800` 重启 `11 labels × 2 protocols` 的22-run断点队列。主计划和命令参考拆分 `embedding_seed=240800` 与 downstream seeds `240800,240801,240802`。Synced through `97a87f9` plus current working tree. | 精确终止旧父子进程 `3000080/3000082` 后确认无93残留；新任务父子进程 `3001724/3001726` 正常，命令行仅含 `--seeds 240800`，日志开始缓存 `cross_day` normalized EEG。 | 正式 embedding bank 每标签每协议一份；多 seed 成本集中到下游结构比较。已生成的 `cross_day/inspired/seed_240801` 仅作为额外审计产物保留。 |
| 2026-09-14 +08:00 Asia/Shanghai | 将多情绪结果轴收敛为“固定 embedding 下的0814单结构 + 0906结构变体 × 11情绪”，同时继续生成标签专属 EEGPT embedding | 主计划锁定共同输入 `B0_Wphysio_no_audio__eeg_eegpt_partial_ft_v1`；0814仅保留 `window_attention_regression_full_mean`，0906保留 static/state/prior/global/dynamic kernel 结构；label-specific EEGPT token bank 与结构主表解耦。远端从断点恢复 `scripts/multilabel/93_run_eegpt_single_label.py` 的完整66-run队列。Synced through `97a87f9` plus current working tree. | 启动前确认远端只有 `cross_day/inspired` seeds `240800,240801` 两个完整 metrics/token，且没有旧93进程；恢复进程开始复用 protocol-normalized GPU cache，已完成项由脚本自动跳过。 | 主矩阵以后只改变结构；标签专属 EEGPT embedding 继续生成供后续替换实验复用。 |
| 2026-09-14 +08:00 Asia/Shanghai | 执行多情绪 EEG 与两层共享 MLP + 11 独立回归头计划 | 新增 event-aware H0/H1 与 ST scalar fusion、Phase 0 审计、validation-only route/head 汇总、严格 last-2-block ST/MT EEGPT 入口、监督 EEG 下游 fusion 和断点续跑；完成 Phase 0、fatigue checkpoint 复现、72-run E0 route screen 与 12-run H0/H1 paired screen；同步轻量证据到 `outputs/server_sync/multiemotion_20260913/`。Synced through `97a87f9` plus current working tree. | Phase 0 全 gate 通过；fatigue cross-day 精确复现 RMSE/raw-r/centered-r `0.9272/0.2741/0.1005`；Phase 2 的 `84/84` runs 完成；服务器 6 项结构、梯度、事件权重和严格解冻测试通过；严格选择器只训练 EEGPT blocks `6,7` 与 final norm，共 `26/104` encoder 参数张量。 | Gate 2 为 `promote_multihead`；`cross_day` 锁定 `B0_Wphysio_full`，`within_subject_day` 锁定 `A1_Wphysio_no_audio`。ST-11 在完成 `cross_day/inspired` 两个 seeds 后暂停；用户目标已澄清为“0814 窗口级主线与 0906 路线变体 × 11 情绪”结果矩阵，后续先重构行轴。 |
| 2026-09-13 17:45:19 +08:00 Asia/Shanghai | 删除旧宽松 within-day split，并将修复后协议统一命名为 `within_subject_day` | 将 aligned `outputs/splits/within_subject_day_strict` 移出活动路径并保存到可恢复备份；新增统一 split-path resolver，使仍以 `splits_new` 为默认根的当前训练入口在请求 `within_subject_day` 时读取 aligned canonical split；更新 multi-label、EQL-CAF、daily-affect、Wear、EEG encoder 入口及多情绪计划、handoff、术语表、数据合同和命令参考。Synced through `97a87f9` plus current working tree. | 服务器复核 canonical `split_info.name=within_subject_day`，四个索引 SHA-256 与迁移前一致，strict 活动目录不存在且备份存在；本地 19 tests 通过、1 个依赖 PyTorch 的测试按环境条件跳过；`py_compile`、`git diff --check` 通过；repo-docs validator 为 `0 errors / 24 warnings`，均为既有密度、缺本地重型产物和 freshness 提示。 | 新运行统一使用 repaired `within_subject_day`。root-owned `/vePFS-0x0d/DailyEEG/splits_new/within_subject_day` 因当前账号及父目录不可写、无免密 sudo，尚待管理员物理删除；当前入口已绕开该目录，旧结果仅保留历史 provenance。 |
| 2026-09-13 17:22:14 +08:00 Asia/Shanghai | 澄清 `within_subject_day_strict` 与 `within_subject_day` 的区别 | 在字段契约和术语表中将 split root 纳入协议标识；区分 `splits_new/within_subject_day` 的历史窗口级划分、aligned `outputs/splits/within_subject_day_strict` 的 subject-day 整体划分，以及 aligned `outputs/splits/within_subject_day` 的修复兼容名称；同步多情绪实验计划。Synced through `97a87f9` plus current working tree. | 服务器逐文件 SHA-256 显示 aligned `within_subject_day` 与 strict 的四个索引完全一致；read-only 审计显示 legacy split 三组之间均重叠 `150` 个 subject-day，strict 为零重叠；`git diff --check` 通过，repo-docs validator 为 `0 errors / 24 warnings`，warnings 均为既有密度、缺本地重型产物与 freshness 提示。 | 后续实验必须同时记录 protocol name 和 split root；多情绪主实验使用 strict，legacy 结果保持独立。 |
| 2026-09-11 21:08:37 +08:00 Asia/Shanghai | 将 W3FM 的 PPG、ACC、GSR 三路都替换为 NormWear embedding 进行诊断筛查 | 新增 `scripts/wear_only_fm/90_run_w3fm_normwear_all_screen.py`，在同一 `wear_complete_mask` 上缓存三路 frozen NormWear 768D embedding，其中 ACC 三轴先轴向 mean pooling 为单个 ACC token；完成 `cross_day,within_subject_day` x `240729,240730,240731` 的 6-run `W3FM_normwear_all` 训练，并同步完整远端结果到 `outputs/server_sync/wear_fm_normwear_all_screen_20260911/`。同步 scripts 索引、Wear-only 计划和 repo-docs 命令/产物指南。Synced through `97a87f9` plus current working tree. | 本地与远端 `py_compile` 通过；远端 cache-only smoke 通过；正式 CUDA run 完成 6/6；manifest 显示 PPG/ACC/GSR shape 均为 `(28819,768)`、valid count 均为 `24127`；运行前后空间复查 `/vePFS-0x0d` 从约 `79G` 到 `78G` 可用，根分区维持约 `88M` 可用且临时目录指向项目内 `outputs/tmp`。 | `cross_day` raw r 提到 `0.1515+/-0.0649`，但 RMSE `0.9938+/-0.0198` 和 centered r `0.0353+/-0.0463` 未超过 `W3FM_no_acc` 的主诊断优势；`within_subject_day` raw r `0.1879+/-0.0035` 接近 Wdeep，但 RMSE `1.0363+/-0.0167` 明显高于 Wdeep。该分支保留为 post-Phase3 诊断，不改变 formal stop 判定。 |
| 2026-09-11 15:17:49 +08:00 Asia/Shanghai | 梳理现有研究成果并规划 IEEE Transactions on Affective Computing 投稿框架、图表和对比基线 | 新建 `outputs/researchwrite/taffc_daily_multimodal/` 写作基础文件与中文投稿蓝图；以标量 EMA-event seven-seed 证据为正文主线，将 ordinal、robustness、label permutation、MOMENT、calibration、multilabel、W3FM 与 EQL-CAF 分层；给出章节合同、6 张正文图、5 张正文表、核心 baseline 矩阵、公平性合同和投稿前实验门槛；在读者指南加入投稿蓝图入口。Synced through `97a87f9` plus current working tree. | 对照 `README.md`、`technical_route_20260906.md`、标量回归技术报告、fusion/Wear/置换报告、相关源码和本地同步产物复核数据合同与数值；视觉检查 3 张现有 Daily-affect 图；检索并核验 TAFFC 官方范围、TAFFC 相关论文及 MulT/TFN/LMF/MISA/EMT-DLFR 原始来源；repo-docs validator 通过。 | 形成可执行投稿路线：正文以 event-level scalar regression 与协议依赖的 temporal evidence 为主，外部基线、validation-only 选型、上游 seed、正式 null、最终标量 robustness、伦理与数据发布信息为投稿前必补项。 |
| 2026-09-08 +08:00 Asia/Shanghai | 执行窗口架构与 0906 EMA-bag 架构的统一标量回归比较计划 | 新增 `daily_affect.regression` 与 `regression_training`：所有模型以 train-only standardized scalar MSE 训练、validation RMSE 选 checkpoint；窗口路线按 event-balanced replicated-window MSE 训练并 full-mean 汇总。新增 `90` preflight/smoke/matrix 入口、`91` paired raw-r summary 与 subject-day bootstrap，以及合成回归测试。独立输出根为 `outputs/daily_affect_scalar_regression_20260908/`。Synced through current working tree. | 本地 `compileall` 与 `unittest tests.test_daily_affect_scalar_regression` 3/3 通过；远端同一 3/3 unittest 通过。远端 9 个 protocol×seed bag preflight 通过；cross-day 四条件 GPU smoke 完成，`91` 成功汇总 4 runs、3 条配对 bootstrap。 | 纯回归的闭环与数据契约已通过；三协议×三 seed×19 条件矩阵已启动，完成后才按三 seed paired gate 决定 7-seed 扩展。 |
| 2026-09-08 +08:00 Asia/Shanghai | 扩展 Daily-affect 标签置换 Phase 1 到另外两个协议 | 将 `89` 的 clean-result 解析兼容 phase0/static 与 run/candidate 目录；H20 对 `cross_subject/A2_Wdeep_full/bag_static` 和 `within_subject_day/B0_Wphysio_full/state_uniform` 分别完成 `5×1` global null。Synced through `5995502` plus current working tree. | 两协议均使用 `label_free_or_fixed_embeddings_only` bag，5/5 audit 通过并保留 test 标签/index。cross-subject clean QWK/raw-r `0.2158/0.1146` 高于 `5/5` null，centered-r null 仅 `2/5` 正；within-subject-day clean `0.3115/0.3015` 高于 `5/5` null，centered-r null 为 `4/5` 正。 | cross-subject `bag_static` 通过 Phase 1，待独立正式 30×3 null；within-subject-day 的 `state_uniform` 触发 `stop_and_audit`，不推广为个体内状态跟踪证据。`dynamic_kernel_no_prior` 保持独立、尚未置换检验。 |
| 2026-09-08 +08:00 Asia/Shanghai | 执行 Daily-affect 标签置换验证计划 | 新增 `scripts/daily_affect/89_run_daily_affect_label_permutation.py`，在 EMA event 层对 train/validation 独立 global 或 within-subject 置换，保留输入、split 和真实 test 标签；同步 daily-affect 索引、命令指南和 Phase 1 stop 结论。Synced through `5995502` plus current working tree. | 本地 `py_compile` 与无 Torch 的 Daily-affect tests（7 passed、14 skipped）通过；远端 H20 完成 `5×1` global null。每条 run 的标签变化比例 `0.647--0.694`，split histogram/test index/test labels 均通过；保存的 val QWK 与置换标签重算值一致，test QWK 与真实标签重算值一致。 | clean QWK/raw-r 高于 `5/5` null，但 `4/5` null centered-r 为正，触发 `stop_and_audit`。按门槛未启动 `30×3` within-subject expansion，个体内 event-level 状态跟踪结论保持未支持。轻量产物同步到 `outputs/server_sync/daily_affect_label_permutation_20260907/`。 |
| 2026-09-08 +08:00 Asia/Shanghai | 将另外两种协议的 Daily-affect focused 3-seed 阶梯结果写入当前技术路线 | 更新 `technical_route_20260906.md` 的 completed evidence、三协议当前最佳结果、下一步决策和产物定位；更新读者指南以标注远端未同步的结果根。Synced through `d22978d` plus current working tree. | 对照远端 cross-subject 和 within-subject-day 的 `daily_affect_ordinal_summary.md`、`protocol_route_summary.csv` 与 focused diagnostics，复核 route、normalization、adapter、3-seed mean、paired delta 和胜出数；本地 Markdown 链接/标题搜索与 `git diff --check` 通过。 | cross-subject 的受控阶梯停在 `bag_static`；within-subject-day 由 `state_uniform` 和 `dynamic_kernel_no_prior` 进入 5--7 seed paired gate。跨日 7-seed 候选保持独立，不与其他协议混合。 |
| 2026-09-07 16:00:12 +08:00 Asia/Shanghai | 整理 `scripts/` 中所有非归档脚本，按研究路线分类迁移并修正硬编码路径 | 将当前非归档脚本迁移到 `scripts/embeddings/`、`scripts/window_fatigue/`、`scripts/calibration/`、`scripts/multilabel/`、`scripts/eql_caf/`、`scripts/wear_only_fm/`、`scripts/daily_affect/`；新增总索引与各子目录 README；把当前脚本、根 README、路线文档、repo-docs、测试和当前报告中的硬编码入口路径改为新目录；迁移后脚本内项目根目录统一改为 `Path(__file__).resolve().parents[2]`。Synced through `df95634` plus current working tree. | `python -m compileall -q scripts src tests` 通过；`PYTHONPATH='src;C:\torch'` 下重点入口 `32`、`51`、`73`、`89` 的 `--help` 通过，`42`、`57`、`65` 直接 `--help` 通过；迁移相关单测 `tests.test_phase2_fusion_loss tests.test_eeg_encoder_fusion_tokens tests.test_fusion_variants tests.test_daily_affect_route` 为 42 tests 通过、1 个本机缺 matplotlib 的图像 smoke 跳过；旧当前入口路径搜索只剩已归档/未创建的历史引用；`git diff --check` 通过，仅有 Windows 换行提示。 | 当前脚本入口按路线分组，`scripts/archive_legacy/` 保持归档边界。 |
| 2026-09-07 12:51:00 +08:00 Asia/Shanghai | 尝试用稳定运动统计替代 W3FM 的 HARNet10 ACC embedding | 新增 `scripts/wear_only_fm/89_run_w3fm_acc_handcrafted_screen.py`，从 staged `acc_10s.npz` 生成 88 维 label-free ACC 统计/频域特征，训练 `W3FM_acc_handcrafted`，并读取已有 `W3FM_frozen` 与 `W3FM_no_acc` reference 形成同 seed 对照；同步完整远端结果到 `outputs/server_sync/wear_fm_acc_replacement_screen_20260907/`，并更新 Wear-only FM 计划、脚本索引和命令产物指南。 | 本地与远端 `py_compile` 通过；远端 CPU smoke 完成；正式 `cross_day,within_subject_day` x 3 seeds 的 6 个 CUDA run 完成；空间复查 `/vePFS-0x0d` 可用约 `105G`，新结果目录约 `13M`。 | `cross_day` 中 `W3FM_acc_handcrafted` raw r `-0.0001+/-0.0168`、RMSE `1.0289+/-0.0120`，低于 `W3FM_no_acc` raw r `0.0656+/-0.0610`、RMSE `0.9841+/-0.0080`；`within_subject_day` raw r `0.1580+/-0.0212` 高于 reference，但 centered r `0.0253+/-0.0169` 接近完整 W3FM。结论为 post-Phase3 诊断，不改变 formal stop 判定。 |
| 2026-09-07 12:20:00 +08:00 Asia/Shanghai | 在 matched partial-FT daily `bag_static` 上试验 expected-score 连续辅助损失 | `daily_affect.training` 增加 event-level Huber loss、配置/epoch audit/objective metadata，并新增 `88_run_daily_affect_expected_score_huber.py`。固定 canonical bag、模型、优化器、QWK checkpoint selection 与三 seed，网格仅改变 Huber weight；验证集 raw-r/QWK gate 锁定候选，输出独立 root 并同步零权重与锁定候选的 metrics/predictions。 | 本地语法通过；远端新增 loss unit test 与 CPU training smoke 通过；真实 `(1253,23,4,256)` bag preflight 对三 seed 通过；GPU `5` weights x `3` seeds 的 `15/15` run 成功。 | `lambda=0.2` 由 validation-only gate 锁定，validation raw-r delta `+0.0142`、`3/3` wins。锁定 test raw-r delta `+0.0031+/-0.0058`、`2/3` wins，QWK `-0.0029+/-0.0368`；作为轻量 exploratory candidate，不替换窗口主线或扩展为 dynamic-kernel 结论。 |
| 2026-09-07 10:52:43 +08:00 Asia/Shanghai | 用数据验证 daily-affect 的动态范围扩大及其与 raw-r 的关系 | 新增 `scripts/daily_affect/87_report_window_daily_range_precision.py`，从 matched event bridge 的 `event_predictions.csv` 计算 per-seed prediction range、连续关联、label-mean separation、within-label variation、label-explained variance 与 subject-day bootstrap paired delta；输出 JSON/Markdown/CSV 并同步到 bridge comparison root。同步脚本索引、项目导航、命令表和技术路线。Synced through `d2c1f1a` plus current working tree. | 本地 `py_compile` 通过；远端 2,000 次 subject-day bootstrap 成功写出 `9` per-seed 与 `6` paired rows；本地同步报告核验三系统均为 253 个相同 held-out event。 | 每个 seed 的 daily P10--P90 均大于 window：static mean delta `+0.6960+/-0.4009`、dynamic `+0.9043+/-0.3322`，全部 bootstrap CI 高于零。raw-r delta 分别为 `-0.0640+/-0.0084` 与 `-0.0965+/-0.0367`，均为 0/3 positive。static 的 label-mean separation 增大而 within-label variation 增长更多；dynamic 的 label-explained variance 最低。三 seed bridge 为诊断性跨线证据，未改变主线或 frozen-token candidate gate。 |
| 2026-09-07 10:40:12 +08:00 Asia/Shanghai | 在 matched event 图的窗口主线标题中明确显示连续回归指标 | `86_plot_window_daily_event_bridge_series.py` 将每个模型面板标题拆为模型名和指标行；窗口主线指标行显式展示 QWK、raw r、centered r 与 RMSE。Synced through `d2c1f1a` plus current working tree. | 本地 `py_compile` 通过；远端重画成功并同步本地，视觉检查确认窗口主线标题完整可读。 | 图仍读取同一 `event_predictions.csv` 和 `seed_metrics.csv`，数据、聚合与统计数值未变化。 |
| 2026-09-06 23:42:56 +08:00 Asia/Shanghai | 在 daily-affect event 图中加入窗口级主线 | `84` 新增 primary full-mean 的逐 seed/event `event_predictions.csv`；新增 `86_plot_window_daily_event_bridge_series.py`，从该 CSV 和 native metric 表生成 window regression、`bag_static`、`dynamic_kernel_prior_uniform` 的三面板 matched event 图，并同步到 `outputs/server_sync/daily_affect_window_event_bridge_20260906/comparison/`。同步脚本索引、项目导航、命令表和技术路线。Synced through `d2c1f1a` plus current working tree. | 本地 `py_compile` 通过；远端重算 `84` 成功写出 `42` metric、`60` paired delta 和 `2,277` event prediction rows；`86` 成功读取该表，确认 `253` event 与 `30` subject-day group 并生成 PNG/JSON；本地同步 PNG 后视觉检查完成。 | 三面板严格使用 `cross_day / A1_Wphysio_full / eeg_eegpt_partial_ft_v1 / per_modality` 与相同三个 seed。窗口主线 event-level mean 为 QWK `0.209`、raw r `0.366`、RMSE `0.909`；daily static 为 `0.235/0.302/1.004`，dynamic 为 `0.176/0.269/1.035`。图作为 matched bridge 的直观审阅，不改变 window 主线或 frozen-token 7-seed daily candidate 的证据地位。 |
| 2026-09-06 23:32:19 +08:00 Asia/Shanghai | 为当前 daily-affect 提供类似窗口主线真值-预测曲线的可视化 | 新增 `scripts/daily_affect/85_plot_daily_affect_event_series.py`，从 current `cross_day / A1_Wphysio_full / per_modality` 7-seed static 和 dynamic candidate 的保存 prediction 绘制 253 个 held-out event 的真值、均值 expected score 与 seed 10--90% 区间；同步 PNG、逐 event CSV、metadata JSON 到 `outputs/server_sync/daily_affect_dynamic_a1_crossday_routefix_20260906/norm_per_modality__adapter_per_modality/figures_7seed/`。更新脚本索引、项目导航、命令表和技术路线。Synced through `d2c1f1a` plus current working tree. | 本地 `py_compile` 通过；远端读取 14 个 prediction 与 metric 文件后完成绘图，`event_count=253`、`subject_day_group_count=30`；本地同步 PNG 后视觉检查完成。 | 图同时展示 `bag_static` 与 `dynamic_kernel_prior_uniform`，保留每模型的 seven-seed mean+/-std QWK/raw-r/centered-r/RMSE。横轴按 subject-day 内自然 event 序排列，边界线阻止跨 subject-day 的连续时间误读。 |
| 2026-09-06 23:25:38 +08:00 Asia/Shanghai | 在同一 event 单位上计算并比较窗口级主线与 daily-affect | 新增 `scripts/daily_affect/84_compare_window_daily_event_level.py`，以 `cross_day / A1_Wphysio_full / eeg_eegpt_partial_ft_v1 / per_modality` 建立独立 bridge root；构建三个 matched EMA bags，运行三个窗口回归控制和三个 `bag_static` 加三个 `dynamic_kernel_prior_uniform`，并将 event-level report/CSV/JSON 同步到 `outputs/server_sync/daily_affect_window_event_bridge_20260906/comparison/`。technical route 与脚本导航补入 bridge。Synced through `d2c1f1a` plus current working tree. | 每个 seed 的 `253` 测试 event 与 `269` 验证 event 均逐 event 验证为对应 split 的完整 `23/23` 窗口；远端 `84` 成功写出 `42` metric rows 与 `60` paired rows。`full_mean` 主比较使用每 seed `2,000` 次 subject-day bootstrap；窗口侧和 daily-affect 分别仅从验证 event 拟合 isotonic 校准。 | event 级 native 均值为 window `QWK=0.2093/raw-r=0.3660/RMSE=0.9092`，`bag_static=0.2350/0.3020/1.0038`，dynamic `=0.1759/0.2695/1.0346`。static 对窗口的 QWK 差值为 `+0.0257+/-0.0868`、`1/3` wins，raw-r 为 `-0.0640+/-0.0084`、RMSE 为 `+0.0946+/-0.0254`；各 seed QWK 区间覆盖零。该三 seed partial-FT bridge 是描述性跨线证据，保留原 frozen-token 7-seed daily 内部 gate 的独立地位。 |
| 2026-09-06 13:30:00 +08:00 Asia/Shanghai | 编写当前多模态疲劳预测技术路线文档 | 新增 `technical_route_20260906.md`，以 2026-08-14 历史 window-level matrix、当前 daily-affect routefix 7-seed 证据、EQL-CAF temporal-token 前置条件和可执行脚本/产物路径重建总览；更新根 README 与读者指南入口。Synced through `d2c1f1a` plus current working tree. | 对照 canonical index、daily-affect bag contract、7-seed summary、399 条 robustness 输出、当前训练脚本和已同步路径核验文档中的形状、协议、指标与关键数字。 | 文档明确窗口级 regression 主线、daily-affect ordinal candidate 与 EQL-CAF research 三条线的样本单位、监督边界、promotion gate 和下一步，避免跨线混比。 |
| 2026-09-06 13:00:00 +08:00 Asia/Shanghai | 续跑 routefix focused 7-seed paired gate 与 missing/corruption robustness，并封口无 prior 路由 metadata | 为新增四个训练 seed 生成与 seed_240729 token/mask/label/split 一致的 EMA bags，完成两候选与 bag_static 的 12 个补充训练、7-seed subject-day bootstrap、两份 focused report、3 张聚合图和 399 条 robustness 明细。`global_kernel_no_prior` 的 difficulty 解析固定为 `none`，`79` 显式传零 lambda，并加回归测试；轻量产物同步至 `outputs/server_sync/daily_affect_dynamic_a1_crossday_routefix_20260906/norm_per_modality__adapter_per_modality/`。Synced through `d2c1f1a` plus current working tree. | 四份新增 bag 与参考 bag 的 tokens/mask/labels/event split 均逐项相等；远端训练 `12/12`、robustness `399/399` 完成；7-seed bootstrap 每 seed 30 subject-day block；本地 daily-affect suite 为 7/7 通过、13 项无 Torch 跳过，远端为 20/20 通过（145 s）。 | `dynamic_kernel_prior_uniform` 相对 static 为 QWK `+0.0651+/-0.0656`、MAE `-0.0350+/-0.0650`、QWK `6/7` wins；`global_kernel_no_prior` 为 `+0.0366+/-0.0716`、`-0.0045+/-0.0556`、同为 `6/7`。state-compatible candidate clean QWK `0.2515`，且 video-missing QWK `0.2144`，优先进入后续 video-stability 设计。 |
| 2026-09-06 12:31:24 +08:00 Asia/Shanghai | 修复 daily-affect Probe reliability atlas 缺失行标签 | `77_plot_daily_affect_diagnostics.py` 取消 reliability 子图的共享 y-axis，非首列仅隐藏标签而不改写共享 formatter，首列保留完整 route/model/normalization/adapter 行标签。Synced through `d2c1f1a` plus current working tree. | 本地 `py_compile` 通过；bag/metrics/summary/atlas 单元测试为 7/7 通过、1 项本机无 Torch 跳过；远端同一 8/8 通过，`77 --clean` 读取 60 run、输出 3 张 atlas，并完成本地同步和 reliability atlas 视觉检查。 | reliability atlas 可逐行定位指标来源。 |
| 2026-09-06 00:18:00 +08:00 Asia/Shanghai | 补齐 daily-affect 审计 P2 的 Probe reliability 可视化 | `77_plot_daily_affect_diagnostics.py` 从全 EMA diagnostics 与 `predictions.npz` 的 `test_index` 严格对齐，按有效模态计算每 seed test-event Probe Accuracy、NLL、ECE 和 RPS，再以 seed 等权均值生成每协议一张 reliability atlas；保留既有紧凑 diagnostics/confusion atlas，不恢复逐 run 图。同步计划、脚本索引和命令参考。Synced through `d2c1f1a` plus current working tree. | 本地 `compileall` 通过；`PYTHONPATH=src python -m unittest tests.test_daily_affect_route.DailyAffectBagTests -v` 为 7/7 通过、1 项本机无 Torch 跳过，新增测试验证 test-index 与 valid-modality mask 对齐。 | P2 的可执行 Probe reliability 报告已补齐。300 秒 look-back 与连续 video ROI-quality routing 仍等待上游数据重建，保持为明确的数据前置项。 |
| 2026-09-06 00:06:52 +08:00 Asia/Shanghai | 补齐 daily-affect 审计 P0 中窗口标签复制与 EMA-bag supervision 的公平对照 | `DailyAffectOrdinalModel` 新增 `window_replicated`，并让它与新的 `bag_static` 共享同一逐窗口 modality self-attention、learnable query pooling、adapter 和分类头。`window_replicated` 仅在有效窗口上复制 event 标签计算加权 CE、cumulative ordinal 和 ranking；`bag_static` 对同一窗口表示做时间池化后以单个 event 标签训练，两条线路都以 event-level 概率输出测试指标。`74` 成对执行并将 model/normalization/adapter 写入独立路径；metrics/config 写入 `supervision_unit`。同步计划、scripts README、项目概览和命令参考。Synced through `d2c1f1a` plus current working tree. | 本地 `PYTHONPATH=src python -m unittest tests.test_daily_affect_route -v` 通过 6 项非 Torch 测试、13 项按环境跳过；远端 `huoshan_TriDim` 的 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest tests.test_daily_affect_route -v` 为 19/19 通过；远端一轮 GPU Phase 0 smoke 完成两条分支并写入 `outputs/daily_affect_phase0_attention_smoke_20260906/`。 | P0 的此项已可执行且与现有 routefix 产物隔离。smoke 的 QWK 仅验证训练/评估闭环，正式三协议 multi-seed paired 矩阵完成后才判断 EMA-bag supervision 的效应。 |
| 2026-09-05 23:45:03 +08:00 Asia/Shanghai | 修复 daily-affect 当前实现与计划的序数目标、P0-P5 routing、adapter 分离、报告和鲁棒性缺口 | 新 routefix root 只读取 `daily_affect_ordinal_20260903/bags`；训练采用 class-weighted CE + cumulative ordinal + within-subject ranking、bag-level modality dropout、routing warm-up；Probe 支持 cumulative/categorical、per-modality temperature calibration 与 routing/head-only finetune；`75` 显式 P0-P5 profile，`76` 按 objective/adapter 配对并做 subject-day bootstrap，`77` 以完整因子分组，`80` 加成对 missing 与 routing response。同步 scripts/repo docs 契约。Synced through `d2c1f1a` plus current working tree. | 本地 `compileall`、7 个非 Torch bag/metrics/summary/atlas tests 通过；远端 `huoshan_TriDim` 16/16 daily-affect tests 通过，另有 P1 与 P4 一轮 GPU script smoke、`76` summary 和 `77` atlas smoke 均通过。 | 新实现及结果输出与历史 run 分离；routefix 的正式训练结论待实际矩阵运行完成后产生。 |
| 2026-09-05 17:55:00 +08:00 Asia/Shanghai | 排查部分 daily-affect 路线 Wear routing weight 异常偏高的共同原因 | 扩展 `run_daily_affect_run`/`fit_daily_affect_model`，使 train-only token normalization scope 与 adapter mode 可独立指定，保持旧调用默认二者相同；新增 `scripts/daily_affect/83_run_daily_affect_routing_factorial.py`，在独立 root 执行 `cross_day / A1_Wphysio_full / state_uniform` 的 2x2 normalization x adapter、三 seed 机制诊断，并只在 test event 汇总 routing weights。同步 scripts README、命令产物指南和项目概览。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile` 通过；`$env:PYTHONPATH='src;C:\torch'; python -m unittest tests.test_daily_affect_route -v` 为 13 tests 通过、1 个本机缺 matplotlib 的绘图 smoke 跳过；远端一条件 one-epoch smoke 成功，随后完整矩阵 `12/12` run 完成并写出 JSON/Markdown。 | 高 Wear 的共同点是当前 `shared normalization + shared adapter` 的组合，二者在历史结果中被同名 `shared` 混合。dual-shared 的 test Wear weight 为 `0.7363`；三个其余组合为 `0.2783-0.3218`。保持 shared normalization 仅替换 adapter，QWK delta `+0.0631`、3/3 wins，Wear delta `-0.4580`；保持 shared adapter 仅替换 normalization，QWK delta `+0.1208`、3/3 wins，Wear delta `-0.4259`。结论为共享归一化与共享 adapter 的交互性路由失配；这是诊断结果，不替代现有 mainline/candidate gate。远端完整产物为 `outputs/daily_affect_routing_factorial_20260905/`，本地同步报告为 `outputs/server_sync/daily_affect_routing_factorial_20260905/`。 |
| 2026-09-05 17:09:54 +08:00 Asia/Shanghai | 修复 daily-affect `77` 单 run 图覆盖，并将大量图片整合为可读拼图 | 重写 `scripts/daily_affect/77_plot_daily_affect_diagnostics.py`：按 `protocol/route/model/normalization` 聚合，每个 seed 先在 test event 内汇总再做 seed 等权平均；每协议输出 routing/difficulty/temporal heatmap atlas，另输出一张按 protocol/route 选 mean-QWK 最高组的混淆矩阵 atlas。manifest 记录 aggregation contract 和每组源 `metrics.json`；`--clean` 只删除旧版 77 的已知单 run 文件名。新增合成诊断 artifact smoke test，并同步脚本索引与产物说明。修复 confusion atlas colorbar 布局后，在 `huoshan_TriDim` 以完整产物重画并同步。Synced through d2c1f1a plus current working tree。 | `python -m py_compile scripts/daily_affect/77_plot_daily_affect_diagnostics.py tests/test_daily_affect_route.py` 通过；本地 daily-affect unittest 13 tests 通过（`matplotlib` smoke 因本机缺包跳过）；远端 `77 --clean` 成功读取 270 runs、聚合为 90 组、输出 4 张 atlas，且本地同步后逐图视觉检查通过。 | 新版将完整正式矩阵收敛为三张 protocol diagnostics atlas 加一张 confusion atlas，避免 seed/normalization 被同名图覆盖；远端 `figures/` 由 199 张旧逐 run 图片替换为 4 张 atlas 和 manifest，本地当前 `figures/` 同步相同 4 张图，旧 199 张保留在可恢复的 `figures_legacy_per_run_20260903/`。 |
| 2026-09-05 00:30:00 +08:00 Asia/Shanghai | 查验事件级标签可辨识性、跨日表征和 Video 稳定性 | 扩展 daily-affect static aggregation，支持统一、固定 look-back 和 short/medium/long EMA kernel，且把 temporal policy 记录进 config/checkpoint；新增 `scripts/daily_affect/82_run_daily_affect_bottleneck_audit.py`，在独立 root 上以同 bag/split/seed 训练 8 个时间策略与 video-only/non-video 对照，并输出 per-day modality drift、video coverage 和 `quality_flags` 审计；同步计划、脚本索引和 repo-docs。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile` 通过，本地和远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest tests.test_daily_affect_route -v` 通过 11 tests；远端审计完成 30 个 temporal screen、360 条 drift rows、882 条 video-quality rows，本地同步 reports/CSV/checkpoints。 | 标签主要对应近期加权历史：`kernel_short` 和 `kernel_medium` 相对 full-2min 的 mean delta QWK 为 `+0.0677` 和 `+0.0585`，medium 为 3/3 wins；last-10s 无稳定增益。Video-only 有 QWK `+0.0310` 的单模态信号，但 video test event availability 为 `0.6319`、centroid shift RMS 为 `0.8074`，缺失/日期漂移构成主要稳定性问题。有效 video 质量字段全为 4/4 usable frames，当前无法检验连续 ROI-quality weighting。下一步是 fixed-kernel 的 5-7 seed paired gate，再补质量特征后试 video dropout/quality-aware routing。 |
| 2026-09-05 00:00:00 +08:00 Asia/Shanghai | 修复计划所述 Prior-guided modality weighting 强于当前实现的问题，并重新判断 prior 的作用 | 重构 `AffectStateFilter` 以暴露逐步 state update；扩展 `DailyAffectOrdinalModel`，由上一 GRU state 形成 state prior 并以 compatibility 进入当前模态 routing score；把 state prior、ordinal difficulty 和 dynamic kernel 拆成 `prior_uniform`、`prior_ordD_uniform`、`dynamic_kernel_prior_uniform`、`dynamic_kernel`；新增 `81_report_daily_affect_prior_guidance.py`，并在独立 `daily_affect_prior_guidance_v2_20260905` root 对 7 个模型运行 3-seed paired ablation；同步计划、脚本索引和 repo-docs。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile` 通过；本地和远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest tests.test_daily_affect_route -v` 均通过 10 tests；远端 21 个 v2 run 全部完成，`81` 输出 JSON/Markdown/paired CSV/summary CSV。 | 修复后的真实 state prior 没有改善当前 `cross_day / A1_Wphysio_full / per_modality` dynamic-kernel 候选：`dynamic_kernel_no_prior -> dynamic_kernel` mean delta 为 QWK `-0.0461`、ordinal MAE `+0.0316`、Expected RMSE `+0.0445`、raw r `-0.0470`、centered r `-0.0634`，QWK wins `1/3`。state-only prior 的 QWK `+0.0007` 是诊断级弱信号；ordinal difficulty 在 static-state 路径为 QWK `+0.0168`、3/3 wins，但没有支持动态核组合。当前保留 `dynamic_kernel_no_prior` 为候选，不扩展 prior 到更多 seeds。 |
| 2026-09-03 21:20:00 +08:00 Asia/Shanghai | 为窗口级主线与 daily-affect EMA-bag 工作线建立可横向阅读的指标接口 | 扩展 daily-affect `classification_metrics`，从五分类概率的 `expected_score` 计算 1--5 分数轴的 Expected RMSE、Expected raw r 和 Expected within-subject centered r；训练、温度校准、early-stopping validation 与 focused robustness 均记录该接口；`76_summarize_daily_affect_results.py` 能从既存 `predictions.npz` 回填旧 run 的 bridge metrics，并在 summary/paired delta 报告输出；同步计划、scripts README、repo docs README 和命令产物参考。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile src\\daily_multimodal\\daily_affect\\metrics.py src\\daily_multimodal\\daily_affect\\training.py scripts\\76_summarize_daily_affect_results.py scripts\\80_run_daily_affect_focused_robustness.py tests\\test_daily_affect_route.py` 通过；`PYTHONPATH='src;C:\\torch' python -m unittest tests.test_daily_affect_route -v` 通过 8 tests。 | bridge 只用于共同解释：daily-affect 保持 event-level bag 与 ordinal paired gate，窗口级主线保持原有 window-level regression protocol；相同 protocol 下可并列比较 RMSE/raw r/centered r，解读时仍需显式标注样本单位与测试行数。 |
| 2026-09-03 20:52:01 +08:00 Asia/Shanghai | 开始做 `A1_Wphysio_full / cross_day` 的 kernel 和 modality 诊断、focused ablation、候选与 baseline 5-7 seeds、missing/corruption robustness | 新增 focused follow-up 入口 `scripts/daily_affect/78_report_daily_affect_focused_diagnostics.py`、`scripts/daily_affect/79_run_daily_affect_focused_ablation.py`、`scripts/daily_affect/80_run_daily_affect_focused_robustness.py`；扩展 `DailyAffectOrdinalModel` 支持 `dynamic_kernel_no_prior` 与 `dynamic_fixed_short/medium/long`；把 `73_build_daily_affect_bags.py` 默认 split root 改为当前 `/vePFS-0x0d/DailyEEG/splits_new`；远端执行 `A1_Wphysio_full / cross_day / per_modality` 诊断、3-seed focused ablation、`bag_static` 与 `dynamic_kernel` 7-seed 扩展和 robustness，并同步轻量结果到 `outputs/server_sync/daily_affect_dynamic_a1_crossday_20260903/`。Synced through d2c1f1a plus current working tree。 | 本地 `python -m compileall -q src\daily_multimodal\daily_affect scripts\daily_affect\78_report_daily_affect_focused_diagnostics.py scripts\daily_affect\79_run_daily_affect_focused_ablation.py scripts\daily_affect\80_run_daily_affect_focused_robustness.py tests\test_daily_affect_route.py` 通过；本地 `PYTHONPATH='src;C:\torch' python -m unittest tests.test_daily_affect_route -v` 通过 8 tests；远端同一 unittest 通过 8 tests；`78` 输出 focused diagnostics；`79` 完成 `24` 个 focused ablation run；候选与 baseline 补齐到 `7` seeds；`80` 输出 `182` 条 robustness 明细和 summary。 | 3-seed focused ablation 显示 learned dynamic kernel 最强但 no-prior dynamic kernel 接近，说明增益主要来自动态时间核和状态路径，prior 是次要候选因素；7-seed clean gate 中 `dynamic_kernel` 对 `bag_static` 的 mean delta 为 QWK `+0.0133`、Macro-F1 `+0.0124`、ordinal MAE `-0.0378`，QWK wins `4/7`，可保留为 promising candidate 继续发展。Robustness 显示 video missing/shuffle 是主要脆弱点：相对 baseline QWK 分别 `-0.0210` 和 `-0.0306`。 |
| 2026-09-03 19:50:11 +08:00 Asia/Shanghai | 直接开始执行 daily-affect 更新计划并按步骤继续 | 在远端 `/vePFS-0x0d/home/wangzw/DailyEEG_multimodal_eeg_aligned` 以 `RUN_TAG=daily_affect_ordinal_20260903` 执行正式 daily-affect EMA-bag ordinal route：用 `/vePFS-0x0d/DailyEEG/splits_new` 的 `cross_subject`、`cross_day`、`within_subject_day` 构建三 route、三 seed bags；完成 phase0 `bag_static` baseline；完成 `state_uniform`、`prior_uniform`、`prior_ordD_uniform`、`dynamic_kernel` 与 `shared/per_modality` 全矩阵；运行 summary 和 diagnostics figure；同步轻量结果到 `outputs/server_sync/daily_affect_ordinal_20260903/`；同步更新计划、scripts README、repo-docs README、字段契约和命令产物参考。Synced through d2c1f1a plus current working tree。 | 本地 `python -m compileall -q src\daily_multimodal\daily_affect scripts\daily_affect\73_build_daily_affect_bags.py scripts\daily_affect\74_run_daily_affect_phase0_baselines.py scripts\daily_affect\75_run_daily_affect_state_matrix.py scripts\daily_affect\76_summarize_daily_affect_results.py scripts\daily_affect\77_plot_daily_affect_diagnostics.py tests\test_daily_affect_route.py` 通过；本地 `PYTHONPATH='src;C:\torch' python -m unittest tests.test_daily_affect_route -v` 通过 7 tests；远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest tests.test_daily_affect_route -v` 通过 7 tests；正式 `73` 完成 `27/27` bags，`74` 完成 `54/54` metrics，`75` 完成 `216/216` metrics，`76` 汇总 `270` runs，`77` 的 manifest 记录 `918` 个 figure 条目、实际图像文件 `199` 个；repo-docs validator 返回 `OK: 0 errors, 19 warning(s)`。 | 正式 daily-affect 路线已完成首轮三协议、三 route、双 normalization、三 seed 矩阵。所有 bag 形状均为 `tokens (1253,23,4,256)` 和 `modality_mask (1253,23,4)`；`cross_subject/cross_day` 各有 `42` 个 `pretrain/finetune` 训练叶子混合 event，`within_subject_day` 有 `214` 个 train/val/test 边界投影 event。preliminary gate 结果为 `18 pass / 54 stop_or_continue_diagnostic`；完整报告在远端 `outputs/daily_affect_ordinal_20260903/reports/`，本地轻量副本在 `outputs/server_sync/daily_affect_ordinal_20260903/`。 |
| 2026-09-03 13:49:24 +08:00 Asia/Shanghai | 补足 daily-affect 更新计划可以执行前的所有缺口 | 新增 `src/daily_multimodal/daily_affect/` 可执行包，包含 EMA bag 构建、shared/per-modality adapter、ordinal modality probe、affect state filter、dynamic EMA kernel、ordinal 分类模型、loss、metrics、temperature scaling、sampler、diagnostics 和训练闭环；新增 `scripts/daily_affect/73_build_daily_affect_bags.py`、`74_run_daily_affect_phase0_baselines.py`、`75_run_daily_affect_state_matrix.py`、`76_summarize_daily_affect_results.py`、`77_plot_daily_affect_diagnostics.py`；`77` 在缺少 `matplotlib` 时写 SVG fallback；新增 `tests/test_daily_affect_route.py`；同步 daily-affect 更新计划、scripts README、repo-docs README、字段契约和命令产物参考。Synced through d2c1f1a plus current working tree。 | `python -m compileall -q src scripts tests` 通过；`PYTHONPATH=src python -m unittest tests.test_daily_affect_route -v` 通过 2 tests、Torch 相关 3 tests 按预期跳过；`PYTHONPATH='src;C:\torch' python -m unittest tests.test_daily_affect_route -v` 通过 5 tests；临时合成数据端到端 CLI smoke `73_build_daily_affect_bags.py -> 74_run_daily_affect_phase0_baselines.py -> 75_run_daily_affect_state_matrix.py -> 76_summarize_daily_affect_results.py -> 77_plot_daily_affect_diagnostics.py` 通过，生成 bag、phase0 run、dynamic kernel run、summary 和 5 张 SVG fallback 图；repo-docs validator 返回 `OK: 0 errors, 19 warning(s)`。 | daily-affect route 的首个可执行切片已补齐：`73` 可从 canonical EEG-aligned 23-window event 构建 `(N_ema,23,4,256)` bags，`74` 跑 `bag_static` baseline，`75` 跑 state/prior/kernel 矩阵，`76/77` 汇总和画图。路线使用窗口级 `modality_mask (N_ema,23,4)`，`shared` 与 `per_modality` 是并列线路；EQL-CAF 继续独立使用 temporal token/`token_mask`，两条路线不会互相覆盖。本行记录的是执行前补缺口状态；正式远端全量矩阵见 2026-09-03 19:50:11 记录。 |
| 2026-08-27 19:09:34 +08:00 Asia/Shanghai | 开始检查 W3FM 的 ACC 跨天不稳、三路 embedding 尺度/分布不匹配、gate 小样本过拟合三类失败原因 | 新增 `scripts/wear_only_fm/72_diagnose_w3fm_failure_modes.py`，读取 canonical index、`wear_complete_mask`、三路 frozen embeddings、Phase 2 W3FM checkpoints/predictions 和 internal ablation summary；输出 embedding split shift、top-feature correlation stability、轻量 ridge probe、checkpoint gate 权重和 gate/error association；远端结果同步到 `outputs/server_sync/wear_fm_failure_diagnostics_20260827/`；同步计划、scripts README、repo-docs README 与命令产物参考。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile scripts\wear_only_fm\72_diagnose_w3fm_failure_modes.py` 通过；远端 `PYTHONPATH=src ./runtime/envs/eegpt-gpu-min/bin/python -m py_compile scripts/wear_only_fm/72_diagnose_w3fm_failure_modes.py` 通过；远端执行 `scripts/wear_only_fm/72_diagnose_w3fm_failure_modes.py --device cpu --out-root outputs/wear_fm/failure_diagnostics` 输出 JSON/MD 和 5 个 CSV；远端目录约 `130K`，`/vePFS-0x0d` 约 `191G` 可用，`/tmp` 约 `3.0G` 可用。 | 诊断结论：ACC/HARNet10 是当前最强可疑拖累分支。`cross_day` 删除 ACC 后相对完整 W3FM raw r `+0.0366`、RMSE `-0.0441`、centered r `+0.1130`；ACC top train features 在 cross_day test signed corr 为 `-0.0195`、sign agreement `0.2266`，轻量 probe 中 PPG raw r `0.1107` 高于 ACC `0.0599`。gate 在 test 上仍给 ACC 约 `0.50` 权重，且高误差样本 ACC 权重更高。GSR split shift 最大但 leave-one-out 和 gate/error 不支持其为主拖累；尺度/分布不匹配是次级风险。 |
| 2026-08-27 18:27:19 +08:00 Asia/Shanghai | 检查 Wear-only FM best epoch 偏早是否由过拟合或训练参数导致，并评估是否调整训练参数 | 新增 `scripts/wear_only_fm/70_summarize_wear_fm_training_diagnostics.py`，汇总 Phase 2、post-Phase3 internal ablation 与 training sweep 的 `metrics.json`/`val_history.csv`；扩展 `scripts/wear_only_fm/66_run_wear_only_fm_phase2.py` 与 `scripts/wear_only_fm/69_run_wear_only_fm_internal_ablation.py`，让后续新 run 记录 `val_raw_r`、`val_within_subject_centered_r`、`val_prediction_std` 和 `selection_score`，并支持 `--selection-metric rmse|raw_r|centered_r` 作为诊断选择口径；新增 `scripts/wear_only_fm/71_summarize_wear_fm_training_sweep.py` 汇总 W3FM_frozen 训练参数 sweep；同步计划、scripts README、repo-docs README 与命令产物参考。Synced through d2c1f1a plus current working tree。 | `python -m py_compile scripts\wear_only_fm\66_run_wear_only_fm_phase2.py scripts\wear_only_fm\69_run_wear_only_fm_internal_ablation.py scripts\wear_only_fm\70_summarize_wear_fm_training_diagnostics.py scripts\wear_only_fm\71_summarize_wear_fm_training_sweep.py` 通过；远端恢复后在 huoshan `outputs/wear_fm/training_sweep/` 完成 33-run sweep，本地同步到 `outputs/server_sync/wear_fm_training_sweep_20260827/`；重新运行 `python scripts\wear_only_fm\70_summarize_wear_fm_training_diagnostics.py` 覆盖 93 个 run；`python scripts\wear_only_fm\71_summarize_wear_fm_training_sweep.py` 输出 `wear_fm_training_sweep_summary.md`；远端空间复查 `/vePFS-0x0d` 约 `192G` 可用，training sweep 目录约 `101M`。 | 诊断结论：best epoch 偏早主要是 checkpoint selection 现象，训练实际继续到 patience 窗口；W3FM-like route 的 train loss 持续快速下降且 val RMSE 在 best 后漂高，符合 fast overfit 或优化过快。W3FM-like route `60/66` 触发 fast-overfit flag，普通 baseline `0/27`；W3FM-like train loss 平均下降 `64.71%`，baseline `8.45%`。`lr=1e-4` 改善 RMSE 但未恢复 cross_day raw r；`centered_r` selection 提高 within_subject_day centered r 但 RMSE 代价明显。默认 Phase 2/3 RMSE selection 和训练参数暂不替换；低学习率与 centered-r selection 保留为诊断分支。 |
| 2026-08-25 12:29:49 +08:00 Asia/Shanghai | 排查 W3FM 内部问题并加入 PPG partial FT 诊断 | 新增 `scripts/wear_only_fm/69_run_wear_only_fm_internal_ablation.py`，在 Phase 3 停止判定后按诊断边界运行 `W3FM_no_ppg/no_acc/no_gsr` 三路 leave-one-out 与 `W3FM_ppg_partial_ft`；同步远端产物到 `outputs/server_sync/wear_fm_internal_ablation_20260825/`，并更新计划、scripts README、repo-docs README 与命令产物参考。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile scripts\wear_only_fm\69_run_wear_only_fm_internal_ablation.py` 通过；远端 `py_compile` 通过；远端 frozen smoke 3/3 通过、PPG partial FT smoke 1/1 通过；正式矩阵完成 24 runs，输出 `wear_only_fm_internal_ablation_report.{json,md}`、`internal_ablation_summary.csv` 和 24 组 per-run metrics/checkpoint/history/prediction artifacts；本地同步副本共 148 个文件；空间复查显示 `/vePFS-0x0d` 可用约 `264G`，`/tmp` 可用约 `703M`，运行时使用项目内 `outputs/tmp`。 | 诊断结论：`cross_day` 上 `W3FM_no_acc` 最像解除拖累，均值 raw r `0.0656`、RMSE `0.9841`，相对完整 W3FM delta 为 raw r `+0.0366`、RMSE `-0.0441`、centered r `+0.1130`；`within_subject_day` 按 centered-r 主口径删除任一单模态都没有超过完整 W3FM，`no_acc` 虽降低 RMSE 但 centered r 降到 `-0.0949`；`W3FM_ppg_partial_ft` 未稳定修复问题。该诊断不覆盖 Phase 3 formal gate。 |
| 2026-08-25 03:50:00 +08:00 Asia/Shanghai | 进行 Wear-only FM Phase 3 | 新增 `scripts/wear_only_fm/68_summarize_wear_only_fm_phase3.py`，基于 Phase 2 冻结预测执行 protocol/route 三 seed 汇总、seed-level 最强 baseline 选择、subject-day block paired bootstrap、gate 判定和 PNG 诊断图；同步远端产物到 `outputs/server_sync/wear_fm_phase3_20260825/`，并更新计划、scripts README、repo-docs README 与命令产物参考。Synced through d2c1f1a plus current working tree。 | 本地 quick smoke `--bootstrap-iters 100` 通过；远端正式运行 `--bootstrap-iters 2000 --plot` 输出 `wear_only_fm_phase3_report.{json,md}`、`phase3_route_summary.csv`、`phase3_paired_bootstrap.csv` 和 6 张 PNG；远端报告含 9 个 seed-level paired comparisons；本地同步副本核对 JSON/MD、2 CSV、6 PNG 均存在；`python -m py_compile scripts/wear_only_fm/68_summarize_wear_only_fm_phase3.py` 通过。 | Phase 3 判定为 `stop_after_phase3_do_not_run_second_round_ablation`。gate 六项均未通过：`cross_day` W3FM 对最强 baseline mean Δraw r `-0.0848`、mean ΔRMSE `+0.0460`；`within_subject_day` mean Δcentered r `-0.0160`、mean ΔRMSE `+0.0480`。因此不进入第二轮消融。 |
| 2026-08-25 00:42:00 +08:00 Asia/Shanghai | 完成 Wear-only FM Phase 2 | 新增 `scripts/wear_only_fm/66_run_wear_only_fm_phase2.py` 与 `scripts/wear_only_fm/67_summarize_wear_only_fm_phase2.py`；先补齐 `Wmoment_frozen` 在 `cross_day/within_subject_day/cross_subject` × seed `240729/240730/240731` 的匹配 token，再执行 `Wphysio/Wdeep/Wmoment_frozen/W3FM_frozen` 36-run wear-only fatigue 矩阵；完整远端产物同步到 `outputs/server_sync/wear_fm_phase2_20260825/`；同步计划、scripts README、repo-docs README 与命令产物参考。Synced through d2c1f1a plus current working tree。 | 远端 Wmoment token 补齐 run_count=`9`；Phase 2 smoke 4/4 routes 通过；正式矩阵 run_count=`36`，per-run `metrics.json`/`best_checkpoint.pt`/`val_history.csv`/`predictions.npz`/`test_predictions.csv` 均为 36 个；本地同步副本计数一致；`python -m py_compile scripts/wear_only_fm/66_run_wear_only_fm_phase2.py scripts/wear_only_fm/67_summarize_wear_only_fm_phase2.py` 通过；`/vePFS-0x0d` 可用约 `265G`，`/tmp` 可用约 `705M`，运行时使用项目内 `outputs/tmp`。 | Phase 2 完成。三 seed 均值：`cross_day` 最强 raw r 为 Wdeep `0.1110`、W3FM `0.0290`；`within_subject_day` 最强 raw r 为 Wdeep `0.2087`、W3FM `0.1132`；`cross_subject` 最强 RMSE 为 Wmoment `0.9057`、W3FM `0.9737`。W3FM 对当 seed 最强 baseline 的 delta 在 9/9 seed-protocol 组合 raw r 为负、RMSE 为正；Phase 3 bootstrap 可作为正式停止判定，但 Phase 2 已显示无需进入第二轮消融。 |
| 2026-08-24 22:24:00 +08:00 Asia/Shanghai | 执行 Wear-only FM 计划至 Phase 1，并检查服务器空间 | 新增 `scripts/wear_only_fm/65_run_wear_only_fm_phase1.py` 与 `scripts/wear_only_fm/wear_fm_phase1_downloads.sh`；在远端 `/vePFS-0x0d/home/wangzw/DailyEEG_multimodal_eeg_aligned` 获取并校验 PaPaGei-S、HARNet10、NormWear 权重/源码，把 PPG/ACC/GSR 10 秒 staged inputs 对齐到 canonical 28,819 行，批量缓存三路 frozen embeddings；修订 `wear_only_fm_experiment_plan.md` 的 Phase 1 执行记录，并同步 scripts README、repo-docs README 与命令产物参考。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile scripts/wear_only_fm/65_run_wear_only_fm_phase1.py` 通过；远端 Phase 0 staged audit 完成 `row_count=28819`、`wear_complete_count=24127`、`failure_count=0`；远端 three-model smoke 通过；远端 batch 输出 PPG `(28819,512)`、ACC `(28819,1024)`、GSR `(28819,768)`，三路 valid count 均为 `24127`、finite true、mask 均等于 `wear_complete_mask`；`/vePFS-0x0d` 可用约 `265G`，`/tmp` 可用约 `1.9G`，`outputs/wear_fm` 约 `959M`。 | Phase 1 完成：外部 encoder 获取与 embedding 缓存已落盘，远端完整产物在 `outputs/wear_fm/`，本地摘要同步到 `outputs/server_sync/wear_fm_phase1_20260824/`；后续可进入 Phase 2，但需继续保持 frozen encoder 与监督 projection/gate/head 边界。 |
| 2026-08-24 16:12:38 +08:00 Asia/Shanghai | 将 fusion token normalization 做成参数并试跑 per-modality 效果 | 扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`，新增 `--token-normalization shared|per_modality`；默认 `shared` 保持历史跨模态共享 train-only 统计，`per_modality` 为每个模态 slot 单独拟合 train-only mean/std；JSON/runtime/train_audit 记录 normalization scope；新增单测覆盖 shared/per-modality shape 与 mask 行为；同步 `scripts/README.md`、`cross_attention_implementation_20260824.md`、命令参考，并新增 `token_normalization_probe_20260824.md`。Synced through d2c1f1a plus current working tree。 | 本地 `python -m py_compile .\scripts\window_fatigue\32_run_eegpt_centered_loss.py .\tests\test_fusion_variants.py` 通过；本地 `PYTHONPATH='src;C:\torch' python -m unittest tests.test_fusion_variants -v` 通过 12 tests；repo-docs validator 返回 `OK: 0 errors, 19 warning(s)`；远端 H20 使用临时脚本 `scripts/32_run_eegpt_centered_loss_token_norm_probe.py` 完成 `shared` 与 `per_modality` 2-route paired screen 及 48-route raw-only `video_only` screen，产物同步到 `outputs/server_sync/token_norm_probe_20260824/` 和 `outputs/server_sync/token_norm_route_screen_20260824/`；远端 token-normalization shape/assertion smoke 通过。 | 48-route single-seed raw-only screen 显示协议分化明显：`cross_day` 上 `per_modality` mean Δraw r `+0.0311`、ΔRMSE `-0.0126`，raw r 20/24 胜；`within_subject_day` 上 mean Δraw r `-0.0235`、ΔRMSE `+0.0331`，centered r 0/24 胜。因此 `per_modality` 可作为 cross_day 候选进入 3-seed paired gate，但不能替换全局默认 `shared`。 |
| 2026-08-23 13:58:00 +08:00 Asia/Shanghai | 开新分支并执行 EQL-CAF 计划的首批可执行切片 | 新建分支 `codex/eql-caf-plan`；新增 `src/daily_multimodal/temporal/` 的 5×2s temporal index、NPZ 契约与四模态合包模块；新增 `src/daily_multimodal/training/eql_caf.py`，包含 EQL-CAF EEG-anchored lag/quality/residual forward、temporal concat 与 temporal self-attention baseline；新增 `scripts/eql_caf/57_run_eql_caf_phase0_baselines.py`、`58_build_temporal_token_index.py`、`63_pack_eql_caf_tokens.py`、`64_run_eql_caf_fatigue_matrix.py` 和三个测试文件；同步 `scripts/README.md`、repo-docs README、命令参考与字段契约。Synced through d2c1f1a plus current `codex/eql-caf-plan` working tree。 | `python -m compileall -q src scripts tests` 通过；`PYTHONPATH='src;C:\torch' python -m unittest discover -s tests` 通过 161 tests；`python C:\Users\28303\.codex\skills\repo-docs\scripts\validate_repo_docs.py repo-docs --repo-root .` 返回 `OK: 0 errors, 18 warning(s)`；`scripts/eql_caf/57_run_eql_caf_phase0_baselines.py` 1-command manifest smoke 通过；`scripts/eql_caf/58_build_temporal_token_index.py` 合成 1-row index smoke 通过。 | EQL-CAF 计划已落下首批可执行本地切片：Phase 0 manifest、Phase 1 temporal index/pack 契约和 Phase 2-4 packed-NPZ fatigue matrix 入口已可验证；59-62 四模态真实 temporal token 提取仍需远端数据侧继续实现。 |
| 2026-08-23 14:18:00 +08:00 Asia/Shanghai | 继续 EQL-CAF 计划，但按 `$stop-that-shit` 收住范围 | 新增 `src/daily_multimodal/temporal/global_repeat_tokens.py` 与 `scripts/eql_caf/59_extract_eeg_temporal_tokens.py`、`60_extract_wear_temporal_tokens.py`、`61_extract_video_temporal_tokens.py`、`62_extract_audio_temporal_tokens.py`，作为 `global_repeat_smoke_v1` 管线验证入口；新增 `tests/test_global_repeat_temporal_tokens.py`；同步脚本索引、命令参考、字段契约和 README，明确这些 smoke token 不作为 lag-aware 证据。Synced through d2c1f1a plus current `codex/eql-caf-plan` working tree。 | `python -m compileall -q src\daily_multimodal\temporal scripts\eql_caf\59_extract_eeg_temporal_tokens.py scripts\eql_caf\60_extract_wear_temporal_tokens.py scripts\eql_caf\61_extract_video_temporal_tokens.py scripts\eql_caf\62_extract_audio_temporal_tokens.py tests\test_global_repeat_temporal_tokens.py` 通过；`PYTHONPATH='src;C:\torch' python -m unittest tests.test_global_repeat_temporal_tokens tests.test_temporal_token_contract tests.test_temporal_window_slicing tests.test_eql_caf_model -v` 通过 12 tests；合成 24-row 端到端 smoke 完成 58→59→60→61→62→63→64，`B1` CPU 2 epoch 输出 `eql_caf_matrix_report.json/md`。 | EQL-CAF 本地执行链已打通到 packed-NPZ 训练入口；下一步应在远端真实数据侧把 `global_repeat_smoke_v1` 替换为真实 EEG/Wear/Video/Audio 2 秒 temporal encoder。 |
| 2026-08-23 13:04:25 +08:00 Asia/Shanghai | 同步当前仓库状态 | 对当前 dirty worktree 做 Understanding Sync：检查 `git status --short`、`git diff --stat`、`repo-docs/README.md`、`repo-docs/references/commands-and-artifacts.md`、`scripts/README.md` 与当前脚本/报告变更是否对齐。当前 repo-docs 已覆盖 2026-08-17 到 2026-08-20 的 EEG dual-branch CNN、calibration Phase 0-4、fixed-token 11-label fusion、Wear × MOMENT、fusion variant 和 attention-vs-concat 证据文档；本次只补同步锚点，不改动源码、实验报告或命令口径。Synced through 44ddd57 plus current dirty working tree。 | `git status --short` 显示已有脚本、测试、技术路线和 repo-docs 未提交变更及多份 untracked 报告/脚本；`python C:\Users\28303\.codex\skills\repo-docs\scripts\validate_repo_docs.py repo-docs --repo-root .` 返回 `OK: 0 errors, 18 warning(s)`。剩余 warning 为既有阅读提示、缺本地 outputs 产物、外部 Markdown 缺 repo-docs agent 规则提示和工作区晚于旧同步锚点。 | 完成一次当前仓库状态同步；repo-docs 结构有效，后续若提交或继续扩展实验，应在对应页按实际变更继续同步。 |
| 2026-08-20 Asia/Shanghai | 总结对话内完成的工作，形成 handoff | 新增根目录 `wear_moment_handoff_20260820.md`：工作链总览（T0→方案 C）、代码改动清单（新增/修改文件 + 服务器代码位置）、服务器环境与数据事实（连接方式、numpy 降级副作用、权重 sha、数据映射）、结果文件路径（本地 + 服务器）、三个实验矩阵口径与关键数字、复现命令（token 矩阵 / Phase 1-2 / 方案 C / 门槛汇总 / 测试）、给下一个工作者的注意事项（seed 口径、EEG token 仅 240800、旧路径不可用、长任务与显存、转录错误教训），并补充「同日并行工作提示」节（fusion variant 消融结论：attention 无价值、建议 concat+MLP 新基线；Wear × MOMENT 结果基于 attention 口径，需在新基线下重验 frozen 增益）；repo-docs README 阅读路径新增「接手 Wear × MOMENT 工作」入口。仅文档变更。 | handoff 中所有路径/命令/数值与仓库现状核对（脚本路径、`outputs/server_sync/wear_moment_20260820/` 产物、gates 数字、sha256）；并行工作条目从 change-log 现有记录（fusion variant 消融）交叉引用。 | handoff 已生成：`wear_moment_handoff_20260820.md`。 |
| 2026-08-20 Asia/Shanghai | 整理一份证据充分、附表完整的文档，证明当前 attention 融合相比朴素拼接没有价值 | 新增 `scripts/window_fatigue/56_build_fusion_attention_vs_concat_evidence.py`：从归档 attention 矩阵（`eeg_encoder_256d_5route_fusion_video_only_seed240800_raw.json`）、决策切片四变体 JSON、全量 concat/eeg_anchor 矩阵 JSON 与 3 条定点复现 JSON 生成根目录 `fusion_attention_vs_concat_evidence_20260820.md`（431 行）与 `outputs/reports/fusion_attention_vs_concat_evidence_20260820.json`；文档含结论摘要、实验设计、基线可复现性（3/3 逐位一致）、决策切片均值与完整 32-run 明细、全量 180 行配对明细（分协议三张表）、协议/分支/组合/分支×协议聚合、胜/负/平与双侧符号检验 p 值、eeg_anchor 对照、稳健性与局限、复现路径；同步 scripts/README 与服务器副本。Synced through 44ddd57 plus current working tree。 | 生成脚本 `py_compile` 通过；配对完整性校验 paired=180/180（concat 与 eeg_anchor）；文档所有表格由脚本直接读取 JSON 生成（无手工转录）；关键数字与 `fusion_variant_results_20260820.md` 交叉一致。 | 证据文档已完成：全量配对 `within_subject_day` Δraw r `+0.0394`（42 胜/12 负，符号检验 p=5.2e-5）、ΔRMSE `−0.0143`；`cross_day` Δraw r `+0.0137`；`cross_subject` 持平；唯一 attention 略优分支 `eegpt_partial_ft`（−0.008）。结论：以 `concat + MLP` 替换当前 attention 融合作为新基线。 |
| 2026-08-20 Asia/Shanghai | 实现并执行 fusion variant 消融（attention vs concat vs 多头多 query vs EEG 锚点），直接在服务器完成实验 | (1) `scripts/window_fatigue/32_run_eegpt_centered_loss.py` 的 `AttentionRegressor` 新增 `variant`（`attention` 默认 / `concat` / `attention_multihead_pma` / `eeg_anchor`）与 `num_heads/num_latent`，各变体 `encode()` 输出 `(B, hidden_dim)`，head/损失/评估/`_predict` 链路复用；新增 CLI `--fusion-variant/--attn-num-heads/--attn-num-latent`，JSON 记录 `fusion_variant` 等字段，markdown 增 fusion 列；(2) 新增 `tests/test_fusion_variants.py`（9 tests）、`scripts/window_fatigue/54_summarize_fusion_variants.py`（切片汇总）、`scripts/window_fatigue/55_summarize_fusion_variant_full_paired.py`（全量配对）；(3) 服务器执行：决策切片 32 runs（seed 240800 固定）× 4 变体、3 条归档同 seed 定点复现、concat 与 eeg_anchor 各 180-run 全量配对矩阵（与归档同 seed 序列，paired 180/180）；(4) 同步命令参考、scripts/README、新增根目录 `fusion_variant_results_20260820.md` 与 `outputs/server_sync/fusion_variant_20260820/`。Synced through 44ddd57 plus current working tree。 | 本地 19 tests + 全量 151 tests + compileall 通过；服务器 19 tests 通过；定点复现 3/3 逐位命中归档（cross_day `0.9481/0.3504/0.1176`、within_subject_day `0.9243/0.4049/0.2062`、cross_subject `0.9624/0.0841/0.0865`）；决策切片 32 runs 与全量 360 runs 全部 rc=0；配对分析 paired=180/180。 | **最终结论：当前 attention 融合相比朴素拼接无价值。** concat 全量同 seed 配对：within_subject_day Δraw r `+0.0394`（42 胜/12 负）、Δcentered r `+0.0311`、ΔRMSE `−0.0143`；cross_day Δraw r `+0.0137`、ΔRMSE `−0.0058`；cross_subject 持平；除 `eegpt_partial_ft` 外 4/5 EEG 分支 concat 更优。`attention_multihead_pma` 未通过（切片 raw r 低于 concat）；`eeg_anchor` 仅 cross_subject raw r（+0.012）与 cross_day RMSE（−0.008）小幅改善，within_subject_day raw r −0.012，不作主线。→ raw r 主线建议改用 `concat + MLP` 基线并转向提升单模态表征质量。 |
| 2026-08-20 Asia/Shanghai | 实现 fusion variant 消融（实验2 拼接 vs 实验1 attention 基线；实验3 多头+多 query；实验4 EEG 锚点非对称 cross-attention） | 扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py` 的 `AttentionRegressor`：新增 `variant`（`attention` 默认、`concat`、`attention_multihead_pma`、`eeg_anchor`）与 `num_heads/num_latent`，所有变体 `encode()` 输出 `(B, hidden_dim)`，head/损失/评估/`_predict` 链路完全复用；`concat` 用零掩码拼接 + mask 指示位（维度 `256*M+M` 动态），`attention_multihead_pma` 用 k 个 latent query 对模态 token 做多头 cross-attention 后 mean-pool，`eeg_anchor` 以 EEG token（index 0）作 query、其余模态作 key/value；新增 CLI `--fusion-variant`、`--attn-num-heads`、`--attn-num-latent`，结果 JSON 记录 `fusion_variant` 等字段，markdown 报告新增 fusion 列；新增 `tests/test_fusion_variants.py`（9 tests）；命令参考新增「Fusion variant 决策切片」行。Synced through 44ddd57 plus current working tree。 | 本地 3.12 runtime（CPU torch 2.13.0 装入 `C:\torch` 以规避 Windows 路径过长 WinError 206）：`PYTHONPATH='src;C:\torch' python -m unittest tests.test_fusion_variants tests.test_phase2_fusion_loss tests.test_eeg_encoder_fusion_tokens -v` 通过 19 tests；全量 `python -m unittest discover -s tests` 通过 151 tests；`python -m compileall -q src scripts tests` 通过。 | 四变体代码与单测就绪，待服务器执行：先按原始 0814 命令 + `--fusion-variant attention` 复现基线，再跑决策切片（cross_day/within_subject_day × `eeg_eegpt_partial_ft_v1` × 4 experiments × 4 variants，`--experiment-seed-fixed --seed 240800`），按 test `raw_r`/`within_subject_centered_r`/`rmse` 判定 concat 是否追平 attention、pma/eeg_anchor 是否有增益。 |
| 2026-08-20 Asia/Shanghai | 更新 `technical_route_20260814.md` 的 Top 3 / 按 EEG 平均 / 全部融合结果附表为含 Wear × MOMENT 的最新口径 | 用脚本从 `planC_144.json` 重新生成三个表：各划分协议 Raw R Top 3（9 行）、按 EEG Route 分类的四模态平均表现（2 EEG × 3 protocols）、附表（144 runs 按 protocol 分组按 raw r 排序，72 行），并在每表前注明口径（seed 240729 固定、`--experiment-seed-fixed`、2 EEG × 24 fusion routes；旧 180-run 矩阵为 run_number 递增 seed，不可混比）。同时核查发现 `wear_moment_results_20260820.md` §3 Phase 2 完整表存在转录错误（如 cross_day 240729 `Wmoment_frozen` 误写为 raw r 0.3901/RMSE 0.9235，真实为 0.3319/0.9159），已用 JSON 生成的修正表替换；§5 的 3-seed 范围修正为 raw r 0.309–0.332 vs 0.230–0.280；`technical_route_20260814.md` 2026-08-20 小节的典型数值同步修正（0.3319/0.9159 vs 0.2579/0.9479）。经 phase1/phase2/planC 三份 JSON 交叉核对，0 diff（固定 seed 运行可确定性复现），门槛均值（+0.0626/-0.0390 等）与 G1/G2/G3 判定均正确无需改动。 | 生成脚本直接读 `planC_144.json`（144 rows）与 `phase2_seed2407{29,30,31}.json` 输出表格；抽查附表行（within_subject_day `A1_Wmoment_frozen_no_audio` raw r 0.3901）与 JSON 一致；全库 grep 确认无残留 0.3901/0.9235/0.3161 等错误数值（历史 strict 报告中的 0.3161 为合理旧值）。 | `technical_route_20260814.md` 三个表已更新为新口径（318 行）；`wear_moment_results_20260820.md` §3 表与 §5 数字已修正。 |
| 2026-08-20 Asia/Shanghai | 按方案 C 执行全维度矩阵（加入 moment frozen/partial ft 后的矩阵变化落地） | 扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py` 的 `EXPERIMENT_BRANCHES`（新增 A1/B0/A2 × moment × full/no_audio 共 10 个 route）；泛化 `scripts/window_fatigue/53_summarize_wear_moment_gates.py` 为全维度（36 配置组 paired delta + 门槛 + 144 行完整指标表，合成数据测试通过）；更新实验方案文档 §9（方案 C 口径：2 EEG × 24 routes × 3 protocols = 144 runs，固定 seed 240729）；服务器完成方案 C 全量 144 runs（约 40 分钟）；更新 `wear_moment_results_20260820.md` §5b 与 `technical_route_20260814.md` 2026-08-20 小节；产物同步本地。 | 服务器先 8-run 冒烟验证新 route（B0/A2 moment 组合 × 2 EEG branches 全部通过）；全量 144 runs 全部 completed（监控确认）；53 脚本输出 gates_planC（config_count=36）；本地合成数据测试 432 行/36 配置组通过。 | 方案 C 结论：主线 EEG × cross_day 下 `Wmoment_frozen` 相对 Wdeep 在全部 6 个 video/audio 配置上都更优（raw r Δ +0.033~+0.087），within_subject_day RMSE 6/6 更低；低监督 EEG 下增益不稳定；partial FT 全维度无一致增益，维持"不作为默认路线"结论。推荐主线组合 `eeg_eegpt_partial_ft_v1 + Wmoment_frozen`。产物在 `outputs/server_sync/wear_moment_20260820/planC_144/`。 |
| 2026-08-20 Asia/Shanghai | 完成 Wear × MOMENT 实验并出结果 | 服务器完成完整 token 矩阵（3 协议 × 2 profiles × 3 seeds = 18 runs，19.5 min）与融合矩阵：Phase 1 screen（12 runs，seed 240729 固定组，`--experiment-seed-fixed`）G1 通过；Phase 2 confirm（3 seed 组 × 12 = 36 runs，wear token seed 240800/240801/240802，EEG token 固定 240800——为此给 `32_run_eegpt_centered_loss.py` 新增 `--wear-token-seed` 解耦参数）。新增 `scripts/window_fatigue/53_summarize_wear_moment_gates.py` 输出 G1/G2/G3 判定与 paired 表。写根目录 `wear_moment_results_20260820.md` 正式报告；更新 `technical_route_20260814.md`（Wear 表新增 `Wmoment_frozen`/`Wmoment_partial_ft` 两档 + 2026-08-20 更新小节）；repo-docs README 阅读路径新增结果入口。 | 服务器 53 脚本对 3 个 phase2 JSON 汇总：pair_count=9、G1/G2/G3 全部通过（详见报告）；完整结果表 `phase2_full_results.md` 由真实 report JSON 生成；所有产物同步到本地 `outputs/server_sync/wear_moment_20260820/`。 | **`wear_moment_frozen_v1` 建议纳入主线**：cross_day 3/3 seeds 优于 Wdeep（raw r Δ+0.063、RMSE Δ-0.039），cross_subject 稳定更优，within_subject_day 持平；partial FT 仅 cross_subject 稳定，保留为诊断。环境副作用：numpy 2.4.6→1.25.2（momentfm 钉死），已记录。 |
| 2026-08-20 Asia/Shanghai | 开始执行 Wear × MOMENT 实验（T0 前置 + T1-T4 实现 + 服务器 smoke） | T0：服务器 `eegpt-gpu-min` 装 momentfm 0.1.4（`--no-deps` + `transformers==4.33.3`；副作用 numpy 2.4.6→1.25.2，pandas 2.3.3 共存已验证）；经 hf-mirror.com 下载 MOMENT-1-small（repo sha `411e2882`，safetensors 145MB，sha256 `785e6c6f...`）到 `outputs/checkpoints/moment-1-small/`；数据侧核验：aligned index 无 wear 字段，wear 元数据取自 `wear_physio_preprocessed_eeg23win_embeddings.npz`（wear_mask 24,127/28,819、source_wear_file、窗口时间），源 CSV 按 basename 重定位到 `/vePFS-0x0d/DailyEEG_multimodal/raw/wear/out/`（372 个引用文件全部存在），sample_id 顺序与 canonical index 完全一致；MOMENT 实测 d_model=512、10.4ms/窗口。T1-T4：新增 `src/daily_multimodal/training/wear_moment_matrix.py`（frozen/partial-FT 两档 token 训练，复用 `wear_real` CSV 解析与对齐矩阵，输出 `wear_tokens/{protocol}/{profile}/seed_{seed}.npz` 契约仿 `write_eeg_embedding_npz`）、`scripts/window_fatigue/16_run_wear_moment_matrix.py`、`scripts/window_fatigue/53_summarize_wear_moment_gates.py`（G1/G2/G3 门槛汇总）、`tests/test_wear_moment_matrix.py`（6 tests）；扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`：BRANCHES 增加 `wear_moment_frozen_v1`/`wear_moment_partial_ft_v1`、EXPERIMENT_BRANCHES 增加 `A1_Wmoment_frozen_full`/`A1_Wmoment_ft_full`、新增 `--experiment-seed-fixed`；同步 `scripts/README.md`、repo-docs README 与命令参考。 | 本地 `py -m unittest tests.test_wear_moment_matrix` 6/6 通过（测试临时目录改到 `outputs/.test_tmp`，绕开沙箱对 tempfile 的拦截）；服务器 `unittest` 6/6 通过、`tests.test_eeg_encoder_fusion_tokens` 3/3 通过；服务器全链路 smoke 通过：token 契约（28819×256、NaN=0、sample_id 对齐、mask 与 wear_physio 完全一致）、1-epoch 融合 `cross_day/A1_Wmoment_frozen_full` RMSE 0.9233/raw r 0.3211/centered r 0.1287；全量 preflight：coverage 0.8372、MOMENT 加载 1.79s、64 窗口推理 0.66s。 | T0-T4 与 smoke 完成，产物在 `outputs/server_sync/wear_moment_20260820/`（preflight/smoke/fusion_smoke 已同步本地）；完整 token 矩阵 18 runs 与 Phase 1/2 融合矩阵执行中。 |
| 2026-08-20 Asia/Shanghai | 决定本轮不试 MOMENT-large，只跑 MOMENT-1-small | 修订 `wear_moment_experiment_plan_20260820.md`：删除 large-frozen 消融档（Phase 2 wear routes 回到 4 档、runs 回到 36、删除 G4 门槛、删除 large 下载项），§4 规模决策改为「只跑 MOMENT-1-small，暂不试 large；若 small 结果接近门槛再重新评估」。仅文档变更。 | 全文检索确认无残留 large-frozen 档位/run 数/G4 引用；run 数核算回到 1×4×1×1×3×3=36，合计 48 runs + 18 次 token 训练。 | 矩阵定格为 MOMENT-1-small 单规模方案：Phase 1 12 runs + Phase 2 36 runs，合计 48 个融合 runs。 |
| 2026-08-20 Asia/Shanghai | 确认 MOMENT 模型规模：用 MOMENT-1-small 是否够 | 修订 `wear_moment_experiment_plan_20260820.md`：新增「模型规模决策」小节（主路线 small 约 12M；large 约 120M 仅做 frozen 免费消融，不做 partial FT；large-frozen 无稳定增益则主线定格 small）；Phase 2 wear routes 由 4 档改为 4–5 档（默认加 `wear_moment_large_frozen_v1`，runs 36→45，large-frozen 只增加表征推理 + 9 次投影头训练）；新增验收门槛 G4（large-frozen 相对 small-frozen 3-seed raw r Δ≥0.01 且 ≥2/3 seeds 同号才考虑 large）；规模汇总表同步更新（合计 48 或 57 runs）。仅文档变更。 | 决策依据与 `wear_moment_experiment_plan_20260820.md` 现有口径核对一致（输入 320×5×10s、下游仅轻量融合器、28,819 窗口监督规模、EEG 侧 partial FT 低 LR/解冻 2 block 先例）；run 数按维度乘积核算（1×5×1×1×3×3=45）。 | 规模决策已写入方案：主路线 MOMENT-1-small，large 仅 frozen 消融档；默认预期主线定格 small。 |
| 2026-08-20 Asia/Shanghai | 缩小 Wear × MOMENT 实验矩阵：EEG 只留 eegpt partial ft、audio 默认保留（只跑 full）、video 只留 A1、protocol 三种全跑 | 修订 `wear_moment_experiment_plan_20260820.md`：Phase 1 screen 由 96 runs 收窄为 12 runs（1 EEG × 4 wear × 1 video × 1 audio × 3 protocol × 1 seed）；Phase 2 confirm 为 36 runs（3 seeds，`--experiment-seed-fixed` 保证 4 条 wear route 严格同 seed 配对）；新增 route 由 12 个收窄为 2 个（`A1_Wmoment_frozen_full` / `A1_Wmoment_ft_full`），基线复用已有 `A1_Wphysio_full` / `A1_Wdeep_full`；执行方式改为 `--experiment-set custom` 显式 12 项（避免 `video_only` 带回 B0/A2 与 no_audio）；token 训练 18 次一次算齐（3 协议 × 2 profiles × 3 seeds），Phase 1 复用 seed 240800 组；合计 48 个融合 runs，预计 H20 GPU 约 3–6 h。仅文档变更。 | run 数按维度乘积重新核算（1×4×1×1×3=12；1×4×1×1×3×3=36）；确认 `A1_Wphysio_full`/`A1_Wdeep_full` 已存在于 `32_run_eegpt_centered_loss.py` 的 `EXPERIMENT_BRANCHES`，基线无需新增；确认 `--experiment-seed-fixed` 为新增可选参数、默认行为不变。 | 矩阵修订完成并写入 `wear_moment_experiment_plan_20260820.md`；G1 门槛口径不变（cross_day / within_subject_day 为主，cross_subject 诊断）。 |
| 2026-08-20 Asia/Shanghai | 给出接入 MOMENT 的实验方案，包括实验矩阵 | 新增根目录 `wear_moment_experiment_plan_20260820.md`：两阶段矩阵（Phase 1 screen 1-seed 96 runs → Phase 2 paired confirm 3-seed 24 runs），固定 EEG 主线 `eegpt_partial_ft_v1`，wear 基线同配对照（Wphysio/Wdeep vs wear_moment_frozen_v1/wear_moment_partial_ft_v1），新增 12 个 `B0/A1/A2_Wmoment_*` 融合 route；给出 token 训练机制（仿 `eeg_encoder_matrix` 输出 `wear_tokens/{protocol}/{profile}/seed_{seed}.npz`）、工程改动清单（新模块 `wear_moment_matrix.py`、新脚本 `16_run_wear_moment_matrix.py`、`32_run_eegpt_centered_loss.py` 挂载点、测试）、前置 preflight（wear 序列覆盖率、sample_id 顺序、mask 复用现有 wear_mask）、验收门槛 G0–G3 与风险回退；在 `repo-docs/README.md` 阅读路径表新增指向该方案的行。仅文档变更，未改代码/数据/脚本。 | 对照 `wear_real.py`（`sequence.npz` 键 ppg/gsr/acc、`_wear_cache_dir` 布局、window index 源路径回退）、`eeg_encoder_matrix.py`（profile 机制、`write_eeg_embedding_npz` 契约、frozen/partial FT 超参）与 `32_run_eegpt_centered_loss.py`（BRANCHES 加载要求 `np.array_equal(sample_id)`、EXPERIMENT_BRANCHES、video_only 集合）逐项核验工程落点；run 数按维度乘积核算。 | 实验方案已生成于 `wear_moment_experiment_plan_20260820.md`；合计约 120–132 个融合 runs + 17 个 token 训练 runs，预计 H20 GPU 约 4–7 小时。下一步按 §8 任务清单从 preflight（T0）开始执行。 |
| 2026-08-20 Asia/Shanghai | 调研 Wear 模态是否可以接入开放获取的预训练模型，并单独写一份模型选择方案 | 新增根目录 `wear_model_selection_20260820.md`：梳理 Wear 现状（Wphysio/Wdeep 均为非预训练、固定随机投影），核验候选模型开放获取状态（MOMENT-1 MIT/HF 直下、Pulse-PPG Zenodo 发布、PRIMUS BSD-3-Clause-Clear、AnyPPG open-source weights、NormWear GitHub、IMU2CLIP 开源但许可待确认、SensorFM 未开源排除），给出分阶段推荐（MOMENT-1 small frozen 起步 → partial FT/large → Pulse-PPG/PRIMUS 领域专精）、工程落点（`wear_real.py` 新 profile、仿 `eeg_encoder_matrix` 的 token 训练、`32_run_eegpt_centered_loss.py` 的 BRANCHES/EXPERIMENT_BRANCHES 挂载）与验收门槛；在 `repo-docs/README.md` 阅读路径表新增指向该方案的行。仅文档变更，未改代码/数据/脚本。 | 对照 `wear_real.py`（64/32/32 Hz 目标采样率、`_aligned_sequence_matrix` 320×5）、`32_run_eegpt_centered_loss.py`（BRANCHES 从 npz 懒加载、EXPERIMENT_BRANCHES route 元组）和 `34_run_eeg_encoder_matrix.py`（profile 输出 `{protocol}/{profile}/seed_{seed}.npz`）确认工程落点与现有机制一致；候选模型逐项检索官方仓库/论文/Zenodo/HF 记录核验许可证与权重获取途径，未核验项在文档中标注「待核」。 | 方案文档已生成于 `wear_model_selection_20260820.md`；结论为可以接入，首选 MOMENT-1-small frozen（MIT、320×5 零格式改造、覆盖 PPG/GSR/ACC 三路），领域专精候选为 Pulse-PPG（PPG）与 PRIMUS（ACC，需 gyro 填充适配）。 |
| 2026-08-17 Asia/Shanghai | 试用用户附件 `networks.py` 中的 EEG 模型并输出 EEG-only 指标和可视化 | 在 `daily_multimodal.training.eeg_encoder_matrix` 新增 `eeg_cnn_dual_branch_v1` profile，把附件 dual-branch `CNNEncoder` 适配为 200 Hz、10 秒、59 通道 EEG 输入：2000 点池化到 128 步，双分支各 128D 后拼为 256D pooled feature，再接 projection/head；scratch CNN 使用 `1e-3` AdamW 训练，并对该 profile 启用 GPU cached tensor 读取以避免 memmap CPU 瓶颈。更新 `scripts/embeddings/34_run_eeg_encoder_matrix.py` 默认 profile、单元测试、脚本索引和命令参考。Synced through current working tree。 | 本地 `python -m py_compile src\daily_multimodal\training\eeg_encoder_matrix.py scripts\embeddings\34_run_eeg_encoder_matrix.py tests\test_eeg_encoder_matrix.py` 通过；本地 `PYTHONPATH=src python -m unittest tests.test_eeg_encoder_matrix -v` 通过且 torch 相关 smoke 在本机无 torch 时跳过；远端 H20 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest -v tests.test_eeg_encoder_matrix.EEGEncoderMatrixTests.test_dual_branch_cnn_profile_smoke_writes_metrics_and_256d_tokens` 通过；远端 H20 seed 240800 三协议正式 run 完成，GPU cached run 峰值显示约 15.4GB 显存和约 89% GPU utilization；本地同步并生成 HTML/PNG 可视化，人工查看 PNG 无明显布局问题。 | 新模型 EEG-only seed 240800 test：`cross_subject` RMSE `0.9377`、raw r `0.0861`、centered r `0.0875`；`cross_day` RMSE `0.9506`、raw r `0.0890`、centered r `0.0032`；`within_subject_day` RMSE `0.9754`、raw r `0.2837`、centered r `0.1279`。汇总 Markdown 与图位于 `outputs/server_sync/eeg_cnn_dual_branch_20260817_gpu_cached/summary/`；该 profile 是 fatigue-supervised EEG-only 对照，当前五条 256D fusion 主线仍按 `eeg_encoder_256d_tokens` 口径解读。 |
| 2026-08-17 Asia/Shanghai | 开始尝试 temporal GRU、subject-day mean+residual 和 high fatigue day oversampling 三条后续路线 | 新增 `scripts/calibration/51_run_prediction_calibration_next3_screen.py`，复用现有 256D EEG/Wear/Video/Audio token 分支并按 subject-day sequence 做 split-safe 小筛查；新增 `scripts/calibration/52_plot_prediction_calibration_next3_trends.py`，绘制类似 `fatigue_top3_routes_by_protocol.png` 的 Phase 4 next-three test 趋势图，并输出 Phase 0 baseline 对比表；同步脚本索引和命令参考。Synced through 44ddd57 plus current working tree。 | 本地 `python -m py_compile scripts\calibration\51_run_prediction_calibration_next3_screen.py scripts\calibration\52_plot_prediction_calibration_next3_trends.py` 通过；远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m py_compile scripts/calibration/51_run_prediction_calibration_next3_screen.py` 通过；远端 H20 smoke `--limit-runs 1 --epochs 1 --device cuda` 通过；远端 H20 正式 `--epochs 60 --device cuda` 完成 6 runs；本地 `python scripts\calibration\52_plot_prediction_calibration_next3_trends.py --split test` 生成 PNG/CSV/Markdown，并人工查看 PNG 无明显布局重叠。 | 产物位于 `outputs/server_sync/fatigue_calibration_20260816/phase4_next3_screen/` 和 `outputs/server_sync/fatigue_calibration_20260816/phase4_next3_trend_visualizations/`。验证集上 temporal GRU 在两条路线都提升 raw/centered r；test 上 `within_subject_day/A2_Wdeep_full` 的 temporal GRU 从 raw r `0.4046` 到 `0.4500`、centered r `0.1736` 到 `0.1986`、RMSE delta `-0.0289`，mean+residual 从 raw r `0.4046` 到 `0.4624`、centered r `0.1736` 到 `0.2226`、RMSE delta `-0.0345`。cross-day test 仅 temporal GRU raw r 小幅改善 `+0.0136`，centered r 下降 `-0.0473`。high fatigue oversampling 可小幅降低高 fatigue 绝对 bias，但相关性和误差不稳定。当前结论是 within-subject-day 的 temporal/residual 方向值得做 3-seed paired screen；还不能作为正式结论。 |
| 2026-08-17 Asia/Shanghai | 生成类似 `fatigue_top3_routes_by_protocol.png` 的 Phase 0-3 趋势图 | 新增 `scripts/calibration/50_plot_prediction_calibration_phase0_to_phase3_trends.py`，按 4 条 representative route 选择 test split 中真实 fatigue 波动最明显的 subject-day，绘制真实 fatigue、Phase 0 raw、Phase 1 variance calibration、Phase 2 best dynamic-range candidate 和 Phase 3 best dynamic-range head 的趋势线；同步脚本索引和命令参考。Synced through 44ddd57 plus current working tree。 | `python -m py_compile scripts\calibration\50_plot_prediction_calibration_phase0_to_phase3_trends.py` 通过；`python scripts\calibration\50_plot_prediction_calibration_phase0_to_phase3_trends.py --split test` 生成 PNG 和 CSV；人工查看 PNG，确认中文字体、legend、四个 panel 与趋势线正常显示。 | 产物位于 `outputs/server_sync/fatigue_calibration_20260816/phase0_to_phase3_trend_visualizations/phase0_to_phase3_route_trends_test.png`。图中 Phase 1 variance 线明显更分散，但常偏离真实阶梯标签；Phase 0、Phase 2 和 Phase 3 线整体仍集中在低 2 到低 3 区间，直观复现动态范围压缩问题。 |
| 2026-08-17 Asia/Shanghai | 可视化对比 Phase 0 到 Phase 3 的 calibration 结果 | 新增 `scripts/calibration/49_plot_prediction_calibration_phase0_to_phase3.py`，从现有 Phase 1/2/3 汇总产物中抽取 Phase 0 raw baseline、Phase 1 variance calibration、Phase 2/3 最佳动态范围候选，生成动态范围、high fatigue bias、误差与相关性 delta 的总览图，并同步脚本索引和命令参考。Synced through 44ddd57 plus current working tree。 | `python -m py_compile scripts\calibration\49_plot_prediction_calibration_phase0_to_phase3.py` 通过；`python scripts\calibration\49_plot_prediction_calibration_phase0_to_phase3.py` 生成 SVG、CSV 和 Markdown；Edge headless 将 SVG 渲染为 PNG，并人工查看无明显布局重叠。 | 产物位于 `outputs/server_sync/fatigue_calibration_20260816/phase0_to_phase3_visual_compare/`。图中显示 Phase 1 variance calibration 可把 `pred_std / true_std` 从约 `0.59` 拉到 `1.00`，并把 high fatigue bias 从约 `-1.59` 改到 `-1.06`，但 RMSE/MAE 明显恶化；Phase 2/3 最佳动态范围候选只到约 `0.61`/`0.57`，没有稳定改变动态范围低的问题。 |
| 2026-08-17 Asia/Shanghai | 基于 `technical_route_20260814.md` 执行多标签 fixed-token 实验计划直到门槛停止 | 新增 `scripts/multilabel/48_run_fixed_tokens_multilabel_fusion.py`，独立于 `39-47` fatigue calibration 链路；该脚本先审计 canonical 28,819 行、1,253 events、`event_window_id=0..22` 和 11 个 PANAS 风格标签，再用 fixed/frozen EEGPT frozen、Wphysio/Wdeep、B0/A1/A2、openSMILE tokens 训练 fusion encoder + 11-label head。同步 `scripts/README.md` 和命令参考。Synced through current working tree。 | 本地 `PYTHONPATH=src python -m py_compile scripts\multilabel\48_run_fixed_tokens_multilabel_fusion.py` 通过；远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m py_compile scripts/multilabel/48_run_fixed_tokens_multilabel_fusion.py` 通过；远端 H20 smoke `--epochs 1 --limit-runs 1` 通过；远端正式 `cross_day,within_subject_day_strict` × 12 video-only full/no_audio routes × seed 240800 完成 `run_count=24`；本地同步 JSON/Markdown/log 到 `outputs/server_sync/multilabel_fixed_tokens_20260817/`。 | Step 0 label audit 通过，负性标签 `hostile/nervous/afraid/ashamed` 仅标记 sparse extreme-tail 风险。Step 1 完成：`cross_day` 最佳 mean RMSE 为 `A1_Wdeep_no_audio` 0.8774，最佳 mean raw r 为 `A1_Wphysio_full` 0.2539，最佳 fatigue raw r 为 `A2_Wdeep_no_audio` 0.2446；`within_subject_day_strict` 最佳 mean RMSE 为 `B0_Wdeep_no_audio` 0.8144，最佳 mean raw r 为 `B0_Wdeep_full` 0.1386，最佳 fatigue raw r 为 `A1_Wdeep_full` 0.0965。停止在 Step 1 -> Step 2 门槛：strict 主协议信号弱，且远端缺少 `eeg_encoder_256d_tokens/within_subject_day_strict/eegpt_partial_ft_v1/seed_240800.npz`，无法对 Step 2 的 11-label EEG embedding 做 strict paired fatigue-supervised baseline。 |
| 2026-08-17 Asia/Shanghai | 试做 handoff Phase 3 ordinal/distributional head | 扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`，新增 `--heads` 和 `--head-lambdas`，支持 `ordinal_cumulative`、`classification_expectation`、`hybrid_regression_ordinal` 三类 head；新增 `scripts/calibration/46_run_prediction_calibration_phase3_ordinal_head.py`，在 Phase 2 未通过后按 raw MSE baseline 设置执行 4 条代表路线、3 seeds、72 个 paired runs；新增 `scripts/calibration/47_summarize_prediction_calibration_phase3_ordinal_head.py`，按 handoff 的 Phase 3 -> Phase 4 门槛汇总 paired deltas 与 rounded prediction bin/confusion 摘要。Synced through 44ddd57 plus current working tree。 | 本地 `python -m py_compile scripts\window_fatigue\32_run_eegpt_centered_loss.py scripts\calibration\46_run_prediction_calibration_phase3_ordinal_head.py scripts\calibration\47_summarize_prediction_calibration_phase3_ordinal_head.py` 通过；本地 `python -m unittest -v tests.test_phase2_fusion_loss` 因无 Torch 跳过；远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest -v tests.test_phase2_fusion_loss` 通过 6 tests；远端 H20 完成 72 个 Phase 3 runs；远端和本地 `python scripts\calibration\47_summarize_prediction_calibration_phase3_ordinal_head.py` 均输出 `candidate_route_count=20`、`passed=0`。 | Phase 3 没有候选通过进入 Phase 4。最佳相关性局部候选为 `hybrid_regression_ordinal_head_0p3 / cross_day / B0_Wphysio_full`：mean val RMSE delta `-0.0148`、raw r delta `+0.0227`、centered r delta `+0.0289`，但 `pred_std / true_std` delta 为 `-0.0271`，high fatigue absolute bias reduction 仅 `0.0007`，未满足方差和高 fatigue 改善门槛。产物位于 `outputs/server_sync/fatigue_calibration_20260816/phase3_ordinal_head/`。 |
| 2026-08-17 Asia/Shanghai | 完成 handoff Phase 2 候选的 3-seed paired gate | 新增 `scripts/calibration/44_run_prediction_calibration_phase2_paired_gate.py`，对收窄后的 Phase 2 候选执行 3 seeds paired run：`within_subject_day/A2_Wdeep_full` 的 `mse_variance_reg λ=0.05`、`weighted_mse_label_bins`，`within_subject_day/B0_Wdeep_no_audio` 的 `mse_variance_reg λ=0.1`，以及 `cross_day/B0_Wphysio_no_audio` 的诊断候选 `mse_variance_reg λ=0.05`；每个候选与同 route、同 seed 的 raw baseline 配对。新增 `scripts/calibration/45_summarize_prediction_calibration_phase2_paired_gate.py`，按 `prediction_calibration_handoff_20260816.md` 的 Phase 2 -> Phase 3 门槛汇总 val paired delta。Synced through 44ddd57 plus current working tree。 | 本地 `python -m py_compile scripts\calibration\44_run_prediction_calibration_phase2_paired_gate.py scripts\calibration\45_summarize_prediction_calibration_phase2_paired_gate.py` 通过；远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest -v tests.test_phase2_fusion_loss` 通过 4 tests；远端 H20 执行 `scripts/calibration/44_run_prediction_calibration_phase2_paired_gate.py --execute --skip-existing` 完成 21 个 run；远端与本地 `python scripts\calibration\45_summarize_prediction_calibration_phase2_paired_gate.py` 均输出 `candidate_count=4`、`passed=0`。 | 严格 handoff gate 下没有 Phase 2 候选通过进入 Phase 3：最佳 primary 候选 `within_subject_day/A2_Wdeep_full/mse_variance_reg λ=0.05` 的 mean val raw r delta `+0.0051`、centered r delta `+0.0088`、std ratio delta `+0.0047`，低于 `+0.015/+0.020/+0.10` 门槛；`weighted_mse_label_bins` 虽提高 std ratio `+0.0625` 和 high-bias reduction `0.1035`，仍未达 `+0.10/15%` 且 MAE 恶化；cross-day 诊断候选 test centered r mean delta `+0.0219`，但 val centered r 与 std ratio 为负，不能用于进入 Phase 3。产物位于 `outputs/server_sync/fatigue_calibration_20260816/phase2_paired_gate/`。 |
| 2026-08-16 Asia/Shanghai | 执行 fatigue calibration Phase 2 loss/sampler 小矩阵 | 扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`，新增 `--sampler`、label-balanced/label-subject-balanced sampler、`weighted_mse_label_bins`、`huber_extreme_weight` 和 `mse_variance_reg`；新增 `scripts/calibration/42_run_prediction_calibration_phase2.py` 生成并执行四条代表路线的 Phase 2 小矩阵；新增 `scripts/calibration/43_summarize_prediction_calibration_phase2.py`，把 Phase 2 prediction NPZ 与同路线 raw baseline 重算 val/test delta gate 和趋势图；新增 `tests/test_phase2_fusion_loss.py` 覆盖 Phase 2 loss/sampler 组件。Synced through 44ddd57 plus current working tree。 | 本地 `python -m py_compile scripts\window_fatigue\32_run_eegpt_centered_loss.py scripts\calibration\42_run_prediction_calibration_phase2.py scripts\calibration\43_summarize_prediction_calibration_phase2.py tests\test_phase2_fusion_loss.py` 通过；本地 `python -m unittest tests.test_phase2_fusion_loss -v` 因无 Torch 跳过；远端 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest tests.test_phase2_fusion_loss -v` 通过 4 tests；远端 H20 运行 `scripts/calibration/42_run_prediction_calibration_phase2.py --execute --skip-existing` 完成 28 个 run；本地 `python scripts\calibration\43_summarize_prediction_calibration_phase2.py` 输出 `phase2_rows=28`、`val_gated_rows=8`。 | Phase 2 产物位于 `outputs/server_sync/fatigue_calibration_20260816/phase2_loss_sampler/`。8/28 条通过 validation gate，其中代表性通过项包括 `cross_day/B0_Wphysio_no_audio` 的 `mse_variance_reg` λ=0.05/0.1/0.2、`within_subject_day/A2_Wdeep_full` 的 `weighted_mse_label_bins` 和 `mse_variance_reg`、`within_subject_day/B0_Wdeep_no_audio` 的 `mse_variance_reg` λ=0.1；test 上仍有回落，因此这些是可进入 paired multi-seed 复跑的候选，不直接定为最终路线。Phase 1 保留为诊断：variance calibration 能拉高 `pred_std / true_std` 并减轻高 fatigue 低估，但 RMSE/MAE 明显恶化，不能作为正式候选。 |
| 2026-08-16 Asia/Shanghai | 根据 `prediction_calibration_handoff_20260816.md` 执行至 Phase 1 结束 | 新增 `scripts/calibration/39_run_prediction_calibration_phase1.py`，读取冻结 `eeg_encoder_256d_5route_fusion_video_only_seed240800_raw` report 和 prediction NPZ；完成四条代表路线的 Phase 0 preflight、baseline distribution diagnostics、linear/variance/clipped-variance post-hoc calibration、calibrated prediction NPZ、val gate 和 frozen test metrics；新增 `scripts/calibration/40_plot_prediction_calibration_phase1.py`，生成 Phase 1 趋势图和 raw r / centered r 新旧对比表；新增 `scripts/calibration/41_run_prediction_calibration_phase1b.py`，固定 Phase 1 诊断结论并检查 split 分布、高低疲劳覆盖和 2.x 预测塌缩；同步 `scripts/README.md` 与命令参考。Synced through 44ddd57 plus current working tree。 | `python scripts\calibration\39_run_prediction_calibration_phase1.py` 通过并输出 `phase0_ok=true`、`phase1_gate_passed=false`；`python scripts\calibration\40_plot_prediction_calibration_phase1.py --split test` 和 `--split val` 通过；`python scripts\calibration\41_run_prediction_calibration_phase1b.py` 通过；`python -m py_compile scripts\calibration\39_run_prediction_calibration_phase1.py scripts\calibration\40_plot_prediction_calibration_phase1.py scripts\calibration\41_run_prediction_calibration_phase1b.py` 通过。 | Phase 0 通过，确认 representative route prediction NPZ 可读、28819 行元数据对齐、train/val/test index 无重叠，并复现预测动态范围压缩；Phase 1 的 val gate 未通过，因此按 handoff 停在 Phase 1，不进入 Phase 2；Phase 1b 显示 train 低疲劳样本充足但高疲劳样本稀疏，代表路线高 fatigue train rate 约 5%-7%，预测集中在低 2 到低 3 区间，高标签样本仍常被预测到 2.x；可视化和 Phase 1b 产物分别位于 `outputs/server_sync/fatigue_calibration_20260816/phase1_posthoc_calibration/visualizations/` 与 `outputs/server_sync/fatigue_calibration_20260816/phase1b_distribution_check/`。 |
| 2026-08-14 Asia/Shanghai | 根据 `technical_route_20260814.md` 更新项目整体 README，并同步当前工作树到 GitHub main | 将根目录 `README.md` 从早期 smoke/basic 工程入口重写为当前 EEG-aligned 28,819 窗口疲劳预测主线入口，覆盖四模态 256D token、五条 EEG route、cross-attention 融合、最新 `eeg_encoder_256d_5route_fusion_video_only_seed240800_raw` 结果和复现实验顺序；同步 `repo-docs/README.md` 的阅读路径与监督边界说明。Synced through current working tree。 | `python -m compileall -q src scripts tests` 通过；`$env:PYTHONPATH='src'; python -m unittest tests.test_eeg_encoder_matrix tests.test_eeg_encoder_fusion_tokens tests.test_centered_metrics -v` 通过 17 tests；repo-docs validator 为 `OK: 0 errors, 16 warning(s)`。 | README 已更新为当前技术路线入口；后续将按用户要求提交并 force push 到 `origin/main`。 |
| 2026-08-14 Asia/Shanghai | 清理脚本目录，仅保留当前最新 EEG-aligned 四模态路线从 embedding 生成开始的必要入口 | 将 `scripts/` 顶层收敛为 `12_extract_audio_embeddings.py`、`15_extract_wear_embeddings.py`、`27_extract_dinov2_roi_embeddings.py`、`34_run_eeg_encoder_matrix.py` 和 `32_run_eegpt_centered_loss.py`；其余 33 个历史实验、诊断、旧口径和调参脚本移动到 `scripts/archive_legacy/`；新增 `scripts/README.md` 和 `scripts/archive_legacy/README.md`，并在命令参考顶部说明新布局。Synced through current working tree。 | `python -m compileall -q src scripts tests` 通过；`$env:PYTHONPATH='src'; python -m unittest tests.test_eeg_encoder_matrix tests.test_eeg_encoder_fusion_tokens tests.test_centered_metrics -v` 通过 17 tests；repo-docs validator 为 `OK: 0 errors, 15 warning(s)`。 | 完成。 |
| 2026-08-14 Asia/Shanghai | 修复 EEG encoder fusion 矩阵误用 EEG-only prediction scalar token 的口径问题 | 扩展 `daily_multimodal.training.eeg_encoder_matrix` 和 `scripts/embeddings/34_run_eeg_encoder_matrix.py`，支持按 protocol/profile/seed 导出真正的 `eeg_emb (N,256)`；EEGPT frozen 直接复用现有 256D frozen embedding，EEGPT/CBraMod 从 encoder pooled hidden state 经 256D projection 输出，DE+MLP 从监督 MLP 倒数 256D projection 输出；扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`，新增 `--eeg-token-root` 和 `eeg_eegpt_frozen_v1` 分支，以便用 `eeg_encoder_256d_tokens` 与旧 `eeg_encoder_tokens` 分开；更新命令参考，明确旧矩阵是 EEG-only prediction token stacking/校准对照。Synced through current working tree。 | `python -m py_compile scripts\embeddings\34_run_eeg_encoder_matrix.py scripts\window_fatigue\32_run_eegpt_centered_loss.py scripts\archive_legacy\35_prepare_eeg_encoder_prediction_tokens.py src\daily_multimodal\training\eeg_encoder_matrix.py tests\test_eeg_encoder_matrix.py tests\test_eeg_encoder_fusion_tokens.py` 通过；`$env:PYTHONPATH='src'; python -m unittest tests.test_eeg_encoder_matrix tests.test_eeg_encoder_fusion_tokens -v` 通过 12 tests；服务器 `PYTHONPATH=src runtime/envs/eegpt-gpu-min/bin/python -m unittest tests.test_eeg_encoder_matrix tests.test_eeg_encoder_fusion_tokens -v` 通过 12 tests。 | 新正式融合口径应使用 `/vePFS-0x0d/DailyEEG_multimodal/embeddings/eeg_encoder_256d_tokens` 和 `eeg_encoder_256d_5route_fusion_video_only_seed240800_raw` 命名；除 EEGPT frozen baseline 外，这些 EEG tokens 是 fatigue-supervised representations，不能表述为 label-free embedding。 |
| 2026-08-13 Asia/Shanghai | 接手前把工作树中未登记的 EEG tuning sweep 入口补进命令参考 | 检查 `scripts/archive_legacy/36_run_eeg_tuning_sweep.py`（默认 run-id `eeg_tuning_sweep_main_seed240800_20260813`，逐个调用脚本 34 跑 EEGPT/CBraMod/DE 固定超参网格，含 manifest.jsonl 与 summary.json 汇总和跳过已完成配置）；在 `repo-docs/references/commands-and-artifacts.md` 补「EEG 超参 sweep」行。Synced through current working tree。 | 重读命令参考确认表格行与脚本 `build_configs()` 的网格参数一致；`python -m py_compile scripts/archive_legacy/36_run_eeg_tuning_sweep.py` 通过。 | 完成。该脚本此前未出现在 repo-docs 中；接手后可据此在服务器跑超参网格。 |
| 2026-08-12 Asia/Shanghai | 审计 EEGPT partial FT、CBraMod 和 DE+MLP 训练参数并补齐后续 loss history 保存 | 检查 EEG-only encoder matrix 与四模态 fusion matrix 的现有 JSON，确认当前正式结果只保存 best epoch、epoch_count、best validation 和少量 train loss 摘要；扩展 `daily_multimodal.training.eeg_encoder_matrix`，让 EEGPT/CBraMod torch fine-tune 保存 `train_audit.history[{epoch, train_loss, val_rmse}]`，让 DE+MLP 保存 `train_audit.history[{epoch, train_loss, val_loss}]`；扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`，让 fusion head 保存逐 epoch raw/centered loss history；生成 `outputs/server_sync/eeg_encoder_matrix/eeg_training_tuning_audit_2026-08-12.md`。Synced through current working tree。 | `python -m py_compile src\daily_multimodal\training\eeg_encoder_matrix.py scripts\window_fatigue\32_run_eegpt_centered_loss.py tests\test_eeg_encoder_matrix.py` 通过；`$env:PYTHONPATH='src'; python -m unittest tests.test_eeg_encoder_matrix -v` 通过 8 tests。 | 当前已完成的正式 run 没有完整 loss curve；后续调参重跑会在 JSON 的 `train_audit.history` 中保留逐 epoch loss/validation 曲线，可直接用于判断 overfit、underfit 和 LR 不稳。 |
| 2026-08-11 Asia/Shanghai | 将新 EEG encoder 分支加入四模态视频矩阵并去掉 no_video/bio_only | 新增 `scripts/archive_legacy/35_prepare_eeg_encoder_prediction_tokens.py`，把 EEG encoder matrix 的 protocol/seed prediction NPZ 转成 fusion 兼容的 `eeg_emb (N,256)` token；扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`，支持 `--experiment-set video_only`、三种已有协议、多个 `--eeg-branches`、protocol/seed 格式化 EEG token 路径和按当前 route 懒加载分支；新增针对 prediction token 与 video-only experiment set 的单元测试。Synced through current working tree。 | `python -m py_compile scripts\window_fatigue\32_run_eegpt_centered_loss.py scripts\archive_legacy\35_prepare_eeg_encoder_prediction_tokens.py tests\test_eeg_encoder_fusion_tokens.py` 通过；`$env:PYTHONPATH='src'; python -m unittest tests.test_eeg_encoder_fusion_tokens -v` 通过 2 tests；`$env:PYTHONPATH='src'; python -m unittest tests.test_eeg_encoder_matrix tests.test_centered_metrics -v` 通过 13 tests。 | 新矩阵可用 `eeg_eegpt_partial_ft_v1`、`eeg_cbramod_frozen_v1`、`eeg_cbramod_partial_ft_v1`、`eeg_de_5band_1s_avg_v1` 替换原 EEG 分支，并只保留 full/no_audio 的含视频路线。 |
| 2026-08-06 Asia/Shanghai | 实现 EEG encoder 三协议对照矩阵入口 | 新增 `daily_multimodal.training.eeg_encoder_matrix` 和 `scripts/embeddings/34_run_eeg_encoder_matrix.py`；默认覆盖 `cross_subject`、`cross_day`、`within_subject_day` 三种已有协议；加入 EEGPT frozen、EEGPT partial/full fine-tune、CBraMod frozen/partial/full fine-tune、DE 五频带 1 秒平均特征 profile；在配置和命令文档中记录 CBraMod Braindecode/Hugging Face 获取方法；加固 `--max-rows` 下的 frozen embedding 过滤、DE cache 行数校验和 Torch OOM fallback 梯度累积尾批次。Synced through 2cf4012 plus current working tree。 | `$env:PYTHONPATH='src'; python -m unittest tests.test_eeg_encoder_matrix -v` 通过 8 tests；`$env:PYTHONPATH='src'; python -m unittest discover -s tests` 通过 124 tests；`python -m compileall -q src scripts tests` 通过；`python -m py_compile src\daily_multimodal\training\eeg_encoder_matrix.py scripts\embeddings\34_run_eeg_encoder_matrix.py tests\test_eeg_encoder_matrix.py` 通过。 | 新入口可在本地无 Torch 环境跑 DE smoke；服务器正式运行前先执行 `--preflight-only`，并用 `python -m pip install 'braindecode[hub]'` 与 `huggingface-cli download braindecode/cbramod-pretrained --local-dir outputs/checkpoints/cbramod-pretrained` 准备 CBraMod。 |
| 2026-08-06 Asia/Shanghai | 完成 EEGPT 实验 3 初步多 seed 验证 | 在 `within_subject_day` 上对 B0/Wdeep centered-MSE λ=0.5 与 A1/Wphysio centered-correlation λ=0.1 分别执行五个同-seed raw 对照；同步 20 个单次结果和汇总 JSON/Markdown，并更新结果报告与 handoff。 | 20 个远端 GPU run 全部完成；每个候选均有 5 个 raw/candidate 配对；汇总 JSON 的 decision 为 `do_not_advance_to_formal_residual_head`。 | B0 mean Δcentered r=`-0.0063±0.0261`、3/5 为正；A1 mean Δcentered r=`+0.0079±0.0135`、4/5 为正，未达到 `+0.02` 门槛。 |
| 2026-08-06 Asia/Shanghai | 按修复后的 `within_subject_day` 重做 EEGPT centered-r 改进实验 1–2 | 将 canonical `within_subject_day` 写回远端可用 split 根目录；覆盖 split audit、subject-mean baseline 和 27-run centered-loss sweep；同步同名 JSON/Markdown 产物并重写实验结果报告。 | 远端划分 train/val/test subject-day overlap 全为 `0`；实验 1 train/test 为 `17135/6026`；实验 2 `run_count=27`；本地结果文件与报告已同步。 | `within_subject_day` subject-mean raw r `0.2806`；centered-loss sweep 最高 centered r `0.0386`（该协议），未形成稳定 centered-r 增益。 |
| 2026-08-06 Asia/Shanghai | 按 strict `within_subject_day` 结果重写 EEGPT 融合报告结论口径 | 修改 `docs/superpowers/reports/2026-08-01-eegpt-b0-a1-a2-cross-attention-results.md` 的摘要、协议级正式结果、centered r 解读、数据划分说明、消融解释、限制和下一步；将原始 `within_subject_day` 明确降级为窗口级诊断，将 `within_subject_day_strict` 作为同一被试 held-out-day 的正式口径。 | 本地 `rg` 检查 strict 数值和旧 within 表述位置；Markdown 仅文案更新，未新增实验运行。 | 报告正式结论现在以 `cross_day` 和 `within_subject_day_strict` 为主：strict 最低 RMSE `0.9678`、最高 raw r `0.1115`、最高 centered r `0.0682`；旧 `within_subject_day` 的 `0.3032–0.3161` raw r 只作为窗口级诊断参考。 |
| 2026-08-06 Asia/Shanghai | 修复 `within_subject_day` subject-day 重叠并完成 strict 重跑 | 新增 `scripts/archive_legacy/33_build_strict_within_subject_day_split.py`，按 subject 内 day 顺序构造 subject-day pair 不重叠的 `within_subject_day_strict`；扩展 `scripts/window_fatigue/32_run_eegpt_centered_loss.py`，支持原 16 个 B0/A1/A2 route-aware 实验、`raw`-only 运行和任意 protocol 名；在新服务器 `wangziwei@124.174.8.252:36083` 生成 strict split、审计 overlap，并完成 16 组合 GPU 重跑；更新 2026-08-06 改进报告、2026-08-01 完整报告和 handoff。 | 本地 `$env:PYTHONPATH='src'; python -m compileall -q src scripts tests` 通过；本地 `$env:PYTHONPATH='src'; python -m unittest tests.test_centered_metrics -v` 通过 5 tests；远端 `within_subject_day_strict` audit 显示 train/val/test subject-day overlap 全为 0；远端 strict route matrix 完成 `run_count=16`。 | strict split 规模为 train `17135`、val `5658`、test `6026`，subject-day pair 为 train `90`、val `30`、test `30`。strict test 最低 RMSE 为 `B0_Wdeep_bio_only` `0.9678`；最高 raw r 为 `A2_Wdeep_no_audio` `0.1115`；最高 centered r 为 `B0_Wphysio_no_video` `0.0682`。原始 `within_subject_day` 降级为窗口级诊断，严格日期泛化以 `cross_day` 和 `within_subject_day_strict` 解释。 |
| 2026-08-06 Asia/Shanghai | 完成 EEGPT centered-r 改进实验 1–2 并审计 split | 新增 `scripts/archive_legacy/30_audit_eegpt_splits.py`、`scripts/archive_legacy/31_run_subject_mean_baseline.py`、`scripts/window_fatigue/32_run_eegpt_centered_loss.py` 和 `training/centered_metrics.py`；在远端固定 EEGPT index、`splits_new` 与多模态 embedding 上完成 split audit、subject-mean baseline 和 27 个 centered-loss sweep run；同步 JSON/Markdown 结果并生成 `docs/superpowers/reports/2026-08-06-eegpt-centered-improvement-results.md`。Synced through b83c15a plus current working tree。 | 本地 `python -m compileall -q src scripts tests` 通过；本地 centered metrics unittest 通过 4 tests；远端四个脚本 `py_compile` 通过；远端实验 1 输出 28819 行 audit，实验 2 完成 `run_count=27`。 | `cross_day` subject-day train/test overlap 为 0；`within_subject_day` train/val/test 两两重叠 150 个 subject-day pairs。subject-mean raw r 为 `0.2828`/`0.4448`；centered loss 未达到预设绝对提升和 RMSE 代价门槛。当前先修复严格日期级 split，再决定 residual head 实验 3。 |
| 2026-08-05 Asia/Shanghai | Add PPT-style HTML briefing for EEGPT fusion report | Added `slides.html` next to the portable report HTML. The new file presents the EEGPT B0/A1/A2 multimodal fusion results as a 10-slide, 16:9 presentation with keyboard navigation, slide dots, metric cards, protocol winner charts, raw-vs-centered-r explanation, alignment notes, fusion architecture, branch definitions, ablation interpretation, and recommended next steps. | Rendered desktop `1440x900` and mobile `390x844` screenshots with headless Chromium and visually checked the cover layout for slide-like composition, readable typography, and no obvious overlap. | PPT-style HTML briefing is available at `docs/superpowers/reports/html/2026-08-01-eegpt-b0-a1-a2-cross-attention/slides.html`. |
| 2026-08-05 Asia/Shanghai | Build HTML briefing for EEGPT B0/A1/A2 fusion report | Created a portable HTML report package under `docs/superpowers/reports/html/2026-08-01-eegpt-b0-a1-a2-cross-attention/`, with `artifact.json`, `source_query.sql`, and `report.html`. The HTML report keeps the Chinese technical narrative, protocol-level findings, centered-r interpretation, split/windowing notes, modality definitions, charts, and complete 48-row audit tables split into error and correlation views. | Generated the portable artifact from the Markdown report, packaged it with the Data Analytics portable builder, patched a top-bar `100vw` overflow in the final HTML, and ran `npm.cmd run report:verify` against the final file. Verification passed with `blocks=26`, `charts=3`, `metrics=6`, `tables=6`, source dialog passed, and desktop/mobile viewport checks at `1440` and `390` px. | HTML briefing is available at `docs/superpowers/reports/html/2026-08-01-eegpt-b0-a1-a2-cross-attention/report.html`. |
| 2026-08-05 Asia/Shanghai | Merge EEGPT B0/A1/A2 fusion report to main | Added `docs/superpowers/reports/2026-08-01-eegpt-b0-a1-a2-cross-attention-results.md` to main with the completed B0/A1/A2 EEGPT multimodal cross-attention report, including split/windowing notes, raw-vs-centered-r interpretation, and B0/A1/A2/Wphysio/Wdeep definitions. | Report structure was checked before merge: windowing/split/abbreviation anchors were present, the full result table contained 48 rows, and `git diff --check` passed. | Main now carries the user-facing complete report without merging the broader `codex/video-modality-roadmap` history. |
| 2026-07-03 Asia/Shanghai | 按新版 2 倍主脸 ROI 预处理结果重跑 Face embedding | 在服务器 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding` 用 `outputs/window_index/real_cache_face_detected_full_v2_mainface.jsonl` 和 `outputs/cache/real_stage12_face_filter_full_v2_mainface` 中已生成的 ROI `window.mp4` 重跑 `scripts/archive_legacy/13_extract_face_embeddings.py`；cache profile 使用 `openface_temporal_v1`，OpenFace wrapper 为 `/mnt/dataset4/sitian/wzw/tools/openface/feature_extraction.sh`。为避免单进程过慢，将 8328 行窗口索引拆成 8 个 chunk 并行写 CSV/单块 `.npz`，再合并成 full mainface ROI face `.npz`、失败清单和质量 summary。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 服务器 `PYTHONPATH=src python -m pytest tests/test_face_real_embedding.py -q` 通过 14 tests；20 窗口 smoke 成功产出 `embedded_count=20`、`success_count=1`、`nan_count=0`；8 chunk 合并后用 conda `lzs` 环境读取 `.npz` 确认 `face_emb=(7603,256)`、`modality_mask=(7603,4)`、face mask sum `1554`、NaN `0`。 | 完成。新产物为 `outputs/embeddings/face_openface_real_full_mainface_roi_embeddings.npz`、`outputs/reports/face_openface_real_full_mainface_roi_quality_summary.json` 和 `outputs/reports/face_openface_real_full_mainface_roi_failures.json`；summary 为 `embedded_count=7603`、`success_count=1554`、`masked_count=6049`、`extraction_failed=725`、`quality_threshold_failed=6049`，相对旧 full OpenFace face mask `207` 明显提升。 |
| 2026-07-03 Asia/Shanghai | 将 Wear-only 消融扩展为 W1-W7，并拆分 W5a-W5d 比较 quality flags 与 sample weight | `wear_quality_ablation.py` 新增 A+B 子集实验 W6 physio / W7 deep；将原 W5 拆为 W5a deep full、W5b deep+quality flags、W5c deep+sample weights、W5d deep+flags+weights；W5a-W5d 使用同一 seed offset 做更公平对照；CLI 描述和测试同步更新。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding tests.test_wear_quality_ablation` 通过 15 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_wear_quality_ablation.py -q` 通过 2 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器全量 `scripts/archive_legacy/22_run_wear_quality_ablation.py` 完成 `experiment_count=10`。 | 完成。Wear-only fatigue 输出在 `outputs/reports/wear_quality_ablation_w1_w7_w5abcd_fatigue_metrics.json` 和 `.md`；W6/W7 A+B 子集为 `6208` 窗口。关键结果：W1 RMSE `0.8855±0.1906`、r `-0.0387±0.1446`；W5a `0.9231±0.1780`、r `-0.0394±0.1200`；W5b `0.9190±0.1754`、r `0.0026±0.0908`；W5c `0.9271±0.1783`、r `-0.0311±0.1173`；W5d `0.9221±0.1769`、r `0.0093±0.0832`；W6 `0.8806±0.1990`、r `-0.0307±0.1183`；W7 `0.9242±0.1846`、r `-0.0160±0.1013`。 |
| 2026-07-02 Asia/Shanghai | 将 wear 消融改为 Wear-only W1-W5，并输出预测分布统计 | `wear_real.py` 新增 A/B/C wear 质量分级、`wear_deep_sequence_v1` 固定随机 1D conv/TCN-like sequence baseline、`--mask-low-quality-wear` 可选低质量 mask；新增 `training/wear_quality_ablation.py` 和 `scripts/archive_legacy/22_run_wear_quality_ablation.py`，直接从 wear 单模态 `.npz` 与 window index 标签运行 W1 physio full、W2 deep full、W3 physio high-quality、W4 deep high-quality、W5 deep+quality flags full。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding tests.test_wear_quality_ablation` 通过；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_wear_real_embedding.py tests/test_wear_quality_ablation.py -q` 通过 15 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器重跑 physio/deep full 均 `success_count=7584`、`source_missing=744`、NaN `0`，质量分布 A/B/C=`3307/2901/1376`。 | 完成。Wear-only fatigue W1-W5 输出在 `outputs/reports/wear_quality_ablation_w1_w5_fatigue_metrics.json` 和 `.md`；结果：W1 RMSE `0.8855±0.1906`、r `-0.0387±0.1446`；W2 RMSE `0.9320±0.1800`、r `-0.0492±0.1094`；W3 RMSE `0.9102±0.2194`、r `-0.0524±0.1315`; W4 RMSE `0.9475±0.1883`、r `0.0256±0.1393`; W5 RMSE `0.9212±0.1738`、r `-0.0046±0.1039`。 |
| 2026-07-02 Asia/Shanghai | 升级 8328 个 face-filtered 窗口的 wear 信号质量检查 | `wear_real.py` 新增 `quality_audit` 汇总，覆盖 rows/rate、invalid/source、duplicate/nonmonotonic timestamps、flatline、PPG peak/heart-rate plausibility、GSR slope/SCR IQR 异常、ACC motion/stationary；`wear_physio_features_v2` 在缺 profile metadata 时可复用 `wear_sequence_v1` metadata 或直接读取 window index 的 wear 源路径；重复秒级时间戳按行顺序摊开用于插值，不再丢弃原始样本。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地 `PYTHONPATH=src python -m unittest tests.test_wear_real_embedding` 通过 11 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_wear_real_embedding.py -q` 通过 11 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 full mainface quality audit 输出 `quality_audit.window_count=8328`、`success_count=7584`、`source_missing=744`、`wear_emb=(7584,256)`、NaN `0`。 | 完成。最终 summary 为 `outputs/reports/wear_physio_features_v2_full_mainface_quality_audit_summary.json`；PPG/ACC 原始有效采样率为 `20 Hz`，GSR 为 `40 Hz`，三路 invalid/source ratio 均为 `0`，所有成功窗口有秒级重复时间戳但非单调数为 `0`，flatline 窗口为 `0`。 |
| 2026-07-02 Asia/Shanghai | 对所有 face-detected 窗口执行 2 倍主脸 ROI 预处理并统计 | 在服务器 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding` 对 `outputs/window_index/real_cache_face_detected_full_v2_mainface.jsonl` 的 `8328` 个窗口批量生成 ROI 版 `window.mp4` 与 `window_face_roi.json`，复用 `outputs/cache/real_stage12_face_filter_full_v2_mainface/openface/*/openface_temporal_v1/` cache 结构；补写汇总 `outputs/reports/face_roi_preprocess_full_v2_summary.json`。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 服务器文件计数确认 `openface_target.json=8328`、`window.mp4=8328`、`window_face_roi.json=8328`；汇总脚本确认 `clip_missing_count=0`、总帧数 `41640`、直接 ROI 帧 `19663`、补 ROI 帧 `21977`、至少一帧可靠 ROI 的窗口 `5602`、整窗全画面兜底 `2726`。 | 完成。全量 ROI 预处理已落盘，后续 OpenFace 可直接读取 ROI 版 `window.mp4`。 |
| 2026-07-02 Asia/Shanghai | Face 视频预处理改为 2 倍主脸 ROI，并在无脸帧沿用上一 ROI | `scripts/archive_legacy/13_extract_face_embeddings.py` 新增默认 `--face-roi-crop-scale 2.0` 和 `--skip-face-roi-crop`；`face_real.py` 在 OpenFace 前生成 2 倍主脸 ROI 的 `640x640` `window.mp4`，逐帧检测最大主脸，个别无脸帧沿用上一有效 ROI，开头无脸使用后续第一个 ROI，整窗没有可靠 ROI 时退回全画面；新增 ROI sidecar 并把 `face_roi_*` 字段写入 `quality_flags`。为避免 Haar 小框假阳性裁坏画面，加入最小面积比例门槛。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 本地新增 ROI TDD 测试先因缺 `_expanded_face_roi` / `_face_roi_sequence` / `face_roi_crop_scale` 失败，再实现并通过；本地 `PYTHONPATH=src python -m unittest tests.test_face_real_embedding -v` 通过 14 tests，`python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_face_real_embedding.py -q` 通过 14 tests，`python -m compileall -q src scripts tests` 通过；服务器真实窗口 ROI smoke 生成 `640x640`、`0.5 fps`、5 帧 clip，大脸样例 `face_roi_detected_frame_count=4`、`face_roi_filled_missing_frame_count=1`。 | 完成。ROI 裁剪默认 2 倍；小脸/低置信窗口退回全画面，避免不现实的假阳性裁剪。 |
| 2026-07-02 Asia/Shanghai | 确认 no-face 是否误删手部遮挡/倒转画面，并统计多脸后仅保留主脸 | 对 240 个 no-face 和 240 个 kept 窗口做服务器抽样审计并生成 contact sheet；发现 no-face 抽样中 `131/240` 可被 alt/180 度/侧脸检测重新命中，且肉眼包含倒转、手/手机/电脑遮挡和侧脸。`cache.py` 的 face-presence detector 扩展为正向正脸、alt 正脸、侧脸、180 度倒转正脸；`face_presence` 新增 `max_face_count`、`main_face_bbox`、`main_face_area_ratio`、`detected_orientations`、`retained_without_detected_face` 和 `retention_reason`；同一评分事件其它窗口有脸时，无脸中点窗口保留并标记上下文保留。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 新增 TDD 测试先因缺 `face_presence_summary_from_detections` 失败，再实现并通过；本地 `PYTHONPATH=src python -m unittest tests.test_real_pipeline tests.test_face_real_embedding tests.test_audio_real_embedding tests.test_event_window tests.test_video_audio_alignment` 通过 39 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests/test_real_pipeline.py -q` 通过 11 tests，`python -m compileall -q src scripts tests` 通过；服务器全量 `scripts/archive_legacy/11_prepare_real_embedding_cache.py` v2 输出 `selected_window_count=8328`、`face_filter_dropped_count=312`。 | 完成。过滤后索引 `outputs/window_index/real_cache_face_detected_full_v2_mainface.jsonl` 为 `8328` 行；`retained_without_detected_face=1939`，直接检到脸 `6389`，`max_face_count>=2` 为 `2308` 个窗口；最终 no-face 剔除样例主要为空椅、桌面或无主脸画面。 |
| 2026-07-01 Asia/Shanghai | 在服务器上全量验证两分钟前向切窗和人脸检测剔除窗口 | 将当前工作区代码同步到服务器 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding`；重建 `outputs/window_index/window_index.jsonl` 和 `outputs/reports/window_index_summary.json`；用 OpenCV Haar + ffmpeg midpoint 并行 face-presence detector 运行全量 `scripts/archive_legacy/11_prepare_real_embedding_cache.py`，输出 `outputs/window_index/real_cache_face_detected_full_midpoint.jsonl`、`outputs/reports/real_embedding_readiness_face_filter_full_midpoint.md` 和 `outputs/reports/real_embedding_failures_face_filter_full_midpoint.json`。Synced through fb37e99df688d3e6dc30c103cc4b91b6f3941345 plus current working tree。 | 服务器 `PYTHONPATH=src python -m pytest tests/test_event_window.py tests/test_video_audio_alignment.py tests/test_real_pipeline.py tests/test_face_real_embedding.py tests/test_audio_real_embedding.py -q` 通过 33 tests，后续 face filter 性能改动后 `PYTHONPATH=src python -m pytest tests/test_real_pipeline.py -q` 通过 9 tests；服务器 `python -m compileall -q src scripts tests` 通过；窗口索引 summary 为 `events_total=1272`、`events_selected=720`、`events_skipped=552`、`windows_total=8640`、`skip_reasons={insufficient_video_coverage:502, insufficient_pre_event_history:50}`；全量 face filter readiness 为 `selected_window_count=4097`、`face_filter_dropped_count=4543`、`face_filter_dropped_no_face_count=4543`。 | 完成。过滤后索引行数为 `4097`；失败清单共 `4907` 条，其中 `no_face_detected=4543`、`source_missing=324`、`extraction_failed=40`。 |
| 2026-07-01 Asia/Shanghai | 两分钟前向切窗并在人脸预检后重建真实缓存入口 | `event_windows.py` 默认改为情绪评分前 `-120` 到 `0` 秒、10 秒窗口、10 秒步长；新增窗口 summary，统计不足两分钟历史和精确视频覆盖不足的事件；每个 10 秒样本重算 `video_candidates` clip 秒数。`scripts/archive_legacy/11_prepare_real_embedding_cache.py` 默认先用 OpenCV Haar face-presence detector 过滤无脸窗口，写出 face-detected window index，并把 `no_face_detected` / `face_detection_failed` 纳入失败清单契约。同步更新事件窗口、walkthrough、字段契约、embedding 契约和命令参考。Synced through f51af7381779166c4d892da6c3a9657081a06970 plus current working tree。 | 先用 `PYTHONPATH=src python -m unittest tests.test_event_window tests.test_real_pipeline.RealPipelineCacheTests` 观察到缺新 API，再实现并通过；相关回归 `PYTHONPATH=src python -m unittest tests.test_event_window tests.test_video_audio_alignment tests.test_real_pipeline tests.test_face_real_embedding tests.test_audio_real_embedding` 通过 34 tests；`python -m compileall -q src scripts tests` 通过；重建 `outputs/window_index/window_index.jsonl` 和 `outputs/reports/window_index_summary.json` 得到 `events_selected=720`、`events_skipped=552`、`windows_total=8640`。 | 代码和窗口索引完成；本地 Windows 副本不能访问 `/mnt/dataset...` 真视频路径，face-detected cache 需在服务器/挂载可用环境重跑。 |
| 2026-07-01 Asia/Shanghai | 抽查 OpenFace `Starting tracking` 后无 CSV 并增加显式 fallback | 从服务器 `face_openface_real_full_failures.json` 中确认 `extraction_failed=73`，其中 `72` 条日志停在 `Starting tracking`；按 subject 抽样 8 个窗口生成 contact sheet 到 `outputs/reports/face_abort_audit/`，画面主要是边缘脸、远景、低头、手机/桌面遮挡。`face_real.py` 在 OpenFace 对窗口 clip 抛错且显式 fallback 可用时，对同一个 `window.mp4` 生成 OpenCV-compatible CSV，并在 `quality_flags` 写 `openface_fallback_used`、`openface_fallback_stage`、`openface_fallback_reason`。Synced through 1a02a84 plus current working tree。 | 先用 `PYTHONPATH=src python -m unittest tests.test_face_real_embedding.FaceRealEmbeddingTests.test_openface_starting_tracking_no_csv_can_fallback_to_generator` 观察到失败，再实现并通过；`PYTHONPATH=src python -m unittest tests.test_face_real_embedding -v` 通过 10 tests。 | 完成 fallback 接入；它只减少无 CSV 失败，窗口是否可用仍由 `face_detection_success_rate` mask 决定。 |
| 2026-06-29 00:00 Asia/Shanghai | 用 `repo-docs-zh` 为仓库生成中文 repo-docs | 新增中文 `repo-docs/` 包，覆盖 README、主 walkthrough、事件窗口模块、embedding 契约模块、命令产物参考、字段契约、术语表；新增根目录 `AGENTS.md` 维护规则。Synced through 7c75bd3；同时检查了当前未提交工作区状态。 | `python -m pytest tests -q` 通过；repo-docs validator 为 `OK: 0 errors, 6 warning(s)`，剩余 warning 是开头代码名密度和示例数字、示例 `sample_id` 是否要进术语表的阅读提示。 | 完成。 |
| 2026-06-29 Asia/Shanghai | 完成多模态嵌入计划阶段 8 到阶段 9 | 新增完整候选集 basic embedding 提取入口和 numpy baseline MLP 训练入口；更新 README、主 walkthrough、命令产物参考，说明 `scripts/archive_legacy/07_extract_all_embeddings.py` 已不再是占位入口，并记录阶段 9 subject split baseline。Synced through 4ecb350。 | `python -m pytest tests -q` 通过；`python -m compileall -q src scripts tests` 通过；临时合成数据 CLI smoke 跑通阶段 8 和阶段 9，阶段 8 `selected_windows=12`、`failure_count=0`，阶段 9 `overfit_passed=True`。 | 完成。 |
| 2026-06-29 Asia/Shanghai | 上服务器验证阶段 8 到阶段 9 | 将本地 `configs/`、`src/`、`scripts/`、`tests/`、`repo-docs/` 和 `pyproject.toml` 同步到 `/mnt/dataset4/sitian/wzw/DailyMultimodalEmbedding`；先跑小批量 10 窗口 smoke，再跑完整候选集和 full baseline。Synced through 4ecb350。 | 服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 19 tests；`python -m compileall -q src scripts tests` 通过；阶段 8 小批量 `selected_windows=995`、`success_count=10`、`failure_count=0`；阶段 8 全量 `success_count=995`、`failure_count=0`；阶段 9 全量 `overfit_passed=True`，subject split 为 train `sub-02` 到 `sub-10`、val `sub-11` 到 `sub-12`、test `sub-13` 到 `sub-15`。 | 完成。 |
| 2026-06-29 Asia/Shanghai | 按阶段完成阶段 10，并维护阶段 10 执行报告 | 新增 `scripts/archive_legacy/10_run_upgrade_ablation.py`、`daily_multimodal.training.upgrade_ablation`、`tests/test_upgrade_ablation.py` 和 `阶段10执行报告.md`；实现 baseline reference 快照、registry smoke 框架、subject split 失败清单、`modality_token_attention` 融合升级和 accepted/rollback 对照表。Synced through b4fcc32。 | 本地 `python -m pytest tests -q` 通过 24 tests；`python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 24 tests；阶段 10 小批量 registry smoke 正确记录 `subject_split_incomplete`；阶段 10 全量 `modality_token_attention` 对照 `baseline_metric=0.8756`、`upgrade_metric=0.6968`、`delta=-0.1788`、`decision=accepted`，失败清单为 `[]`。 | 完成。 |
| 2026-06-29 14:10 Asia/Shanghai | 完成真实多模态完整 embedding 计划阶段 11 到阶段 12 | 新增真实 embedding 契约、失败清单 schema、四模态缓存准备模块和 `scripts/archive_legacy/11_prepare_real_embedding_cache.py`；让窗口索引读取兼容 UTF-8 BOM；更新执行报告、实施计划勾选、README、主 walkthrough、embedding 契约模块和命令产物参考。Synced through b4fcc32。 | `python -m pytest tests/test_real_embedding_contracts.py tests/test_embedding_failures.py tests/test_real_pipeline.py -q` 通过 9 tests；本地 `python -m pytest tests -q` 通过 34 tests；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 34 tests；本地和服务器 `python -m compileall -q src scripts tests` 通过；repo-docs validator 为 `OK: 0 errors, 6 warning(s)`；服务器阶段 12 用 10 条完整且带 `video_candidates` 的窗口验证，EEG、Wear、Face、Audio 均为 `ready=10, missing=0, failures=0`，失败清单为 `[]`。 | 完成。 |
| 2026-06-29 19:47 Asia/Shanghai | 开始阶段 13 Audio 真实 embedding | 新增 `daily_multimodal.embeddings.audio_real`、`scripts/embeddings/12_extract_audio_embeddings.py`、`tests/test_audio_real_embedding.py`，并更新 audio real profile、执行报告、主 walkthrough、embedding 契约和命令参考。Synced through pending phase-13 commit。 | `python -m pytest tests -q` 通过 38 tests；`python -m compileall -q src scripts tests` 通过；服务器 `lzs` 环境具备 `torch`、`torchaudio`、`transformers`，`facebook/wav2vec2-base-960h` safetensors fallback 已跑通 10 窗口 `(10, 256)` 和 `sub-12` 单被试 `(25, 256)`，失败清单均为 `[]`、NaN 为 0。 | 单被试完成；全量和 ablation 后续统一执行。 |
| 2026-06-30 Asia/Shanghai | 完成阶段 17 all-real 单被试打包入口 | 新增 `daily_multimodal.embeddings.real_pipeline` 和 `scripts/archive_legacy/16_extract_all_real_embeddings.py`，以 window index 为主表合并 EEG/Wear/Face/Audio 单模态真实 `.npz`；更新真实 embedding 契约、字段契约、命令产物参考、实施计划和执行报告。Synced through 20c7c54。 | 本地 `python -m pytest tests/test_real_pipeline.py -q` 通过 5 tests；本地 `python -m pytest tests -q` 通过 60 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 60 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 `sub-12` all-real 打包输出四个 `(25, 256)` embedding、mask sum `[25, 25, 25, 25]`、失败清单 `[]`。 | 单被试完成；全量和阶段 18 ablation 后续统一执行。 |
| 2026-06-30 Asia/Shanghai | 完成阶段 18 real ablation 入口和单被试 smoke | 新增 `daily_multimodal.training.real_embedding_ablation`、`scripts/archive_legacy/17_run_real_embedding_ablation.py` 和 `tests/test_real_embedding_ablation.py`；更新实施计划、执行报告和命令产物参考。Synced through 6fcb1a5。 | 本地 `python -m pytest tests/test_real_embedding_ablation.py -q` 通过 3 tests；本地 `python -m pytest tests -q` 通过 63 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 63 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 `sub-12` smoke ablation 产出 `subject_split_incomplete` failures，说明单被试不能训练但入口和失败语义可用。 | 入口完成；全量 all-real ablation 和最终结论后续执行。 |
| 2026-06-30 20:23 Asia/Shanghai | 用 `repo-docs-zh` 更新项目 repo-docs | 同步 repo-docs 到当前 `e64b29f` 和当前工作区 EEG coverage 审计改动：README 改为 basic 到 real 的完整工程线；主 walkthrough 补阶段 14-18、fair leakage controls 和 EEG coverage audit；命令参考补 `scripts/archive_legacy/18_run_fair_embedding_ablation.py`、`scripts/archive_legacy/19_audit_eeg_coverage.py`；字段契约和术语表补 fair/all-real/real ablation/EEG coverage。Synced through e64b29f plus working tree EEG coverage files。 | `python -m pytest tests -q` 通过 76 tests；`python -m compileall -q src scripts tests` 通过；repo-docs validator 为 `OK: 0 errors, 8 warning(s)`，剩余 warning 为阅读提示、外部计划文档缺 agent 规则提示和工作区文件晚于同步锚点提示。 | 完成。 |
| 2026-06-30 Asia/Shanghai | 同步 real embedding v2 代码、服务器 smoke 和阻断状态 | 新增 fair leakage audit、true OpenFace window clip、EEG coverage audit、audio emotion profiles、wear physio v2、subject-level CV；repo-docs 补 v2 profile、Subject CV 命令和 Task 6 阻断说明。Synced through 32259c1。 | 本地 `python -m pytest tests -q` 通过；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m unittest discover -s tests` 通过 86 tests；服务器 `python -m compileall -q src scripts tests` 通过；服务器 fair audit `row_count=781`、`sample_id_aligned=True`、`failure_count=0`；EEG coverage audit `in_range=738`、`negative_offset=29`、`after_recording_end=4`、`whole_day_shift_candidate=10`；Wear v2 10-window `success_count=10`、`nan_count=0`；Subject CV on current all-real `fold_count=14`、`subject_leakage=False`。 | 代码与 smoke 完成；完整 all-real v2 未验收，因 OpenFace Apptainer pull 访问 Docker Hub 超时，服务器缺 `opensmile`、`modelscope` 和默认 Python 下的 `torch`。 |
| 2026-07-01 Asia/Shanghai | 完成 real embedding v2 full、fatigue 下游验证和 repo-docs 同步 | OpenFace Apptainer 通过 Huawei mirror wrapper 接入；EEG full v2、Wear physio v2 full、openSMILE full、true OpenFace full 和 all-real v2 打包完成；fair ablation 与 subject CV 支持 `--modalities` 并输出 Pearson r；下游目标从 `alert` 切为 `fatigue`。Synced through 4b53a36 plus current working tree。 | 本地 `python -m pytest tests -q` 通过 87 tests；本地 `python -m compileall -q src scripts tests` 通过；服务器 `PYTHONPATH=src python -m pytest tests -q` 通过 87 tests；服务器 `python -m compileall -q src scripts tests` 通过；repo-docs validator 为 `OK: 0 errors, 7 warning(s)`；服务器 v2 pack `selected_windows=781`、shape 均 `(781, 256)`、mask sum `[738, 781, 69, 781]`、NaN 为 0；fatigue EEG/Wear/Audio fair real RMSE `1.0160`、r `0.1205`；fatigue EEG/Wear/Audio subject-CV `fold_count=14`、`subject_leakage=False`、RMSE mean `0.9697`、r mean `0.0636`。 | 完成；四模态 subject-CV 因 Face true OpenFace 覆盖稀疏出现空 fold，稳健验证使用 EEG/Wear/Audio 子集。 |
| 2026-07-01 Asia/Shanghai | 生成本地数据概况图 | 新增 `scripts/archive_legacy/21_draw_data_overview.py`，从本地 manifest、window index 和 real embedding 质量摘要生成被试级模态完整性 SVG、完整被试时间轴 SVG、完整率 CSV 和摘要 JSON；命令产物参考补该入口。Synced through 1a02a84 plus current working tree。 | `python scripts/archive_legacy/21_draw_data_overview.py` 生成 `outputs/figures/subject_modality_completeness.svg` 和 `outputs/figures/sub-10_modality_timeline.svg`；`python -m py_compile scripts/archive_legacy/21_draw_data_overview.py` 通过；SVG 可被 XML parser 解析；repo-docs validator 复查。 | 完成；应用内浏览器安全策略阻止直接打开本地 `file://` SVG，改用结构解析和摘要数字核对。 |
| 2026-07-01 Asia/Shanghai | 生成 `sub-10` 单日精确视频音频时间轴 | `scripts/archive_legacy/21_draw_data_overview.py` 新增 `--date` 单日过滤，优先用 `video_candidates` 的 `mp4_start_time`/`mp4_end_time` 画精确 MP4 和音频流横条，并在评分事件标注 fatigue 与对应 MP4；从服务器同步精确 manifest/window index 到 `outputs/server_sync/`；命令产物参考补单日精确用法。Synced through 1a02a84 plus current working tree。 | `python -m unittest tests.test_data_overview_figures -v` 通过；`python scripts/archive_legacy/21_draw_data_overview.py --manifest outputs/server_sync/events_manifest_with_video_audio.jsonl --window-index outputs/server_sync/window_index_with_video_audio.jsonl --subject-id sub-10 --date 2025-07-08 --out-dir outputs/figures` 生成 `outputs/figures/sub-10_2025-07-08_precise_timeline.svg`；summary 核对 `events_total=11`、`precise_video_full_coverage=11`、`precise_audio_full_coverage=11`；SVG XML parse 通过；repo-docs validator 复查。 | 完成。 |
| 2026-07-01 Asia/Shanghai | 修复 true OpenFace HAAR/MTCNN 与 codec 失败路径 | `face_real.py` 对 Apptainer wrapper 自动传入容器内 HAAR cascade `-fdloc`，避免 FeatureExtraction 找不到 HAAR 后走 MTCNN/ONet 并 segfault；OpenFace 返回非零但已生成非空 CSV 时继续解析 CSV，避免 `mpeg4` VideoWriter 警告误判为失败。Synced through 1a02a84 plus current working tree。 | 新增 Face 单测覆盖 `-fdloc` 和非零返回码 CSV 复用；服务器 `PYTHONPATH=src python -m pytest tests/test_face_real_embedding.py -q` 通过 9 tests；Face full 重跑后 `success_count=207`、`masked_count=501`、`extraction_failed=73`，all-real v2 mask sum `[738, 781, 207, 781]`；四模态 fatigue fair real RMSE `1.3647`、r `-0.1437`，四模态 LOSO 仍因 `sub-13` 无四模态完整样本空 fold。 | 完成目标两类修复；剩余 Face 失败主要是低检测成功率和少量 `Starting tracking` 后无 CSV。 |
